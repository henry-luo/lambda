#pragma once

// Transitional core forwarding header. The authoritative MarkReader surface
// is core-owned; callers migrate to core/mark_reader.hpp without duplicate
// declarations.
#include "core/mark_reader.hpp"
