[1, true, null, "hello", 'symbol', {"key": "value", 'symbol': false, name: 'value'}, "string can 
    span multiple lines", 
    1.0, 1.0e10, 1.0e-10, 
    1 to 10, a + b, // a - b, a * b, a / b, a % b, a ** b, a and b, a or b, not a, a < b, a > b, a <= b, a >= b, a == b, a != b, a in b ]
]
fn main() {
    let (a = 10 + 5) a - 3
}