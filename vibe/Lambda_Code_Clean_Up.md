# Lambda / Radiant Code Clean-Up Survey

**Date:** 2026-07-11
**Method:** Six parallel code-survey agents, each auditing one area against `./lib` and against sibling code. Every finding below was verified by reading the cited source (not inferred from file names). Line numbers are current-tree as of commit `95b2689fa`.

**Scope:** `lambda/` core (top-level), `lambda/input/`, `lambda/format/` + `lambda/validator/`, `radiant/` layout+CSS side, `radiant/` paint/display-list/editing side, `lambda/js/` vs Lambda core.

Categories used throughout:
- **C1** — should use `./lib` utils, but doesn't
- **C2** — can be moved to `./lib` and shared
- **C3** — duplicative/repetitive, should be simplified
- **C4** — similar/related, should be unified

---

## Executive Summary

The codebase (~590k LOC surveyed) is not uniformly duplicative — several subsystems are exemplary (markdown-family formatters are 14-line wrappers over table-driven `format-markup.cpp`; graph input parsers share `input-graph.h`; the render backends share `render_walk.cpp` via a vtable; JS and Lambda already share the libuv event loop, module registry, and JSON *parsing*). The problems cluster into **four recurring themes**:

1. **Shared helpers exist but are bypassed.** This is the single biggest pattern. Radiant layout has `layout_box_metrics()`, `layout_apply_min_max_width()`, `layout_alignment.hpp` (whose header even says "extracted from duplicated code in layout_flex and layout_grid") — yet flex/grid/intrinsic call them **zero times** and hand-roll the same math hundreds of times. Input parsers have `parse_shared_quoted_string` and `parse_hex_codepoint` with 0–1 callers while JSON/TOML/Mark clone the logic. `lib/escape.h` rule tables exist while XML/SVG escaping is hand-rolled in 6+ places. **The primary lever is migration to existing helpers, not new abstraction.**

2. **The two-transpiler / two-runtime split duplicates infrastructure verbatim.** `transpile.cpp` vs `transpile-mir.cpp` carry byte-identical analysis helpers; `js_mir_*`'s `jm_call_N`/`jm_new_reg` are line-for-line copies of `transpile-mir.cpp`'s `emit_call_N`/`new_reg`; `js_double_to_string` is a near-verbatim copy of `lambda_double_to_shortest`.

3. **Dead or orphaned duplicate implementations.** `input-kv.cpp` (a full unified INI+Properties engine, ~480 LOC) was written as the replacement for `input-ini.cpp`+`input-prop.cpp` but never added to the build; `.bak` files (`layout_table.cpp.bak` 127 KB, `render_svg.cpp.bak`, `render_img.cpp.bak`, `mark_editor.cpp.bak`, `runner.cpp.bak`) sit beside live sources; `format-utils.cpp` carries dead `markdown_escape`/`wiki_escape`/`rst_escape` functions where every branch returns NULL.

4. **Per-file static-helper copy-paste in lambda/js.** `make_string_item` is redefined `static` in **22 files**; `make_js_undefined` in 21; `is_callable` in 8.

**Estimated removable duplication:** roughly 4,000–6,000 LOC of direct clones, plus large maintainability wins from table-driving the op/builtin dispatch families.

### Progress update (2026-07-12, verified against code)

**Complete** (every ✅ claim below spot-checked in the live tree):
- ✅ Lambda core: §1.1 transpiler analysis helpers, §1.2/§1.3 numeric conversion + ArrayNum readers, §1.6 UTF normalization, all §1.7 small items.
- ✅ Input/parsers: §2.1 unified INI/Properties, §2.2 quoted-string unification, §2.3 hex/surrogate escapes, §2.5 lib/str adoption including the tracked PDF/markup ctype remainder, most of §2.6.
- ✅ Formatters + validator: all of §3 (dispatch visitor, `ElementReader::attrs()`, XML escaping, TypeId names, number/datetime helpers, escaper consolidation, math dispatch tables, §3.8 small items).
- ✅ Radiant layout: §4.1 box-metric migration, §4.3 out-of-flow canonicalization, §4.7 box-shorthand helper, §4.8 box-sizing (only 2 CSS-declaration-keyword reads in `intrinsic_sizing.cpp` + style-resolution assignments remain; ~99 sites on `layout_uses_border_box()`/`layout_box_sizing()`), §4.4 flex alignment distribution (zero switch ladders left in `layout_flex.cpp`; stretch/baseline stay flex-local by design).
- ✅ Radiant paint: §5.2 glyph rasterization, §5.4 lib-reuse batch, §5.5 helper extractions incl. `IRect`/`render_rect.hpp` (`RenderPixelBounds` and `int region[4]` fully gone), §5.6 math templates.
- ✅ JS↔core: §6.1 dtoa, §6.2 base64 decode, §6.3 static-helper dedup, §6.5 civil-date math, §6.6 JSON escaping / percent-encoding / UTF-16 pair math / inspect-vs-print audit.

**Partial** (helpers landed; quantified remainder): see the "Partial — real progress, quantified remainder" table below.

**Not started:**
- §5.7/§4.8 follow-up code extractions from the new audit notes.

### Partial — real progress, quantified remainder

| Item | Landed | Remaining (quantified) |
|---|---|---|
| §4.2 min/max clamps | Shared clamp/floor/positive/explicit min/max helper family in `layout_box.{hpp,cpp}`; grid clean; flex positive-axis, explicit-min, Phase 5b, and multipass height readers routed through helpers | Flex raw `given_min/max_*` mentions down 76→32 (`layout_flex.cpp`), 32 across the flex files; remaining sites are percentage mutation, debug/diagnostic reads, and a few policy-specific definite-size checks |
| §4.5 baseline walkers | `compute_font_baseline_ascender()` shared by flex/table/grid | The 3 recursive first-baseline walkers (flex/table/grid+shared) remain separate |
| §4.6 percentages | `layout_percentages.{hpp,cpp}`; grid fully on `layout_reresolve_percentage_box()`; both engines on shared scalar primitives | Flex bypasses the box re-resolver; 2 containing-content-width wrappers remain separate over shared primitives |
| §4.8 layout_axis | 45 axis-helper uses in flex | ~62 `is_horizontal` conditional branches + 31 `is_main_axis_horizontal()` adapter calls + 10 direction branches in `layout_flex.cpp` |
| §4.8 child iteration | Skip helpers (`layout_display_is_none`, `layout_text_node_has_content`, whitespace suppression) adopted ~18 sites; last raw `grid_utils.cpp` display-none predicate routed | Shared `collect_in_flow_children()` never built; remaining work is collection-shape unification, not skip-predicate cleanup |
| §5.1 replay clone | `dl_replay_tile` 566→207 LOC, `dl_replay` 226→171; vector/raster/glyph/effects/backdrop/shadow helper families shared by both | Two parallel dispatch switches (30 vs 43 `case DL_`) incl. duplicated cull pre-pass; no single parameterized replay core |
| §5.3 paint-op table | `PAINT_OP_LIST` drives the enum + `paint_op_name()` + `PAINT_OP_COUNT` assertion | 3 explicit op switches remain (validate / lower-raster / lower-svg), 79 `case PAINT_` (down from 108); no descriptor table |
| §5.5 rect zoo | `IRect` in `render_rect.hpp`; `RenderPixelBounds` and `int region[4]` fully gone | Broader `Rect`/`Bound`/`DirtyRect`/`Point2D` consolidation in `view.hpp` |
| §2.4 line/col tracking | `LineCounter` shared by CSS/HTML5/markup byte-cursors; YAML speculative state behind `YamlCursor` | YAML keeps its parser-local cursor (no `SourceTracker` migration); `css_parser.hpp`/`css_engine.hpp` structs keep their own line/col fields |
| §1.4 map-field walks | `FOR_EACH_MAP_FIELD` adopted ~18 sites; read-only `build_ast.cpp` walks migrated | Residual `->shape` loops are mutation/codegen/copy paths — review case-by-case, most are legitimately excluded |
| §1.5 type predicates | Shared TypeId predicates broadly adopted; `build_ast.cpp` sweep done with local names for AST-specific memberships | Replaceable single-variable chains concentrated in `transpile-mir.cpp`/`transpile.cpp`; needs a refreshed count before the next pass |
| §6.4 MirEmitter | Mechanical bodies (reg alloc, call assembly, labels, imports, immediates) shared via `mir_emitter_shared.hpp` | No embedded base struct — each transpiler keeps its own reg counter, import cache, and thin wrapper shims |
| §6.6 builtin registration | Registry strncmp ladders → static spec-map tables in `js_runtime_builtin_registry.cpp` | Giant `switch(builtin_id)` (`js_runtime.cpp` ~9470) + 357 `strncmp`s in `js_mir_expression_lowering.cpp`; adding a builtin still touches 3 places |
| §6.6 path_str | `lib/path_str.h` (17 fns); `js_path.cpp` fully routed | `require()` lightly routed (2 calls); remaining local code is intentional policy |
| §6.6 RE2 glue | `lib/re2_glue.hpp` centralizes default options, compile/failure ownership, release, Lambda find/replace match-window selection | JS capture remap/filter lifecycle + Lambda pattern cache policy remain wrapper-local |
| §5.7 deep audits | SVG dual-path and event/event_sim audits written (see §5.7) | Targeted helper extractions from the audit notes, not broad file merges |
| §4.8 htm/css style overlap | Audited (see §4.8) — files own different policy layers | Small presentational-hint/property-writer helper slice is the next safe step |

### Top 10 highest-leverage actions

| # | Action | Area | ~LOC | Effort | Risk |
|---|--------|------|------|--------|------|
| 1 | ✅ Extract `lib/dtoa` from `lambda_double_to_shortest` ≡ `js_double_to_string` | JS↔core | 135 | Low | Low |
| 2 | ✅ Resolve INI/Properties triple implementation (wire `input-kv.cpp`, delete `input-ini`/`input-prop`) | input | 480 | Med | Med |
| 3 | 🔶 Migrate radiant layout to existing `layout_box_metrics`/`layout_apply_min_max_*` helpers — box metrics (§4.1) done; min/max (§4.2) partial, flex positive/explicit min/max readers now shared (32 raw flex mentions left) | radiant | 450–750 | Med | Low |
| 4 | 🔶 De-clone `dl_replay_tile` vs `dl_replay` + shared glyph raster — glyph (§5.2) done; replay helper families shared, two parallel dispatch switches remain (§5.1) | radiant | 530 | High | Med |
| 5 | ✅ Kill the 22×/21×/8× static helper copies in lambda/js | JS | 270 | Trivial | Low |
| 6 | 🔶 `MirEmitter` base shared by `transpile-mir.cpp` and `js_mir_*` — mechanical bodies shared via `mir_emitter_shared.hpp`; embedded base struct not built (§6.4) | JS↔core | 300–500 | Med-High | Med |
| 7 | ✅ Shared formatter dispatch visitor + `ElementReader` attribute iterator | format | 290 | Med | Med |
| 8 | ✅ Done: unify parser quoted-string/reference loops by policy-local helpers | input | 450 | Med | Med |
| 9 | ✅ Move 5 duplicated analysis helpers into `transpile_shared.cpp` | core | 120 | Low | Low |
| 10 | ✅ Replace 3 hand-rolled base64 decode tables with `base64_decode_variant` | JS | 90 | Low | Low |

---

## 1. Lambda Core (`lambda/` top-level)

Already good: `name_pool.cpp`/`shape_pool.cpp` use `lib/hashmap`; `lambda-eval.cpp` uses `str_utf8_*`; `main.cpp`/`runner.cpp` use `read_text_file`/`read_binary_file`. `lambda-data.cpp` vs `lambda-data-runtime.cpp` is a clean arena/GC split, not duplication.

### ✅ 1.1 Duplicated analysis helpers between the two transpilers — C3, HIGH
Byte-for-byte copies under a `mir_` prefix; all are pure functions of `TypeMap`/`ShapeEntry`/`AstNode` with zero backend dependency:

| transpile.cpp | transpile-mir.cpp |
|---|---|
| `find_shape_field_by_name` (5993–6003) | `mir_find_shape_field` (1470–1480) |
| `has_fixed_shape` (6013–6023) | `mir_has_fixed_shape` (1457–1467) |
| `is_direct_access_type` (6092–6105) | `mir_is_direct_access_type` (1495–1508) |
| `resolve_field_type_id` (6112–6119) | `mir_resolve_field_type` (1485–1492) |
| `detect_ndim_literal` (4685–4730) | `mir_detect_ndim_literal` (4739–4785) |

~120 LOC. **Fix:** move into `transpile_shared.cpp` (exists for exactly this; only 103 lines today), delete the `mir_` twins. Only the emit siblings legitimately differ. Effort: low.

**Implementation status (2026-07-11): done.** `find_shape_field_by_name`, `has_fixed_shape`, `is_direct_access_type`, `resolve_field_type_id`, and `detect_ndim_literal` now live in `lambda/transpile_shared.cpp` with declarations in `lambda/transpiler.hpp`. `transpile.cpp` and `transpile-mir.cpp` both call the shared helpers; MIR Direct preserves its stricter assignment-node fast-path guard through an explicit `disqualify_assign` flag. Follow-up removed stale legacy C-transpiler copies of the shared function-name/typed-param helpers and included `transpile_shared.cpp` in the Jube variant so shared helpers link in both `lambda.exe` and `lambda-jube.exe`.

### ✅ 1.2 Hand-rolled Item→number extractors vs canonical `it2d`/`it2i`/`it2l` — C1, HIGH
Canonical converters live in `lambda-data.cpp:322–392+`. Reimplemented: `item_to_int_value`/`item_to_float_value` (`lambda-data-runtime.cpp:665–718`), `item_to_double`/`item_to_integral_index` (`lambda-vector.cpp:34–54, 161–172`). Copies differ only in NUM_SIZED/decimal/NaN edge handling — classic drift-bug territory. ~90 LOC. **Fix:** consolidate on `it2*` (add `_checked` variants if the extra semantics are needed); decide one canonical NUM_SIZED behavior. Effort: medium.

**Implementation status (2026-07-11): done.** Extended canonical `it2i()`/`it2l()` to cover NUM_SIZED and UINT64, routed ArrayNum store coercion through `it2i()`/`it2d()`, and made vector arithmetic use `it2d()` instead of its private numeric switch. The exact-integral index checker remains local because it validates whole-number/range semantics rather than merely converting.

### ✅ 1.3 ArrayNum element-read switch duplicated 3–4× — C3/C1, HIGH
Canonical `array_num_read_scalar_at` (`lambda-data-runtime.cpp:240–264`) vs `read_compact_elem` (`print.cpp:11–29`) vs `compact_elem_to_double` (`lambda-vector.cpp:180–197`). ~60 LOC. **Fix:** expose `arraynum_read_item(arr,i)` / `arraynum_read_double(arr,i)` from the runtime; delete `read_compact_elem`. Verify the intentional UINT64 heap-vs-truncate difference first. Effort: low-medium.

**Implementation status (2026-07-11): done.** Exposed `array_num_read_item()` and `array_num_read_double()` from the runtime and routed `array_num_get()`, N-D scalar reads, print, vector math/broadcasting, and ArrayNum equality through the shared helpers. Compact UINT64 printing now uses the canonical heap-boxed UINT64 reader instead of truncating through an int path.

### 1.4 "Walk each named field in a map shape" loop open-coded ~30× — C4, MED-HIGH
`ShapeEntry* field = map_type->shape; while (field){ ... field = field->next; }` at `lambda-eval.cpp` 1310, 1329, 2111, 2127, 2177, 2197, 2306, 2333, 5958, 6147, 6241, 6367, 6468 (14 sites), 9 sites in `transpile.cpp`, plus `lambda-data*.cpp`. Formatters and validator clone it too (see §3.2). **Fix:** a `FOR_EACH_MAP_FIELD(map_type, map_data, field, value)` iterator/macro in `lambda-data.hpp` yielding `(ShapeEntry*, Item)`. Effort: medium, mechanical.

✅ Partial: added shared `map_field_ptr()`, `map_field_to_item()`, `map_shape_field_to_item()`, and `map_shape_field_to_map()` in `lambda-data.hpp`/`lambda-data.cpp`, including the missing UINT64 typed-slot reconstruction. Routed `Map::get`, MarkReader entry/attribute iteration, MarkBuilder arena/deep-copy attribute reads, render-map field reads, inline SVG attribute serialization, TVG external SVG id lookup, RDB map attribute lookup, procedural `cmd()` map-argument formatting, nested spread-map recursive reads, `print_named_items()`, and Lambda equality/query field reads through the shared slot readers; removed the dead formatter-only `create_item_from_field_data()` clone. Verification: `make build`, `make build-test`, `make check-int-cast`, `git diff --check`, `value.ls`, `unboxed_field_access.ls`, `large_int_map.ls`, `query.ls`, `child_query.ls`, `proc_cmd.ls`, `temp/cleanup_cmd_map_args.ls`, external SVG `<use>` layout/render smokes, `test_mark_reader_gtest`, `test_mark_builder_gtest`, `test_mark_builder_deepcopy_gtest`, `test_source_pos_bridge_gtest`, and focused no-text inline SVG layout/render smokes. A broader Puppertino SVG render smoke still hits the known pre-existing SVG text/effect raster fallback UAF and is not counted for this slot-reader change. The source-position guard now rejects synthetic low-address render-map keys before attempting MapReader inspection.

✅ Partial (2026-07-12): added `typemap_first_field()` / `typemap_next_field()` plus `FOR_EACH_MAP_FIELD()` as the shared read-only shape traversal helper, then routed core map equality, namespace-collision lookup fallback, query/child-query attribute walks, value membership checks, possession key checks, total-order sorted field selection, runtime `_map_get()`, `item_keys()`, and render-map recursive field lookup through it. Mutation/build/editor loops remain explicit because they change shape links or allocation state rather than simply walking fields. Verified with `make build`, `make check-int-cast`, `git diff --check`, `value.ls`, `unboxed_field_access.ls`, `large_int_map.ls`, `query.ls`, `child_query.ls`, `./lambda.exe run test/lambda/proc/proc_cmd.ls`, and `./lambda.exe layout temp/cleanup_flex_form_hypothetical.html`; the layout smoke still emits the known cache-layout memtrack shutdown diagnostic while completing with `1 success, 0 failed`. An attempted stale `test/lambda/proc_cmd.ls` path failed to open and was rerun with the correct `test/lambda/proc/proc_cmd.ls` path.

✅ Partial (2026-07-12): routed Python and Ruby runtime map/hash helper walks through `FOR_EACH_MAP_FIELD()` as well, covering Python dict `keys()`/`values()`/`items()`/`update()`/`copy()`, class-method map copying, mapping-rest pattern copies, Ruby hash `keys()`/`values()`/`to_a()`/`length()`/`value?()`/`merge()`, inline SVG subscene attribute serialization, and validator typo-suggestion field scans. The pass also fixed two iterator-adjacent Python dict bugs found during verification: mapping-rest patterns now read packed map slots through `_map_read_field()` instead of reconstructing raw `Item` words, and `py_dict_get()` now performs a direct empty-map-safe dict lookup instead of routing missing keys through attribute/JS-object lookup. Verified with `make build`, `make build-jube`, `git diff --check`, Python dict/mapping/class temp smokes, `test_py_classes.py`, `test_py_match.py`, `test_py_kwargs.py`, `test_py_stdlib_collections.py`, Ruby numerics, and `test_rb_gtest` (22/22); Jube Python commands still emit the known small eval/py-runtime memtrack diagnostics while exiting cleanly.

✅ Partial (2026-07-12): routed the three remaining read-only `build_ast.cpp` shape walks through `FOR_EACH_MAP_FIELD()`: struct field type resolution, user-member method shadow checks, and inherited-field scope injection. Parent-shape copying and AST/type-construction loops remain explicit because they allocate or relink shape entries. Verified with `make build`, `git diff --check`, and focused Lambda smokes for `unboxed_field_access.ls`, `func_param.ls`, `sized_numeric_type_annot.ls`, `mixed_numeric_ops.ls`, `typed_array_vector_ops.ls`, `eq_total.ls`, and `value.ls`.

### 1.5 Ad-hoc scalar-type predicate chains ~85× — C3/C4, MED-HIGH
`tid == LMD_TYPE_INT || tid == LMD_TYPE_INT64 || tid == LMD_TYPE_FLOAT || ...` appears 42× in `transpile.cpp`, 35× in `transpile-mir.cpp`, 8× in `build_ast.cpp`, plus named variants with *subtly different membership*: `has_typed_params` (`transpile_shared.cpp:14–37`), `mir_is_native_param_type` (`transpile-mir.cpp:673–684`), `is_numeric_type` (`transpile.cpp:352`), `is_scalar_numeric` (`lambda-vector.cpp:67`). **Fix:** canonical predicates (`is_native_scalar_type`, `is_unboxable_param_type`, `is_numeric_type`) defined once near `TypeId`; replace deliberately, not blindly — memberships differ per site. Effort: medium.

✅ Partial: added shared TypeId predicates near the canonical `is_numeric_type_id()` helper and routed the exact-match predicate chains in `transpile_shared.cpp`, `transpile.cpp`, `transpile-mir.cpp`, `transpile-call.cpp`, `build_ast.cpp`, `lambda-vector.cpp`, `lambda-eval*.cpp`, `lambda-data*.cpp`, `lambda-proc.cpp`, `target.cpp`, and `vmap.cpp` through them. The second pass added `is_native_numeric_or_bool_type_id()` and `is_text_type_id()` for exact native/bool and string-or-symbol memberships. This pass added `is_integer_type_id()` and `is_float_type_id()` and routed the remaining exact integer/float-family chains in the core Lambda runtime, validator, format, and MIR paths through them. Follow-up added `is_fn_call_wrapper_return_type_id()` and routed the shared plus legacy C-transpiler wrapper-return checks through it, while also replacing the legacy typed-param chain with `is_typed_wrapper_param_type_id()`. The latest pass routed the core member/split/splice text and integer checks through `is_text_type_id()` / `is_integer_type_id()` and collapsed repeated runtime vector predicates behind one local helper. Language-specific JS/Ruby/Python/TS memberships remain explicit. Verified with `make build`, `make check-int-cast`, `git diff --check`, direct MIR smokes for `mixed_numeric_ops.ls`, `typed_array_vector_ops.ls`, `eq_total.ls`, `value.ls`, `unboxed_field_access.ls`, `sized_numeric_type_annot.ls`, `func_param.ls`, `numeric_sys_func.ls`, procedural `proc_splice.ls`, and `temp/cleanup_predicate_smoke.ls`; an attempted `test_lambda_gtest` numeric filter matched 0 tests and is not counted.

✅ Partial (2026-07-12): collapsed Ruby runtime's repeated `int|float|int64` arithmetic/comparison membership into local `rb_is_numeric_type()` / `rb_are_numeric_types()` helpers, preserving Ruby's intentionally narrower numeric set rather than using Lambda's broader numeric predicates. The same verification pass fixed the Jube-only Bash builtin compile blocker by including the shared `lib/str.h` declaration for `str_char_in_set()`, restored the Jube source overlay for `lambda/jube`, `lambda/module/radiant`, `lambda/module/hostobj_demo`, and `transpile_shared.cpp`, registered the Ruby-emitted `js_function_get_ptr` MIR import, moved Ruby array item growth to GC data-zone storage via `rb_array_ensure_capacity()`, and retained standalone Ruby heaps through `Runtime` so `runtime_cleanup()` owns teardown without leaking into memtrack output. Verified with `make build`, `make build-jube`, `make check-int-cast`, `git diff --check`, direct `lambda-jube.exe rb --no-log test/rb/test_rb_numerics.rb`, and `test_rb_gtest` (22/22).

✅ Partial (2026-07-12): added shared `is_array_family_type_id()`, `is_map_family_type_id()`, and `is_container_type_id()` near the canonical TypeId predicates, then routed the exact `build_ast.cpp` array-family, map-family, and container checks through them. Also named the AST-local predicate memberships for parameter full-type preservation, static scalar spread diagnostics, and mutable-assignment numeric narrowing, and reused existing numeric/float/integer predicates for the compatible coercion paths. `lambda/lambda-embed.h` was regenerated from `lambda/lambda.h`. Verified with `make build`, `git diff --check`, and focused Lambda smokes for `mixed_numeric_ops.ls`, `typed_array_vector_ops.ls`, `unboxed_field_access.ls`, `sized_numeric_type_annot.ls`, `func_param.ls`, `eq_total.ls`, and `value.ls`.

### ✅ 1.6 `utf_string.cpp` copy-paste wrappers — C3, some C2, MED
Five `normalize_utf8proc_nfc/nfd/casefold/nfkc/nfkd` (`utf_string.cpp:17–84, 221–267`) identical except the option flags; five comparators `equal/less/greater/less_equal/greater_equal_comp_unicode` (133–218) identical except a one-line result map. ~150 LOC → ~50. **Fix:** one parameterized `normalize_utf8proc(str,len,out_len,opts)` + one comparator core. The normalize group is generic and a candidate for `lib/utf` (which has no normalization today). Effort: low.

**Implementation status (2026-07-11): done.** Collapsed the five normalization wrappers onto one option-parameterized helper and the five Unicode comparison wrappers onto one comparator core. Added `free_utf8proc_result()` so JS/core/markup callers release `utf8proc_map()` buffers with the matching raw allocator instead of Lambda's tracked `mem_free()`.

### 1.7 Smaller items
- ✅ **`shape_pool.cpp` hand-rolls hashmap glue** (`shape_signature_hash`/`compare`, raw 8-arg `hashmap_new`, :21–47, 99–107) while `name_pool.cpp:18` uses `HASHMAP_DEFINE_LENSTRKEY` cleanly — C1, low effort. **Implementation status (2026-07-11): done.** Shape cache lookup now uses `HASHMAP_DEFINE_FIELD3_KEY` over the existing signature hash/length/byte-size key instead of bespoke hash/compare/new boilerplate.
- ✅ **Refcount + mem_node + release-hook lifecycle duplicated** between `name_pool.cpp` (63–94) and `shape_pool.cpp` (118–144) — C4; extract a shared `RefCountedPool` base (candidate home: `lib/mem_factory.h`). ~40 LOC. **Implementation status (2026-07-11): done.** Added `lib/ref_counted_pool.hpp` with shared retain/finalize helpers for pool-like structs carrying `ref_count`, `parent`, `mem_node`, and a hashmap, then routed name-pool and shape-pool retain/release through it while preserving shape-pool debug logging and existing Pool-owned lifetime semantics. Verified clean build plus `test_name_pool_gtest`, `test_namespace_gtest`, and `test_mark_builder_gtest`.
- ✅ **`print.cpp` number/decimal/escape formatting is inline** (189–791) rather than shared with `format-utils`/`lib/escape` — C3; coordinate with §3 (findings 3.4/3.5 there). **Implementation status (2026-07-11): done for safe shared runtime helpers.** `print.cpp` already shared dtoa and decimal conversion after §3/§6; this pass removed its typed-map-slot reconstruction switch by routing named-field printing through `map_shape_field_to_item()`. Native print string syntax is intentionally left as the Lambda debug printer's surface rather than forcing JSON/XML escaping rules onto it. Verified with `make build`, `make build-test`, `value.ls`, `unboxed_field_access.ls`, and MarkReader/MarkBuilder/source-position bridge gtests.
- ✅ **`shape_builder.cpp` repeats the strcmp linear scan 4×** (add/remove/has/get, 29–114) — factor one `shape_builder_find()`. Low effort. **Implementation status (2026-07-11): done.** Added a private `shape_builder_find_field()` helper and routed add/remove/has/get through it.
- ✅ **`emit_current_func_error_return` (`transpile.cpp:164–187`) re-hardcodes zero values** the `type_box_table.zero_value` column already defines — drive both backends from the table. **Implementation status (2026-07-11): done.** Replaced the local int/float/bool return-zero switch with table-driven `TypeBoxInfo::zero_value` emission and preserved the `ITEM_ERROR` fallback for unknown return types. Verified the current generated main build remains clean; `transpile.cpp` is excluded from that target.
- ✅ **Dead/unreachable duplicate branches in `lambda-eval.cpp`** — `query_collect` has consecutive identical `else if (type_id == LMD_TYPE_ARRAY)` guards at 2135/2140 and 2207/2222; the second is unreachable and one was likely meant to be `LMD_TYPE_LIST`. **Latent bug — check and fix.** **Implementation status (2026-07-11): done.** Removed both unreachable duplicate branches. Runtime lists and arrays now share the single `LMD_TYPE_ARRAY` branch explicitly, matching the current `Array`/`List` typedef model.
- ✅ **Duplicate vector/pointer type disjuncts** — `lambda-eval-num.cpp` repeated `LMD_TYPE_ARRAY` in the vectorized numeric function guards, and `lambda-eval.cpp` repeated it in map-set pointer-kind checks. **Implementation status (2026-07-12): done.** Added a local `IS_VECTOR_TYPE()` helper for the numeric vector guards and removed the unreachable duplicate array predicates from the map-set pointer checks; runtime lists and arrays continue to share the single `LMD_TYPE_ARRAY` membership. Verified with `make build`, `git diff --check`, `value.ls`, `typed_array_iter.ls`, `typed_array_ndim_iter.ls`, `mixed_numeric_ops.ls`, `numeric_sys_func.ls`, and `large_int_map.ls`.
- ✅ **`NUM_SIZED` sub-switch decode duplicated** (`lambda-data-runtime.cpp:673–685, 701–712`, `array_num_set_item` 956+) vs the `get_num_sized_as_double()` accessor path (`lambda.hpp:213`) — standardize on accessors, add `get_num_sized_as_int64()`. **Implementation status (2026-07-11): done.** The live tree already had `get_num_sized_as_int64()`; canonical integer conversion now uses it for integer NUM_SIZED payloads, and ArrayNum store coercion routes through the canonical `it2i()`/`it2d()` conversion path.
- ✅ **`array()/array_int()/array_float()/array_int64()` constructor quadruples** in `lambda-data-runtime.cpp:113–592` are hand-templated per element type — X-macro/table candidate.
  ✅ Done: routed empty typed numeric constructors through `array_num_new()` and added `array_num_prepare_fill()` to share checked allocation, element type setup, and length/capacity initialization across int/int64/float fill paths while leaving vararg value writes explicit. Verified with `make build`, `./test/test_lambda_gtest.exe --gtest_filter='*Array*:*array*:*Typed*:*typed*'`, `./lambda.exe test/lambda/typed_array_iter.ls`, `./lambda.exe test/lambda/typed_array_ndim_iter.ls`, and `./lambda.exe test/lambda/value.ls`.
- ✅ **`mark_builder.cpp:44–45` and `mark_editor.cpp:15–16` re-`extern` `map_put`/`elmt_put`** ad hoc — declare once in a shared header. (Checked: `mark_builder` vs `shape_builder` do NOT meaningfully overlap — different domains.) **Implementation status (2026-07-11): done.** Removed the local extern redeclarations; both files now use the central declarations already provided by `lambda-data.hpp`.
- ✅ **Stale `.bak` files:** `mark_editor.cpp.bak`, `runner.cpp.bak` — delete, rely on git. **Implementation status (2026-07-11): done.** Removed the untracked Lambda backup files named here.

---

## 2. Input Parsers (`lambda/input/`)

Already good: graph parsers share `input-graph.h` (`skip_wsc`, `read_graph_identifier`, `parse_shared_quoted_string`); VCF/ICS share `input-rfc-text.h`; JSON-family auto-typing goes through `parse_typed_value`/`parse_integer_token_exact`.

### ✅ 2.1 INI + Properties implemented twice; the unified engine is dead code — C3/C4, HIGHEST
`input-kv.cpp` (282 LOC) is a config-driven engine defining `parse_ini` + `parse_properties` (`KvConfig INI_CONFIG`/`PROP_CONFIG`, `parse_kv_document` L214) and its header comment documents it as the unified replacement — but `build_lambda_config.json` **never compiles it** (it compiles `input-ini.cpp` L195 + `input-prop.cpp` L142 instead). ~480 LOC of parallel key/value/section/comment logic. **Fix:** switch the build to `input-kv.cpp` and delete `input-ini.cpp`+`input-prop.cpp` (preferred — cleaner design), or delete the dead file. Regression-test against INI/properties corpora; watch error-message text. Effort: medium.

**Implementation status (2026-07-11): done.** The live build had already moved to glob-compiling `input-kv.cpp` and excluding the old parser files; deleted the stale `input-ini.cpp`/`input-prop.cpp` implementations and removed their stale config/generated-build references. Verified clean build plus INI/properties conversions through the unified parser.

### ✅ 2.2 Quoted-string parsing cloned across (and within) parsers — C3, HIGH
`input-utils.hpp:parse_shared_quoted_string` (173–207) exists but only graph parsers use it. Clones: `input-json.cpp:parse_string` (13–63) **plus a second inline copy in the object-key loop** (187–207); `input-mark.cpp:parse_string`/`parse_symbol` (39–120); `input-toml.cpp` has **five** near-identical ~30-line copies (`parse_quoted_key` 223–259, `parse_literal_key` 261–290, `parse_basic_string` 292–328, `parse_literal_string` 330–360, `parse_multiline_basic_string` 362+); `input-xml.cpp:parse_string_content` (20+). ~250+ LOC. **Fix:** extend `parse_shared_quoted_string` with a policy struct (quote char, escape handler, allow-newline); route JSON/TOML/Mark/XML through it. At minimum JSON's key loop should call its own `parse_string`. Effort: medium (escape/error semantics vary per format).

**Implementation status (2026-07-11): done.** Removed JSON's inline object-key string parser clone by routing object keys through `parse_string()`, keeping key-name interning and error recovery local. Added TOML-local single-line and multiline string helpers, routing quoted keys, literal keys, basic strings, literal strings, multiline basic strings, and multiline literal strings through them while preserving TOML's escape/raw-literal/newline policies and error wording. Collapsed Mark's double-quoted string and single-quoted symbol loops into one local quoted-content helper while keeping their distinct escape/newline policies. Factored XML reference decoding into one bounded helper used by both attribute strings and text nodes, so named and numeric references now share policy while unknown/invalid references remain preserved for roundtrip compatibility. Verified existing JSON conversion plus escaped newline, Unicode, and surrogate-pair object keys, focused TOML single-line and multiline smokes, a Mark quoted-key/symbol smoke covering newline, slash, quote, unknown-symbol escape, and surrogate-pair decoding, and an XML smoke covering named, decimal, hex, unknown, and invalid references in attributes and text.

### ✅ 2.3 `\uXXXX` + surrogate-pair handling triplicated — C3/C1, HIGH
`input-utils.hpp:parse_escape_char` (71–138), `input-toml.cpp:handle_escape_sequence` (32–165, near-verbatim clone plus `\U`), `input-mark.cpp` inline (58–76) — all hand-roll `strtol(hex,16)` + `0xD800..0xDBFF` combining. Meanwhile `input-utils.cpp:parse_hex_codepoint` (29–42) exists for exactly this and has **zero callers**. ~200 LOC. **Fix:** one shared `\u`/`\U` routine built on `parse_hex_codepoint` + `str_utf8_encode`. Effort: medium.

**Implementation status (2026-07-11): done.** Routed JSON/properties shared escaping, TOML, Mark, and YAML hexadecimal escape decoding through `parse_hex_codepoint()`. TOML and Mark now share surrogate-pair validation through `decode_surrogate_pair()` instead of local `strtol(hex)` blocks; YAML `\x`/`\u`/`\U` uses the shared hex decoder while preserving its parser cursor model.

### 2.4 Line/column tracking proliferation — C4, MED (flag to stop the bleeding)
Canonical `SourceTracker` (`source_tracker.hpp`, UTF-8-aware, O(1)) is bypassed by: `input-yaml.cpp` `YamlParser{pos,line,col}` + hand-rolled `advance` (46–92) — *even though YAML holds an `InputContext*` with a live tracker*; `html5/html5_tokenizer.cpp` (:21, :1111, iterator 135–231); `css/css_engine.hpp:266–267`; `css/css_parser.hpp` (two structs, 111–112, 141–142); `markup/markup_parser.cpp`. **Fix:** migrate YAML to `ctx.tracker`; give the CSS/HTML5 byte-cursors a small shared `LineCounter`. Effort: high (YAML) — mainly enforce "no new trackers."

✅ Partial: added a `YamlCursor` save/restore helper and routed YAML's speculative parse lookaheads through it, replacing repeated manual `pos`/`line`/`col` snapshot triples in blank-line skipping, plain scalar continuation, block-scalar indent detection, anchor/tag lookahead, inline block parsing, document-boundary probing, and empty-input/comment-only detection. YAML still has its parser-local cursor and should eventually migrate to `SourceTracker`, but speculative rewind points now have one helper boundary instead of open-coded state assignment. Verified with `make build`, `git diff --check`, existing `test/input/more_test.yaml` conversion, and a focused YAML cursor smoke covering anchors/aliases, folded and literal block scalars, blank/comment lookahead before sequences, plus hex/binary numeric scalars.

✅ Partial (2026-07-12): added a shared byte-oriented `LineCounter` helper for lightweight tokenizer cursors, routed HTML5's source-line scan through the shared LF counter, and used the same counter to stamp CSS tokenizer `line`/`column` metadata at whitespace, normal-token, and EOF boundaries. This does not replace YAML's parser-local cursor or HTML5's UTF-8 iterator, but it gives the byte-cursor tokenizers one shared line/column update primitive. Verified with `make build`, `make check-int-cast`, `git diff --check`, focused `temp/cleanup_line_counter.html` layout/render smokes, and an existing `flex_direction_row_reverse.html` layout smoke; the baseline fixture kept its known optional `test_helper.js` warning and cache-layout memtrack diagnostic.

✅ Partial (2026-07-12): routed the HTML5 UTF-8 iterator's newline/tab column update through `LineCounter` as well, while keeping UTF-8 decoding and byte-offset advancement local to the iterator. YAML remains explicit because its parser cursor uses zero-based columns and speculative rewinds. Verified with `make build`, `make check-int-cast`, `git diff --check`, and the existing line-counter HTML layout/render smokes.

✅ Partial (2026-07-12): added a YAML `move_to_line_start()` cursor helper and routed the repeated `pos`/`col` line-start rewinds in block sequence, block mapping, inline mapping, anchor/tag lookahead, quoted-key mapping, and document-boundary probing through it. YAML still owns its parser-local cursor, but the remaining manual cursor rewinds are now narrowed to special block-scalar state and saved-cursor restore paths. Verified with `make build`, `make check-int-cast`, `git diff --check`, existing `test/input/more_test.yaml` conversion, and `temp/cleanup_yaml_cursor.yml`.

✅ Partial (2026-07-12): routed the markup parser's line-count prepass through the shared `line_counter_count_lf()` helper added for lightweight tokenizer cursors, leaving the actual line splitting and parser `current_line` behavior unchanged. Verified with `make build`, `make check-int-cast`, `git diff --check`, and a Markdown-to-HTML conversion smoke for `test/input/test.md`.

### ✅ 2.5 lib/str.h adoption gaps — C1
- ✅ **ctype on signed char (UB for UTF-8 bytes ≥0x80)** in ~30 files: `input-graph.h:36, 65–72`, `input-jsx.cpp:28–34`, `input-mdx.cpp`, `input-toml.cpp`. `lib/str.h` §17 documents this exact hazard and provides `str_char_is_ascii_*`. Mechanical replace. **Implementation status (2026-07-11): done for the doc-named parser hot spots.** Replaced direct ctype use in `input-graph.h`, Mermaid graph parsing, JSX, and TOML with `lib/str.h` ASCII-safe classifiers; `input-mdx.cpp` had no remaining ctype calls in the current tree. Follow-up removed stale `ctype.h` includes from the DOT and D2 graph parsers after the classifier migration. Verified with `make check-int-cast`, `git diff --check`, and DOT/D2/Mermaid conversion smokes to JSON.
  ✅ Follow-up (2026-07-12): routed XML tag-name scanning, RTF control-word scanning, and LaTeX command-boundary scans through `lib/str.h` ASCII classifiers instead of libc `isalpha`/`isalnum`, keeping the unsigned-byte-safe policy consistent beyond the originally named parser hot spots. Verified with `make build`, `make check-int-cast`, `git diff --check`, XML and RTF conversion smokes to JSON, and a LaTeX conversion smoke to JSON; the LaTeX fixture kept its pre-existing parse-tree warning while completing.
  ✅ Follow-up (2026-07-12): routed the remaining direct libc classifier calls in the CSS tokenizer and HTML5 tokenizer tag-name byte paths through `lib/str.h` ASCII classifiers, and removed their stale `ctype.h` includes. Verified no ctype calls remain in those tokenizers, plus `make build`, `make check-int-cast`, `git diff --check`, CSS and HTML conversion smokes to JSON, and `make layout test=flex_direction_row_reverse`.
  ✅ Follow-up (2026-07-12): removed another classifier/include slice from shared input helpers and ASCII grammar parsers: `parse_hex_codepoint()` now uses `str_is_hex()`, unified KV key scanning and `SourceTracker::skipWhitespace()` use `str_char_is_ascii_space()`, RFC parameter key normalization and shared markup prefix matching use `str_to_upper()` / `str_to_lower()` instead of caller-provided ctype headers, stale CSS/LaTeX parser `ctype.h` includes are gone, and CommonMark raw HTML tag/attribute/autolink scans use `lib/str.h` classifiers. Verified with `make build`, `make check-int-cast`, `git diff --check`, CSS/LaTeX/Markdown conversion smokes, INI/properties conversions (with their existing parser diagnostics), and VCF/ICS/EML conversions to JSON. The follow-ups below close the tracked PDF/markup remainder from this row.
  ✅ Follow-up (2026-07-12): routed PDF parser and PDF postprocess token classifiers through `lib/str.h` (`str_is_space`, `str_is_digit`, `str_is_alnum`, `str_is_hex`) and removed the stale `ctype.h` include from `input-pdf-postprocess.cpp`. Verified no libc ctype calls remain in `input-pdf.cpp` / `input-pdf-postprocess.cpp`, plus `make build`, `git diff --check`, PDF conversions for `test.pdf` and `advanced_test.pdf`, and `test_input_pdf_gtest` (20/20).
  ✅ Follow-up (2026-07-12): routed the remaining markup classifier and case-fold sites through `lib/str.h`, including Textile/AsciiDoc/Man spacing, numbered-list detection, format registry sniffing, and link-reference label comparisons in inline markup via `str_ieq()`. Removed the stale `<cctype>` includes from the markup tree. Verified no direct libc ctype/case-fold calls remain under `lambda/input/markup` or the PDF parser/postprocess files, plus `make build`, `git diff --check`, and Markdown/RST/AsciiDoc/Textile/Org/Man/Wiki conversion smokes to JSON.
- ✅ **Dead `input-utils` helpers duplicating lib:** `input_strncasecmp` (wrapper of `str_icmp`), `try_parse_int64`, `input_split_lines`, `input_trim_whitespace` — 0 callers each; `try_parse_double` has 1. Delete; point future needs at `str_to_int64`/`str_to_double`/`str_trim`/`StrSplitIter`. **Implementation status (2026-07-11): done.** Removed the unused public helpers and declarations; kept `try_parse_double()` as a private helper for its one real `parse_typed_value()` use.
- ✅ **Trailing-trim tail loops** open-coded in `input-kv.cpp:110,144`, `input-prop.cpp:129`, `input-ini.cpp:131`, `input-graph-d2.cpp:70`, `input-yaml.cpp:447,774` — use `str_rtrim`. **Implementation status (2026-07-11): done.** Replaced the key-value/D2 trims with `str_rtrim()`, added `str_rtrim_chars()` for the YAML line-space-only cases, and routed YAML tail trims through the shared lib helper without changing newline semantics.
- ✅ **`input-mark.cpp:parse_binary` (145–203)** hand-checks hex/base64 char classes 3× and `#include`s `lib/base64.h` without using it — use `str_char_is_hex`, `lib/hex.h`, `lib/base64.h`. **Implementation status (2026-07-11): done.** Replaced the repeated hex/base64 character tests with shared `lib/str` classifiers plus one local base64 predicate, preserving the current normalized textual binary payload behavior; the stale base64 include was already absent in the live tree.
- ✅ **`parse_typed_value` numeric sniff (`input-utils.cpp:148–177`)** — base on `str_to_int64`/`str_to_double` end-pointer checks instead of a bespoke char loop. **Implementation status (2026-07-11): done.** Exact-token integer and double parsing now rely on the shared `lib/str` parsers' end pointers, with the old local digit/exponent scan and private `try_parse_double()` removed.

### 2.6 Other repetition
- ✅ **Float boxing:** ~10 sites in `input-toml.cpp` (e.g. 482–503, 604–618) and `input-mark.cpp` (236–252) do `pool_calloc(sizeof(double))`+null-check+`lambda_float_ptr_to_item` when `MarkBuilder::createFloat` exists (and `input-json.cpp:97` already uses it). Low effort. **Implementation status (2026-07-11): done.** TOML, Mark, and shared typed-value float parsing now use `MarkBuilder::createFloat()` instead of local double boxing.
- ✅ **Number parsing:** the "strtod → scan for float marker → integer promotion" shape recurs (`input-json.cpp:65–99`, `input-toml.cpp:472–645`, `input-mark.cpp:236–252`, YAML ×8); TOML additionally repeats its radix blocks 3× (530–584). **Fix:** shared `parse_scanned_number(ctx,&p,flags)` in input-utils. ~120 LOC.
  ✅ Partial: collapsed TOML/YAML copied hex/octal/binary integer parsing into shared `parse_prefixed_integer_value()`, preserving TOML's int64-boxed output, error wording shape, cursor advancement, and source-tracker updates while keeping YAML's non-matching radix-like scalars as strings. Added shared `parse_scanned_decimal_number()` / `scanned_number_has_float_marker()` for scanner-delimited decimal tokens, and routed JSON, TOML decimal numbers, plain Mark numbers, and YAML plain decimal scalars through it so exact integers still promote through `parse_integer_token_exact()` and TOML underscore floats use the same cleaned-token path. The Mark migration also stopped plain large-integer Mark tokens from being rounded through `strtod`; suffixed `n`/`m` Mark tokens now share Lambda literal policy via `decimal_from_literal_string_arena()` (`n` integer-valued only, `m` decimal, `N` retired) instead of the old double fallback. Explicit YAML `!!float` remains a separate tag policy path. Verified with `make build`, `git diff --check`, and JSON/TOML/Mark/YAML conversion smokes covering exact int64-sized and oversized integers, decimal floats, TOML underscores, TOML/YAML hex/octal/binary integers including `0b1010`, YAML invalid radix-like strings, Mark valid `n`/`m` suffixes, Mark fractional-`n`/retired-`N` parser errors, and existing `test/input/value.m` / `test/input/more_test.yaml`.
- **Whitespace/comment skipping:** `input-yaml.cpp:94–148` (5 helpers), `input-jsx.cpp:36–40`, `input-mark.cpp:skip_comments` (16–37) re-implements the same line+block comment skip as `input-graph.h:skip_wsc`. Consolidate one marker-parameterized helper. ~120 LOC.
  ✅ Partial: added pointer-based `skip_whitespace_and_comment_markers()` beside the existing SourceTracker `skip_wsc()` and routed Mark comment skipping through it. Graph parsers are already marker-parameterized via `skip_wsc()`, and JSX already uses `jsx_skip_whitespace()` from `input-jsx-common.hpp`; YAML remains parser-local because its helpers update `YamlParser` cursor state and overlap the broader §2.4 SourceTracker migration, but the speculative YAML cursor state is now centralized through `YamlCursor` save/restore instead of open-coded `pos`/`line`/`col` triples. Verified with `make build`, `./lambda.exe convert temp/cleanup_mark_comments.mark -f mark -t json -o temp/cleanup_mark_comments.json`, DOT/D2/Mermaid graph conversion smokes, existing `test/input/more_test.yaml`, and a focused YAML cursor smoke.
  ✅ Partial (2026-07-12): added shared line-oriented `skip_line_comment_markers()` and `skip_line_whitespace_and_comment_markers()` for parsers that must leave line terminators visible, then routed YAML's horizontal whitespace/comment helpers through them while preserving the parser-local cursor updates. Verified with `make build`, `make check-int-cast`, `git diff --check`, existing `test/input/more_test.yaml` conversion, and a focused YAML fixture covering leading comments, trailing comments, comment-only lines before sequence items, and folded scalar text containing `#`.
  ✅ Partial (2026-07-12): moved the SourceTracker-backed graph whitespace/comment loop into `input-utils.hpp` as `input_skip_whitespace_and_comment_markers()` / `input_skip_to_eol()`, leaving DOT/D2/Mermaid marker choices in their parser wrappers, and routed JSX whitespace classification through the shared `input_is_whitespace_char()` helper while keeping JSX's bounded `end` loop local. Verified with `make build`, `make check-int-cast`, `git diff --check`, JSX conversion, DOT/D2/Mermaid graph conversion smokes, YAML conversion, and Mark conversion via `test/input/value.m`.
- ✅ **`skip_to_newline` divergence:** `input-utils.h:105–114` vs an INI-local variant with different signature (`input-ini.cpp:11–26`) vs YAML `skip_line` — extend the shared one to sync a tracker. **Implementation status (2026-07-11): done.** Added shared raw/tracker-aware newline helpers, fixed the tracker wrapper to advance by consumed byte count instead of byte value, removed the stale INI duplicate through the unified KV parser, and routed YAML `skip_line(YamlParser*)` through the shared raw newline skipper while preserving YAML's own `advance()` state updates. Verified clean build plus YAML/INI/properties conversion smokes; the properties fixture still emits its pre-existing empty-key diagnostic while completing.
- ✅ **MDX vs JSX tag machinery:** `input-mdx.cpp:23–50` vs `input-jsx.cpp:42–44` duplicate component-vs-HTML classification and tag-name scanning; MDX re-finds JSX bounds instead of delegating. Extract `jsx_common.h`. ~80 LOC. **Implementation status (2026-07-11): done.** Added `input-jsx-common.hpp` for JSX identifier/whitespace predicates, component-vs-HTML tag classification, whitespace skipping, and tag-name scanning. Routed JSX and MDX through the shared helpers and removed MDX's stale ctype include. Verified clean build plus JSX, uppercase-component JSX, and MDX conversion smokes.
- ✅ **JSX drops entity resolution** (`input-jsx.cpp:164–186` "preserve as-is for now") that XML centralizes via `html_entity_lookup` (`input-xml.cpp:74–98`) — route JSX through `html_entities.h` (behavioral change; confirm roundtrip expectations). **Implementation status (2026-07-11): done.** JSX text now resolves known named HTML entities through the shared `html_entity_lookup()` table and preserves unknown entities for roundtrip compatibility. Verified clean build plus explicit JSX entity, simple JSX, and MDX conversion smokes; the focused fixture decodes `&amp;`, `&lt;`, and `&copy;` while retaining `&unknown;`.
- ✅ **EML vs RFC-text scaffold:** `input-eml.cpp:parse_header_value` (35–60) re-implements folded-header reading that `input-rfc-text.h` provides to VCF/ICS. ~40 LOC; verify RFC 5322 nuances. **Implementation status (2026-07-11): done.** Refactored the RFC folded-value helper to support EML header whitespace/trim policy, deleted EML's local folded-header parser, and verified EML/ICS/VCF conversions. The VCF guard exposed and fixed a latent container invariant bug: VCF nested/root maps now use direct `.map` Items instead of corrupting container pointers with high-byte tags.
- ✅ **`append_codepoint_utf8` twin overloads** (`input-utils.hpp:40–58`) differing only in buffer type, both appending byte-at-a-time — unify + bulk-append. **Implementation status (2026-07-11): done.** Added one shared UTF-8 codepoint encoder and routed both `StringBuf` and `StrBuf` wrappers through bulk append calls; verified TOML/YAML/Mark unicode parsing and `test/lambda/ext/unicode.ls`.
- ✅ **Dispatch chains:** `input.cpp:mime_to_parser_type` (733–795, ~35 strcmps) + `effective_type` dispatch (852–930, ~25-way if/else) + markup flavor sub-dispatch (876–901) — replace with static `{string, enum}` tables. **Implementation status (2026-07-11): done.** Replaced MIME, exact parser, and markup-flavor ladders with static lookup tables while leaving parameterized PDF/math/graph cases explicit. Verification also exposed a graph post-finalization shape mutation bug: element types are now marked private until shape finalization, `elmt_put()` detaches before appending to a pooled shape, DOT default-attribute statements are parsed separately, and graph child appends keep `TypeElmt::content_length` synced. Verified clean build plus JSON/YAML/HTML/JSX/MDX/CSS/math/PDF/RST/Asciidoc/DOT/D2/Mermaid conversion smokes.
- ✅ **C2 candidate:** `html_entities.*` is consumed beyond input (`css/dom_node.cpp`, `css/dom_element.cpp`, `html5_tokenizer.cpp`, `markup/`) — promote to `lib/` (or `lambda/text/`) so formatters can share the reverse map. Also verify `markup/block/block_html.cpp` reuses `format/html-defs.h` raw-text classifiers rather than a private list. **Implementation status (2026-07-11): done.** Moved `html_entities.{h,cpp}` and the generated table to `lib/`, updated the entity generator and build config, switched callers in JSX/XML/HTML5/markup/Radiant to `lib/html_entities.h`, and removed the old input-layer dependency by using `lib/utf.h` for reverse lookup encoding. Verified clean build plus JSX entity, XML, Markdown/HTML, and layout smokes; one initially guessed XML fixture path did not exist and was replaced with the real validator XML fixture.
- ✅ **HTML5 tokenizer UTF-8 DFA** (`html5_tokenizer.cpp:92–118`) is a legitimate perf choice — leave it; just prevent new hand-rolls (use `lib/str.h`/`lib/utf.h`). **Implementation status (2026-07-11): audited/no code change.** Verified the live tokenizer still uses the documented Hoehrmann DFA for iterator state, while general UTF-8 helpers remain centralized in `lib/str.h`/`lib/utf.h` for new non-tokenizer code.

---

## 3. Formatters + Validator (`lambda/format/`, `lambda/validator/`)

Already good: `lib/escape.h` centralizes escape tables; `format-md/org/rst/wiki/textile.cpp` are 14-line wrappers over table-driven `format-markup.cpp`; `format-kv.cpp` unifies INI/Properties output; `html-defs.cpp` centralizes tag classification.

### ✅ 3.1 Type-dispatch skeleton cloned 8× — C3, HIGH
Every serializer opens with the identical `RecursionGuard` + `isNull/isBool/isInt/isFloat/isString/isArray/isMap/isElement` ladder; only leaf emits differ. Sites: `format-json.cpp:151–232`, `format-yaml.cpp:193–241`, `format-toml.cpp:39–75`, `format-text.cpp:132–160`, `format-kv.cpp:89–133`, `format-html.cpp:351–440`, `format-xml.cpp:182–346`, `format-latex.cpp:17–53`. The `"[max_depth]"` sentinel is re-spelled in each. ~200–240 LOC. **Fix:** a `FormatterContextCpp::dispatch_item(item, Handlers&)` (or CRTP visitor) owning guard + ladder; formatters supply leaf handlers. Start with json/toml/kv (closest). Effort: medium.

**Implementation status (2026-07-11): done.** Added shared `FormatterContextCpp::dispatch_item()` plus `FormatItemHandlersDefault` in `format-utils.hpp`, so the recursion guard, max-depth sentinel hook, and scalar/container type ladder live in one place. Routed JSON, YAML, TOML, KV/INI/Properties, text, LaTeX, HTML, and XML value dispatch through formatter-local handlers while preserving their existing leaf policies, object/type handling, sectioning, scalar extraction, raw-text behavior, XML element handling, and container formatting. Remaining type checks in these files are root-shape decisions, attribute-value policies, or child/container formatting rather than cloned top-level dispatch. Verified with `make build`, `value.ls`, `ext/unicode.ls`, and JSON→JSON/YAML/TOML/INI/Properties/text/LaTeX/HTML/XML temp conversion smokes.

### ✅ 3.2 `map_type->shape` attribute-walk cloned 5× → add `ElementReader::attrs()` — C3, HIGH (best value-per-line)
`format-json.cpp:105–119`, `format-yaml.cpp:147–163`, `format-xml.cpp:263–288`, `format-html.cpp:286–323`, `format-jsx.cpp:58–113` all reach through raw `TypeMap*`/`ShapeEntry*`; `format-jsx.cpp:56–57` even documents the root cause: "ElementReader lacks a generic attribute iterator." The validator does the same at `validate.cpp:430–440`. ~90 LOC + unblocks 3.1/3.8. **Fix:** add an attribute iterator to `ElementReader` mirroring `MapReader::entries()`. Effort: low-medium. *(Same root pattern as core §1.4 — coordinate.)*

**Implementation status (2026-07-11): done.** Added `ElementReader::attrs()` as a stack-only attribute iterator backed by the same typed field reconstruction as `MapReader::entries()`. Routed JSON/YAML/XML/HTML/JSX attribute emission through it and removed JSX's raw-element TODO. Verified HTML/XML to JSON/YAML/XML/HTML plus JSX/MDX fixture roundtrips after a clean build.

### ✅ 3.3 XML hand-rolled escape switch — C1, HIGH
`format-xml.cpp:14–80` — 66-line manual switch duplicating `ESCAPE_RULES_XML_ATTR` (declared in `escape.h`/`format-utils.h:78`), the control-char handling in `format_escaped_string_ex(...ESCAPE_CTRL_XML_NUMERIC)`, and the entity-preservation logic in `format_html_string_safe` (`format-utils.cpp:202–279`). ~70 LOC. **Fix:** use the shared escaper; factor "don't double-encode entities" into one `format_markup_string_safe()` shared by HTML+XML. Verify against XML corpus (apostrophe handling differs).

**Implementation status (2026-07-11): done.** Added shared `format_markup_string_safe()` with an explicit apostrophe policy, routed HTML through the HTML wrapper and XML through the same entity-preserving/control-character path with XML apostrophe escaping. Verified clean build plus XML/HTML corpus smokes and an escape fixture covering `<`, `>`, `"`, `'`, raw `&`, and existing `&amp;`/`&lt;` entities.

### ✅ 3.4 Three TypeId→name implementations — C4, HIGH (correctness)
Canonical `get_type_name()` (`lambda-data.cpp:99`, uses `type_info[]`) vs `validator/error_reporting.cpp:22–28 get_type_name_str()` (same lookup, re-done) vs `validator/doc_validator.cpp:470–484 type_to_string()` (hand-written switch that silently drifts). **Fix:** delete both validator copies, delegate to `get_type_name(type->type_id)`; keep the `TYPE_NUMBER`→"number" special case.

**Implementation status (2026-07-11): done.** Validator error reporting now calls canonical `get_type_name()` directly, and `type_to_string()` delegates to `get_type_name(type->type_id)` while preserving the `TYPE_NUMBER` language-level `"number"` special case.

### ✅ 3.5 Number/datetime formatting bypasses shared helpers — C1, MED
- ✅ Shared `format_number()` (`format.cpp:41–95`, used by kv/toml/yaml/latex) is re-implemented inline in `format-json.cpp:163–171`, `format-text.cpp:22–36`, `format-html.cpp:33–43, 369–372`, `format-xml.cpp:108–116, 277–280`. Add a nan/inf-policy arg (JSON emits "null") and route all through it. ~40 LOC. **Implementation status (2026-07-11): done.** Routed JSON, text, HTML, and XML integer/float emission through the shared `format_number()` helper, including XML element bodies and attributes. Follow-up pass also routed `LMD_TYPE_DECIMAL` through the shared formatter dispatch and number formatter, so exact oversized integer tokens from JSON/TOML/Mark no longer serialize as JSON `null` or YAML `"[type_10]"`. Verified `value.m` to JSON/text/XML plus JSON to HTML after a clean build, then rechecked JSON/TOML/Mark oversized integer conversions to JSON/YAML.
- ✅ Datetime→ISO8601 done 3 ways: `format-json.cpp:221–227`, `format-yaml.cpp:208–219` (StrBuf + `datetime_format_iso8601` dance) and `format-text.cpp:49–61` (a divergent manual snprintf **that drops the time component** — correctness inconsistency). Add one `write_datetime_iso8601()` helper. ~30 LOC. **Implementation status (2026-07-11): done.** Added `format_write_datetime_iso8601()` and routed JSON/YAML/text through it; text output now preserves full ISO datetime/time/offset components. Also fixed `format_number()` to use `Item::get_double()` so MarkReader inline float Items cannot crash YAML formatting.

### ✅ 3.6 Escaper proliferation — C1/C2/C3, MED
Three parallel escaping mechanisms now exist: the `EscapeRule[]`/`escape_append` engine in `lib/escape.c`; `format_text_with_escape`+`TextEscapeConfig` (`format-utils.cpp:91–125`) whose `markdown_escape`/`wiki_escape`/`rst_escape` helpers (24–69) are **entirely dead** (every branch returns NULL); and `format-graph.cpp:9–33`'s `GraphEscapeRules`. **Fix:** delete the dead escape_fns; express Markdown/RST/Wiki/DOT/Mermaid/D2 escaping as `EscapeRule[]` tables in `lib/escape.h`; retire the two bespoke engines. ~80 LOC.

**Implementation status (2026-07-11): done.** Deleted the dead Markdown/Wiki/RST escape callbacks and set those `TextEscapeConfig` entries to use the existing backslash escape path directly. Added shared DOT and graph-quoted escape rule tables to `lib/escape`, then routed DOT/Mermaid/D2 graph formatting through `escape_append_stringbuf()` while preserving each flavor's quote-trigger policy. Verified with `make build`, `temp/cleanup_graph_escape.ls`, `test_stringbuf_gtest` escape filters, and `test_strbuf_gtest` escape filters.

### ✅ 3.7 Math ASCII/LaTeX dispatch ladders — C3, MED
`format-math-ascii.cpp:496–562` and `format-math-latex.cpp:645–704` — two ~30-branch `strcmp(tag,...)` ladders with near-identical tag sets; both also re-declare `format_children` identically (ascii:723, latex:710). ~70 LOC. **Done:** extended `format-math-shared.hpp` with a shared `{tag, handler-slot}` lookup and converted both ASCII and LaTeX top-level element dispatch ladders to per-format slot tables. Kept ASCII's custom child coalescing separate because the live tree's `format_children` is no longer identical to LaTeX's simple child loop. Verified clean build, `test_math_ascii_gtest`, math-ascii→math-ascii, math-ascii→math-latex, and latex→latex conversion; the latex fixture keeps its pre-existing parse recovery warnings while completing.

### 3.8 Smaller items
- ✅ **Entry-point pool ceremony ×10** (`stringbuf_new` → `mem_pool_create` → ctx → destroy → `to_string`) at `format-json.cpp:238,249`, `format-xml.cpp:349,415`, `format-yaml.cpp:254`, `format-latex.cpp:183`, `format-html.cpp:51,139`, `format-text.cpp:167`, `format-markup.cpp:1247`, several with `_to_strbuf` twins — a `ScopedFormatPool` RAII/template helper (also fixes early-return leak risk). ~40 LOC. **Implementation status (2026-07-11): done.** Added `ScopedFormatPool` in `format-utils.hpp` and routed JSON/XML/YAML/LaTeX/HTML/text/markup entry points and string-buffer variants through it, removing manual destroy calls including HTML's early-return paths. Verified clean build plus JSON, YAML, XML, HTML, text, LaTeX, and Markdown conversion smokes; the LaTeX fixture keeps its pre-existing parse-tree warning while completing.
- ✅ **XML map path walks the map 3× and duplicates attr-value emit** (`format-xml.cpp:92–180, 260–289`) — single `xml_emit_attr_value()` + one partitioning pass. ~50 LOC. **Implementation status (2026-07-11): done.** Added shared `xml_emit_attr_value()`, routed both XML map attributes and element attributes through it, and collapsed map formatting to one partitioning pass that writes simple fields as attributes while buffering complex child elements. Verified clean build plus XML output smokes, including entity escaping and a map/null/child partition fixture.
- ✅ **HTML special-node memcmp cascade** (`format-html.cpp:160–280`, seven `#document/#doctype/#comment/...` blocks; the "emit first child verbatim" idiom ×5) — table + helper; overlaps XML's `?xml` handling (`format-xml.cpp:238–255`). ~40 LOC. **Implementation status (2026-07-11): done.** Added shared HTML name/raw-string helpers and table-driven special tag dispatch for `#document`, `#doctype`, `#comment`, legacy `!--`, and `?xml`, while keeping legacy doctype-prefix and raw-HTML attribute cases explicit. Verified clean build plus HTML, MDX-to-HTML, and focused escape HTML conversion smokes.
- ✅ **YAML quoting heuristic** (`format-yaml.cpp:22–69`) — extract a testable `yaml_scalar_needs_quotes()`; longer-term share the numeric-literal recognizer with the input side. **Implementation status (2026-07-11): done.** Extracted the scalar quoting decision into `yaml_scalar_needs_quotes()` without changing the emission rules; verified clean build plus Mark/JSON to YAML conversions.
- ✅ **Validator** re-walks structures with the same raw-shape boilerplate as formatters (`validate.cpp:317, 392, 430–440, 551, 603–628`) — adopt the §3.2 iterators. **Implementation status (2026-07-11): done.** Added a shared local schema-shape validation helper in `validate.cpp` and routed map field validation through `MapReader::has/get()` plus element attribute validation through `ElementReader::has_attr/get_attr()`, removing the raw data-shape probes while preserving the existing map missing/null semantics and element-attribute-only-if-present behavior. Verified clean build, `test_validator_features_gtest`, `test_validator_gtest`, and `test_format_validation`; `test_validator_path_reporting` still has pre-existing array-path failures because `company_with_arrays.ls` is currently treated as an exact one-item array pattern and stops at `.employees`.
- ✅ **`print.cpp` overlap** — flagged jointly with core §1.7: `print` is effectively a Lambda-native serializer parallel to format-json; share number/datetime/escape helpers. **Implementation status (2026-07-11): done for the remaining safe overlap.** The field-slot conversion clone is now shared via `map_shape_field_to_item()`; number/datetime/decimal paths already use the canonical helpers. Native print string quoting remains separate by design because it is not JSON/XML serialization.

---

## 4. Radiant Layout + CSS (`radiant/` layout side)

**Answer to "multiple sub-layouts — any code can be shared?": yes, substantially — but the primary lever is adopting the shared helpers that already exist** (`layout_box.cpp`, `layout_alignment.hpp`, `layout_axis.hpp`, `available_space.hpp`, `lib/`), which flex in particular has largely bypassed. Checked and NOT a problem: CSS keyword→enum mapping is properly shared via `css_value.hpp` (only 3 strcmp sites in `resolve_css_style.cpp`).

### ✅ 4.1 Box-metric sums hand-rolled ~160+ times despite `layout_box_metrics()` — C3/C2, HIGHEST
The helper (`layout_box.cpp:5–67`) computes exactly `padding_h/border_h/pad_border_h/...` — but is called in only 4 places, all in `layout_table.cpp`; block/flex/grid/positioned/inline/intrinsic call it **0 times**. Grep: 84 `padding.left + padding.right`, 79 `border->width.left + …right`, 19 margin sums, each usually with the repeated `if (bound->border)` guard the helper encapsulates. Sites incl. `layout_block.cpp:3012, 3177, 3211, 3226, 5694, 6389`, `layout_flex.cpp:110, 222, 418, 631, 772, 935, 1013`, `intrinsic_sizing.cpp:2445, 2451, 2533, 6123`. ~300–500 LOC. **Fix:** mechanical migration. Low risk, medium effort, big readability win.

**Implementation status (2026-07-11): done.** Added `layout_boundary_metrics()` so non-`ViewBlock` element boxes can share the same `BoundaryProp` padding/border/margin aggregation, and extended `BoxMetrics` with horizontal/vertical margin aggregates. Routed the hand-rolled padding/border/margin aggregates across grid, block, intrinsic sizing, flex, flex measurement/multipass, grid multipass, positioned layout, multicol, form sizing, table caption/table layout, block context, scroller, and column-rule rendering through `layout_box_metrics()` or `layout_boundary_metrics()`. The remaining grep hits are `layout_box.cpp`'s own aggregate implementation and `render_text.cpp`'s raw `BorderProp` painter, which is not a layout box. Verified with `make build`, a full Radiant aggregate grep, and focused smokes for grid, block, intrinsic, flex, table/caption, multicol, and absolute positioning. The layout smokes still emit pre-existing optional `test_helper.js`, parser fixture, or cache-layout memtrack diagnostics depending on fixture, but all commands completed with `1 success, 0 failed`.

### 4.2 Min/max clamping reimplemented despite `layout_apply_min_max_width/height()` — C3, HIGH
Helper at `layout_box.cpp:79–131` (incl. min-overrides-max precedence + border-box floor). `layout_flex.cpp` and `layout_grid.cpp` call it **0 times** yet contain dozens of inline clamps (`layout_flex.cpp:2361–2362, 3073–3076, 3221, 5356, 5373`, `layout_table.cpp:462, 9340–9349`, `intrinsic_sizing.cpp:6405`, +82 inline `given_max_*` comparisons). ~150–250 LOC. **Fix:** migrate; add a `layout_apply_min_max_axis()` wrapper for flex's axis-generic needs. Do flex last (FLT_MAX/auto-min subtleties).

✅ Partial: added `layout_apply_min_max_axis()` and routed the straightforward border-box/content-box clamp sites in grid positioning and flex explicit/cross-axis sizing through the shared min/max helpers. The remaining raw `given_min_*` / `given_max_*` hits are concentrated in flex algorithm constraint discovery, flex measurement paths that subtract padding after establishing an available content width, and table collapsed-border/natural-content floors; those still need equivalence checks before migration. Verified with `make build` plus focused grid/flex/positioned layout smokes; the smokes still emit the pre-existing optional `test_helper.js` warning and cache-layout memtrack shutdown diagnostic, but all completed with `1 success, 0 failed`.

✅ Partial (2026-07-12): split min/max-only clamping into `layout_clamp_min_max_width/height/axis()` so sites that already hold a natural/content contribution can share min/max precedence without also applying the border-box padding floor from `layout_apply_min_max_*()`. `layout_apply_min_max_*()` now delegates through the clamp helpers before preserving its existing border-box floor, and the straightforward flex intrinsic item-width and table-cell width clamps route through the shared helper. Verified with `make build` and `git diff --check`.

✅ Partial (2026-07-12): added positive-only contribution clamp helpers for intrinsic/grid/flex measurement paths whose existing policy treats `0` like no contribution constraint, then routed grid pre-measured row/column contributions and flex measured row/content-width clamps through them. Verified with `make build`, `make check-int-cast`, `git diff --check`, grid smokes for `grid_min_content_flex_single_item_margin_auto` and `grid_basic_implicit_tracks`, flex smokes for `flex_010_nested_text`, `flex_017_text_wrapping`, `wrapped_column_max_height_flex`, `flex_wrap_children_with_min_main_overriding_flex_basis`, and `min_max_percent_no_width_height`; two initially guessed flex baseline paths were stale and rerun from `test/layout/data/flex-nest/`.

✅ Partial (2026-07-12): routed two remaining direct border/content-box min/max conversion branches through the shared CSS-size helpers: grid multipass row max-height contribution capping now uses `layout_css_size_to_border_box()`, and absolutely positioned aspect-ratio sizing from `max-height` now uses `layout_css_size_to_content_box()` plus the shared border-box predicate. Verified with `make build`, `make check-int-cast`, `git diff --check`, `grid_min_content_flex_single_item_margin_auto`, `absolute_aspect_ratio_fill_max_height`, and `absolute_child_with_max_height`.

✅ Partial (2026-07-12): routed table explicit-height min/max precedence through `layout_clamp_min_max_height()` instead of the local two-branch clamp. The table natural-content floor paths remain explicit because they intentionally apply min-only constraints and must not shrink below content. Verified with `make build`, `make check-int-cast`, and `git diff --check`.

✅ Partial (2026-07-12): added min-only floor helpers (`layout_floor_min_width/height/axis()`) for layout sites whose comments explicitly say max constraints must not shrink natural/intrinsic content, then routed the block auto-width table floors and table final natural-content floors through them. Verified with `make build`, `make check-int-cast`, `git diff --check`, and table/caption layout comparisons for `radiant_table_caption_box_width`, `table_caption_variations`, and `table_017_caption_bottom`. The WPT `min-max-size-table-content-box` fixture still exposes the pre-existing row/cell height propagation gap for min/max-height tables: table boxes match browser dimensions, but `tbody`/`tr`/`td` heights do not expand with the table.

✅ Partial (2026-07-12): routed flex hypothetical-main-size clamping through the existing `apply_flex_constraint()` path instead of maintaining a second min/max ladder for regular flex items and a third form-control-specific clamp. The form-control guard now explicitly allows `ITEM_PROP_FORM` items without `fi`, preserving their intrinsic min-size policy while sharing min-over-max precedence with the rest of flex. Verified with `make build`, `make check-int-cast`, `git diff --check`, `make layout test=flex_basis_flex_shrink_row`, `make layout test=flex_grow_to_min`, `make layout test=min_max_percent_no_width_height`, and `./lambda.exe layout temp/cleanup_flex_form_hypothetical.html`; the temp smoke still emits the known cache-layout memtrack shutdown diagnostic while completing with `1 success, 0 failed`.

✅ Partial (2026-07-12): added positive min/max reader helpers (`layout_positive_min_*`, `layout_positive_max_*_or`) for code paths that intentionally treat `<= 0` as no constraint, then routed flex aspect-ratio transferred constraints, form-control positive max constraints, image intrinsic max-width measurement, column-wrap max-height caps, and flex shrink-to-fit definiteness checks through them. This leaves the `>= 0` flex auto-min/explicit-zero paths untouched. Flex core raw `given_min/max_*` mentions are now 60 in `layout_flex.cpp` (64 across the flex files). Verified with `make build`, `make check-int-cast`, `git diff --check`, and layout smokes for `flex_basis_flex_shrink_row`, `wrapped_column_max_height_flex`, `flex_wrap_children_with_min_main_overriding_flex_basis`, `min_max_percent_no_width_height`, `aspect_ratio_flex_row_fill_max_height`, `aspect_ratio_flex_column_stretch_fill_max_width`, `radiant_form_replaced_measure_cache`, and `image_001_basic_layout`.

✅ Partial (2026-07-12): added explicit-min reader helpers (`layout_explicit_min_*_or`, `layout_has_explicit_min_*`) for flex paths that must distinguish `min-width:auto` (`<0`) from explicit `min-width:0`, and routed regular flex item constraint discovery plus the form-control min-size branch through them. `layout_positive_min_*` now delegates through the same explicit reader while preserving its `<=0`-means-no-positive-constraint policy. Flex core raw `given_min/max_*` mentions are now 57 in `layout_flex.cpp` (61 across the flex files by the same raw token scan). Verified with `make build`, `make check-int-cast`, `git diff --check`, flex smokes for `flex_basis_flex_shrink_row`, `align_items_stretch_min_cross`, and `percentage_flex_basis_cross_max_height`, plus form-control smokes for `input-width`, `form-controls`, and `form-controls-nested-id-crash`. Broader `form` and `buttons` comparisons still fail on unrelated pre-existing structure/style drift and were not counted for this slice.

✅ Partial (2026-07-12): added explicit-max reader helpers (`layout_explicit_max_*_or`) and made the positive max readers delegate through them, then routed Phase 5b flex container min-size writes, parent flex definite-height probes, and flex multipass border-box height constraints through the shared positive/explicit min/max readers. This preserves `> 0` positive-constraint policy in Phase 5b and `>= 0` explicit-height policy in multipass. Flex raw `given_min/max_*` mentions are now 32 in `layout_flex.cpp` and 32 across the flex files by the same token scan. Verified with `make build`, `make check-int-cast`, `git diff --check`, and layout smokes for `align_items_center_with_min_height_with_align_content_flex_start_with_wrap`, `align_items_center_with_min_height_percentage_with_align_content_flex_start`, `wrapped_column_max_height_flex`, `yoga-flexwrap-wrapped-column-max-height-flex`, `flex_wrap_children_with_min_main_overriding_flex_basis`, and `min_max_percent_no_width_height`.

### ✅ 4.3 Seven+ `is_out_of_flow` clones + 96 raw inline checks — C3/C4, HIGH
Defs at `layout.cpp:1085, 1582`, `layout_inline.cpp:52`, `layout_multicol.cpp:262`, `layout_block.cpp:1787, 2739`, `layout_text.cpp:1500`, `layout_table.cpp:2257`, `stacking_order.cpp:19` — all test `ABSOLUTE || FIXED` (some also float). Plus parallel float-detection helpers (`intrinsic_sizing.cpp:1947`, `layout_table.cpp:5427`, `layout_block.cpp:5234`). **Done:** added canonical `layout_position_is_abs_fixed()`, `layout_position_is_floated()`, `layout_view_is_out_of_flow[_positioned]()` and `layout_block_is_out_of_flow[_positioned]()` in `layout.hpp`; routed the named clone helpers and most raw resolved-position call sites across block/inline/multicol/table/text/intrinsic/stacking code through them. Remaining explicit checks distinguish fixed vs absolute scroll behavior or include relative/sticky containing-block semantics. Verified with `make build` plus layout smokes for fixed abs/z-index blending, block-in-inline floats, inline table content, and table out-of-flow border collapse.

### 4.4 Flex never migrated to `layout_alignment.hpp` — C4, HIGH
`layout_alignment.hpp:11` says "Extracted from duplicated code in layout_flex.cpp and layout_grid.cpp" — grid-multipass uses it (2 calls), flex uses it **0 times** and re-implements the full justify-content switch (`layout_flex.cpp:4599–4658`) plus a second cross-axis block. ~60+ LOC. Caveat: flex uses int truncation for spacing vs float in the shared version — sub-pixel behavior may shift; re-baseline layout tests.

✅ Partial (2026-07-12): flex main-axis `justify-content` and wrapped-line `align-content` now use `radiant::compute_space_distribution()` instead of local switch ladders, while preserving flex's physical `start`/`end` handling for reverse directions and explicit gap accounting. `compute_space_distribution()` now keeps signed offsets for non-`space-*` overflow and uses start fallback for single-subject `space-between`, matching flex behavior. Verified with `make build`, `make check-int-cast`, `git diff --check`, direct layout smokes for row `space-between`/`space-around`/`space-evenly` and `flex_017_align_content`, plus `make layout test=justify_content_row_space_between` and `make layout test=flex_017_align_content`.

✅ Partial (2026-07-12): flex cross-axis regular `align-items`/`align-self` positioning now routes start/end/center through `radiant::compute_alignment_offset_simple()` using margin-box free space. Stretch and baseline remain flex-specific because they mutate item cross size or depend on flex baseline groups. Removed touched `int` dimension temporaries in the stretch path. Verified with a clean debug link after clearing stale generated `html_entities` dependency state, `make check-int-cast`, `git diff --check`, and `make layout` comparisons for `align_items_flex_start`, `align_items_flex_end`, `align_items_center_justify_content_center`, `flex_012_align_self`, and `align_flex_start_with_stretching_children`.

### 4.5 Four recursive first-baseline walkers — C3, MED-HIGH
`layout_alignment.cpp:209` (shared, grid-only user), `grid_baseline.hpp:131–165`, `layout_flex.cpp:3494–3563`, `layout_table.cpp:905, 973` — same recursion, same `font_size * 0.8` ascent, same skip-positioned + `padding.top + border.top`. ~150 LOC. **Fix:** one `compute_first_baseline(View*, options)` in `layout_alignment.cpp`; table keeps its cross-cell aggregation. Baseline is subtle — medium risk.

✅ Partial: strengthened `compute_element_first_baseline()` with the grid walker's existing font/padding/border baseline fallback and DOM-child recursion, then routed grid baseline alignment through that shared helper. Flex and table remain explicit for now because the current code includes flex-line stored baselines, text-line baseline fallbacks, table-row aggregation, strut baselines, and table-specific skip rules that are not equivalent to the simple grid/shared walker. Verified with `make build` plus focused grid baseline smokes for simple, multiline child, and double-nested child cases.

✅ Partial (2026-07-12): extracted the duplicated font-ascender decision used by baseline walkers into `compute_font_baseline_ascender()`, covering normal line-height splits, OS/2 typo metrics, hhea fallback, and CSS `0.8 * font-size` synthesis in one shared helper. Routed shared/grid first-baseline synthesis, table text/inline-strut baseline discovery, and flex text-only baseline fallback through it while keeping flex and table's different recursive row/line policies explicit. Verified with `make build` and layout comparisons for `grid_align_items_baseline`, `grid_align_items_baseline_child_multiline`, `align_baseline`, `align_items_flex_start`, `align_items_center_justify_content_center`, `table_simple`, and `table-vertical-align-baseline-001`; an initially guessed `grid_baseline_simple` fixture did not exist and was replaced with the real grid baseline fixtures.

### 4.6 Percentage re-resolution: grid and flex built parallel machines — C3/C4, MED-HIGH
`layout_grid_multipass.cpp:57–254` (full candidate/cascade machine: `GridPaddingCandidate`, `grid_re_resolve_item_percentage_*`) vs flex's ad-hoc version (`layout_flex_measurement.cpp:368, 2577, 2592`, `layout_flex.cpp:202, 1963–2002`). Also both engines duplicate "containing content width for child percentages" near line-for-line (`flex_item_content_width_for_child_percentages` `layout_flex_measurement.cpp:368–427` vs `grid_container_content_width_for_item_percentages` `layout_grid_multipass.cpp:256–305`). ~320 LOC. **Fix:** new `layout_percentages.cpp` with `layout_reresolve_percentage_box(item, inline_base)` + `layout_resolve_containing_content_width/height()`; both engines call it. Also likely closes flex feature gaps on logical properties.

✅ Partial (2026-07-12): added `layout_percentages.{hpp,cpp}` with shared `CssValue` percentage resolution, used/given/declared CSS-size-to-content-box helpers, and an inline-base auto-width helper. Routed flex child-percentage content-width, flex margin/line-height percentage arithmetic, grid percentage spacing re-resolution, and grid item percentage-base width/height resolution through the shared helpers while keeping each engine's percentage/auto/fallback policy local. Verified with `make build`, `make check-int-cast`, `git diff --check`, and layout comparisons for `min_max_percent_no_width_height`, `grid_basic_implicit_tracks`, `grid_auto_fill_fixed_size`, `grid_min_content_flex_single_item_margin_auto`, and `flex_basis_flex_shrink_row`.

✅ Partial (2026-07-12): promoted the grid-local percentage padding/margin cascade machine into shared `layout_reresolve_percentage_box()`, preserving physical/logical shorthand specificity and margin percentage type metadata. Grid multipass now calls the shared entry point at style resolution, measurement, and final item layout sites. Verified with `make build`, `make check-int-cast`, `git diff --check`, and layout comparisons for `grid_basic_implicit_tracks`, `grid_auto_fill_fixed_size`, `grid_min_content_flex_single_item_margin_auto`, and `min_max_percent_no_width_height`.

✅ Partial (2026-07-12): added shared deferred percentage scalar helpers in `layout_percentages` and routed flex item's width/height/min/max percentage re-resolution through them. Flex still owns its intrinsic-sizing exception and max-size `none` fallback, but the duplicated percent/base arithmetic and target updates now share the same helper used by the broader percentage module. Verified with `make build`, `make check-int-cast`, `git diff --check`, and layout comparisons for `min_max_percent_no_width_height`, `percentage_flex_basis_cross_max_height`, `flex_basis_flex_shrink_row`, and `grid_basic_implicit_tracks`.

### ✅ 4.7 Box-shorthand re-parsing — C1/C4, MED
`intrinsic_sizing.cpp:507–560` re-implements CSS `padding: a/ab/abc/abcd` side-selection and `intrinsic_border_width_from_shorthand_value` (364–453); `layout_grid_multipass.cpp:64–72` and flex-measurement have variants — all duplicating what `resolve_css_style.cpp` does when populating `bound->padding`. ~200 LOC. **Fix:** extract `css_expand_box_shorthand_side(value, side)`; better, have intrinsic sizing read resolved `bound` values where it can.
✅ Done: added shared `css_box_shorthand_side_value()` in `layout_box.hpp` and routed the grid, flex measurement, table, and intrinsic sizing shorthand side-selection fallbacks through it. Verified with `make build`, `./lambda.exe layout temp/cleanup_box_shorthand_layout.html`, `./lambda.exe layout test/layout/data/wpt-css-tables/table-intrinsic-size-001.html`, `./lambda.exe layout test/layout/data/wpt-css-tables/border-collapse-dynamic-oof.html`, and `./lambda.exe layout test/layout/data/wpt-css-box/margin-trim/flex-row-orthogonal-item.html` (the temp smoke still emits the pre-existing cache-layout memtrack shutdown diagnostic).

### 4.8 Other items
- ✅ **Hand-rolled growable arrays** (C1): `layout_flex.cpp:586–588, 2091, 3955–3971`, `layout_grid.cpp:25–31, 1057, 1179`, `grid_utils.cpp:43–48, 168, 320` — each with its own `count/allocated/realloc×2` loop ≡ `lib/arraylist`. Perf-sensitive: measure, or share one `grow_ptr_array()` helper. ~120 LOC.
  ✅ Done: reused `lib/mem_grow.hpp` for flex item/line growth, grid item growth, and grid line-name/area growth; removed the old direct `mem_realloc` growth patterns in the listed files. Verified with `make build`, `./lambda.exe layout temp/cleanup_grow_layout_arrays.html`, `./lambda.exe layout test/layout/data/wpt-css-box/margin-trim/flex-row-orthogonal-item.html`, `./lambda.exe layout test/layout/data/baseline/grid_basic_implicit_tracks.html`, and `./lambda.exe layout test/layout/data/baseline/grid_auto_fill_fixed_size.html` (grid baseline smokes still report the pre-existing optional `test_helper.js` warning and cache-layout memtrack shutdown diagnostic).
- ✅ **Flex scratch arrays** — 7 parallel `mem_calloc(line->item_count,…)` per resolve pass (`layout_flex.cpp:4076–4092, 4315–4318`); use one `FlexItemScratch` struct array and consider `lib/scratch_arena.h` (purpose-built).
  ✅ Done: replaced the parallel flex-basis/hypothetical/frozen arrays with `FlexLengthScratch`, and the per-iteration target/clamp/violation arrays with `FlexIterationScratch` in `resolve_flexible_lengths()`. Verified with `make build`, `./lambda.exe layout temp/cleanup_grow_layout_arrays.html`, `./lambda.exe layout test/layout/data/wpt-css-box/margin-trim/flex-row-orthogonal-item.html`, `./lambda.exe layout test/layout/data/baseline/grid_min_content_flex_single_item_margin_auto.html`, `./lambda.exe layout test/layout/data/baseline/flex_basis_flex_shrink_row.html`, and `./lambda.exe layout test/layout/data/baseline/flex_grow_to_min.html` (baseline fixtures still emit the pre-existing optional `test_helper.js` warning and cache-layout memtrack shutdown diagnostic).
- ✅ **Auto-margin centering math** repeated (`layout_block.cpp:3498, 6462, 6986, 7078`, `layout_flex.cpp:4553–4557, 4667–4671, 4990`, `layout_flex_multipass.cpp:906, 918`) — `layout_center_in_free_space()` + `count_auto_margins()`. ~60 LOC.
  ✅ Done: added `layout_count_auto_margins()`, `layout_auto_margin_share()`, and `layout_resolve_auto_margin_pair()` in `layout_box.hpp`, then routed block auto-margin resolution, flex main-axis auto-margin sharing, and flex cross-axis auto-margin centering through those helpers. Verified with `make build`, block auto-margin baselines, flex auto-margin/shrink baselines, and grid/flex margin-auto smokes; several baseline fixtures still emit the pre-existing optional `test_helper.js` warning and cache-layout memtrack shutdown diagnostic.
- **`layout_axis.hpp` under-adopted** (C2/C3): flex has ~129 manual `is_horizontal` branches duplicating "row→width/x, column→height/y" that `layout_axis_size/pos/set_*` abstract. Incremental adoption in hot paths.
  ✅ Partial (2026-07-12): added physical axis-side helpers for spacing, border, padding, margin, and flex gap selection in `layout_axis.hpp`, then routed flex main-axis outer-size, main/cross position getters/setters, and gap-space calculation through them. This removes duplicated left/top vs right/bottom branching from the shared flex utility path while leaving algorithm-specific sizing branches intact. Verified with `make build`, `make check-int-cast`, `git diff --check`, and `make layout` comparisons for row `space-between`, row-reverse, column-reverse, and `flex_012_align_self`.
  ✅ Partial (2026-07-12): routed flex alignment-loop auto-margin detection, margin start/end reads, and per-axis gap lookup through the new `layout_axis` helpers. This removes another cluster of horizontal/vertical branch duplication in `align_items_main_axis()` and `align_items_cross_axis()` without touching stretch/baseline sizing policy. Verified with `make build`, `make check-int-cast`, `git diff --check`, and `make layout` comparisons for `flex_009_auto_margins`, `yoga-gap-column-gap-justify-flex-start`, row-reverse, and `flex_012_align_self`.
  ✅ Partial (2026-07-12): added a `BlockProp` axis-size accessor and folded flex cross-axis size calculation onto one width/height-neutral path using `layout_axis_size()`, `layout_boundary_padding_border_axis()`, and `layout_apply_min_max_axis()`. The line-wrapping item-margin and `align_content` line-gap paths now use `layout_axis` helpers too. Verified with `make build`, `make check-int-cast`, `git diff --check`, and `make layout` comparisons for `flex_017_align_content`, `yoga-gap-column-gap-justify-flex-start`, `flex_012_align_self`, `align_flex_start_with_stretching_children`, `margin_and_stretch_column`, `align_items_stretch_min_cross`, and `min_max_percent_no_width_height`.
  ✅ Partial (2026-07-12): added a `BlockProp` max-size axis accessor and routed Phase 7c flex max-width/max-height clamp selection and container size writes through `layout_axis`. This removes another row/column mapping ladder while preserving the existing content-box conversion and redistribution behavior. Verified with `make build`, `make check-int-cast`, `git diff --check`, and `make layout` comparisons for `yoga-justifycontent-justify-content-column-max-height-and-margin` and `justify_content_row_max_width_and_margin`.
  ✅ Partial (2026-07-12): removed the flex-local `get_border_box_width()` / `get_border_box_height()` wrappers and routed `get_main_axis_outer_size()` directly through `layout_axis_size()`. This trims another old width/height branch from the shared main-axis sizing path without changing the flex border-box invariant. Verified with `make build`, `make check-int-cast`, `git diff --check`, and `make layout` comparisons for `flex_basis_flex_shrink_row`, `flex_009_auto_margins`, `justify_content_row_space_between`, and `flex_direction_row_reverse`; an attempted `row_reverse` fixture name did not exist and was replaced by the real baseline fixture.
  ✅ Partial (2026-07-12): made `layout_axis`'s flex main-axis helper writing-mode-aware and routed the legacy `is_main_axis_horizontal()` adapter through it. This preserves the vertical-writing-mode row/column inversion while giving flex one canonical direction-to-axis mapping. Verified with `make build`, `make check-int-cast`, `git diff --check`, `make layout` comparisons for `flex_direction_row`, `flex_direction_row_reverse`, `flex_direction_column`, and `align_items_center_justify_content_center`, plus a focused vertical-writing-mode flex layout smoke.
- **Box-sizing unwinding** — 59 inline `box_sizing == CSS_VALUE_BORDER_BOX` subtract sites despite `layout_content_width_from_border_box()` (`layout_box.cpp:45–67`; flex-measurement 384–423 uses it correctly, most others don't). ~120 LOC, overlaps 4.1.
  ✅ Partial (2026-07-12): routed form-control specified width/height sizing through `layout_css_size_to_content_box()` and `layout_css_size_to_border_box()`, and routed table-caption specified width through `layout_css_size_to_border_box()`. This removes local content-vs-border-box conversion branches while preserving intrinsic-size fallback and min/max policy. Verified with `make build`, `make check-int-cast`, `git diff --check`, form/box-sizing layout smokes, and table-caption layout smokes.
  ✅ Partial: added axis-aware `layout_content_size_from_border_box()`, `layout_border_size_from_content_box()`, `layout_padding_border_axis()`, `layout_floor_border_box_axis()`, and `layout_boundary_padding_border_axis()` wrappers beside the existing width/height helpers, then routed the straightforward flex max/min-height, main/cross-axis clamp, nested measurement border-box/content-box conversions, and the old flex-local pad/border axis helper through them. Remaining explicit `box_sizing` checks are largely algorithm branches (table collapsed-border sizing, flex constraint discovery, intrinsic fallback policy) and need per-engine equivalence checks. Verified with `make build`, `git diff --check`, and focused flex smokes for wrapped column max-height, height/padding/border wrap alignment, min/max width, and nested text wrapping; two fixtures still emit the pre-existing cache-layout memtrack shutdown diagnostic while completing with `1 success, 0 failed`.
  ✅ Partial (2026-07-12): added boundary-based CSS size conversion helpers (`layout_css_size_to_content_box()` / `layout_css_size_to_border_box()`) and routed flex measurement explicit/percentage/resolved width handling plus flex-basis, auto-min specified-size, init-container definite max/min detection, column wrap max-height, Phase 7b/7c max/min content conversions, cross-axis explicit sizing, and hypothetical cross-size conversions through them. This removes repeated `box_sizing` padding/border branches from the flex sizing path while leaving policy-heavy constraint discovery branches explicit. Verified with `make build`, `make check-int-cast`, `git diff --check`, and layout comparisons for `flex_basis_flex_shrink_row`, `align_items_stretch_min_cross`, `flex_012_align_self`, `margin_and_stretch_column`, `min_max_percent_no_width_height`, `yoga-justifycontent-justify-content-column-max-height-and-margin`, and `justify_content_row_max_width_and_margin`.
  ✅ Partial (2026-07-12): added shared `layout_box_sizing()` / `layout_uses_border_box()` helpers and routed the repeated null-sensitive border-box predicates in absolute-positioned layout plus form-control layout through them. This keeps content-box as the default policy in one helper and trims another cluster of direct `block->blk && block->blk->box_sizing` checks before deeper conversion work. Verified with `make build`, `make check-int-cast`, `git diff --check`, JS path smoke after the same build, and layout smokes for absolute inset/max-height/padding-border cases plus form-control cases.
  ✅ Partial (2026-07-12): routed grid positioning and grid multipass border-box predicates through `layout_uses_border_box()`, keeping the default content-box fallback in the shared helper while preserving the existing percentage-size and stretch/max/min adjustment branches. Verified with `make build`, `make check-int-cast`, `git diff --check`, and layout smokes for `grid_basic_implicit_tracks`, `grid_auto_fill_fixed_size`, `grid_min_content_flex_single_item_margin_auto`, and `min_max_percent_no_width_height`.
  ✅ Partial (2026-07-12): routed flex multipass height-clamp predicates and multicol content-limit/fragmented-height predicates through `layout_uses_border_box()`. This removes another direct `blk->box_sizing` cluster while leaving the line, fragment, and max-height math unchanged. Verified with `make build`, `make check-int-cast`, `git diff --check`, multicol smokes for `column-height-006`, `spanner-fragmentation-005`, and `columnfill-auto-max-height-001`, plus flex smokes for `justify_content_row_max_width_and_margin`, `yoga-justifycontent-justify-content-column-max-height-and-margin`, `flex_017_align_content`, and `flex_wrap_wrap_to_child_height`; one initially guessed flex fixture path did not exist and was replaced by real baseline fixtures.
  ✅ Partial (2026-07-12): routed table-caption min/max clamp predicates and flex item explicit-size/flex-basis/cross-size content-box checks through `layout_uses_border_box()`, converting `ViewElement*` flex items through `view_as_block()` at the call sites. Verified with `make build`, `make check-int-cast`, `git diff --check`, flex smokes for `flex_basis_flex_shrink_row`, `flex_018_min_max_width`, and `percentage_flex_basis_cross_max_height`, plus caption smokes for `radiant_table_caption_box_width`, `table_caption_variations`, `table_017_caption_bottom`, and `table-caption-margins-001`; two initially guessed caption fixture paths did not exist and were replaced by real fixtures.
  ✅ Partial (2026-07-12): routed the remaining flex measurement row-width border/content-box predicates through `layout_uses_border_box()` as well. The flex/grid/form/positioned/multicol/table-caption subset now has no direct `box_sizing == CSS_VALUE_BORDER_BOX` predicate checks; remaining direct checks are in block/table/intrinsic/view-pool paths that need their own sizing-policy review. Verified with `make build`, `make check-int-cast`, `git diff --check`, and nested/wrapped flex smokes for `flex_010_nested_text`, `flex_017_text_wrapping`, `wrapped_column_max_height_flex`, `yoga-flexwrap-wrapped-column-max-height-flex`, and `flex_wrap_children_with_min_main_overriding_flex_basis`.
  ✅ Partial (2026-07-12): routed table layout's remaining direct border-box predicates through `layout_uses_border_box()`, including cell width/height constraints, caption min/max handling, explicit table width/height content-area conversion, fixed-layout comparisons, and final min-floor checks. Collapsed-border policy branches remain explicit; only the local `blk->box_sizing` tests moved behind the shared helper. Verified with `make build`, `make check-int-cast`, `git diff --check`, and table smokes for `table_simple`, `table_border_collapse`, `table_width_algorithms`, `table_005_caption_header`, `table_colspan_test`, and WPT `collapsed-border-background-inset`.
  ✅ Partial (2026-07-12): routed block layout's direct border-box predicates through `layout_uses_border_box()`, covering auto-height min/max comparison, replaced SVG dimension resolution, float/explicit/parent-derived width paths, and explicit/auto height paths. The policy comments for intrinsic replaced dimensions remain local. Verified with `make build`, `make check-int-cast`, `git diff --check`, `max_height`, and replacement smokes for `min_max_percent_no_width_height`, `floats-009`, `svg_file_sizes_01`, and WPT `object-view-box-fit-fill-svg`; initially guessed min/max, float, and SVG fixture paths did not exist and were replaced by real fixtures.
  ✅ Partial (2026-07-12): routed intrinsic sizing's explicit width/minmax/height predicates and view export's textual/JSON box-sizing emission through `layout_uses_border_box()`, and delegated `layout_apply_min_max_width/height()`'s block predicate to the same helper. A repo scan now leaves only the enum-parameter comparisons inside `layout_css_size_to_content_box()` / `layout_css_size_to_border_box()`, where the helper cannot apply because the caller intentionally passes the CSS enum. Verified with `make build`, `make check-int-cast`, `git diff --check`, and intrinsic/view smokes for `grid_min_content_flex_single_item_margin_auto`, `flex_basis_flex_shrink_row`, `table_width_algorithms`, and `code_001_basic_blocks`.
  ✅ Partial (2026-07-12): routed the remaining flex/form/table-caption/layout-percentage CSS-size conversion call sites through `layout_box_sizing()` instead of passing `blk->box_sizing` directly, including flex item conversions via `view_as_block()`. The targeted layout subset now has no direct `blk->box_sizing` reads outside the helper itself and style-resolution assignment paths. Verified with `make build`, `make check-int-cast`, `git diff --check`, and layout smokes for `flex_basis_flex_shrink_row`, form input text, and `table_005_caption_header`.
- ✅ **Multiple containing-block resolvers** (C4): `layout.cpp:50`, `layout_positioned.cpp:512`, `view_pool.cpp:32`, `layout_multicol.cpp:662` — unify into `layout_containing_block.cpp`; verify abs vs fixed vs ICB nuances. ~80 LOC. **Implementation status (2026-07-11): done for view-side absolute/fixed containing-block lookup.** Promoted `find_initial_containing_view_block()` and `find_positioned_containing_block()` from the positioned layout path, routed `find_containing_block()`, view JSON/export absolute-position coordinate calculation, and multicol spanner anchoring through the shared resolver, and removed the `view_pool`/multicol local ancestor walks. The DOM-only `layout_positioned_containing_block()` helper in `layout.cpp` remains separate because it works on `DomElement` scroll coordinates rather than `ViewBlock` layout objects. Verified with `make build` plus focused absolute, fixed, multicol, and absolute max-height layout smokes; one initially guessed fixed fixture path did not exist and was replaced with real WPT fixed-position fixtures.
- ✅ **`clamp_non_negative`/local clamps** (C1): `layout_containing_block.cpp:9`, `render_filter.cpp:39`, and `view.hpp`'s own `clamp/sign/lerp` (clash acknowledged in `math_utils.h:4–5`) — reconcile with `lib_math` (see also §5.6). **Implementation status (2026-07-11): done.** `render_filter.cpp` already uses shared `clamp_byte()`/`clamp_unit()`, `view.hpp` now delegates its historical helpers to `lib_math`, and `layout_containing_block.cpp` no longer carries a local `clamp_non_negative()` wrapper. Verified clean build.
- **Child-iteration + skip loops**: 67 walks in `layout_block.cpp`, 52 in `layout_table.cpp`, 20 in `intrinsic_sizing.cpp`; `collect_and_prepare_flex_items` (`layout_flex.cpp:2101`) and `collect_grid_items` (`layout_grid.cpp:673`) are parallel — start with a shared `collect_in_flow_children()`.
  ✅ Partial (2026-07-12): added shared container-child helpers for suppressing ignorable whitespace text nodes and filtering hidden/display:none/out-of-flow positioned block items, then routed flex item skipping and both grid collection passes through them. This removes the duplicated flex/grid skip predicates without merging their different preparation/measurement workflows yet. Verified with `make build`, `make check-int-cast`, `git diff --check`, and layout comparisons for `flex_basis_flex_shrink_row`, `grid_basic_implicit_tracks`, `grid_auto_fill_fixed_size`, and `grid_min_content_flex_single_item_margin_auto`.
  ✅ Partial (2026-07-12): added `layout_text_node_has_content()` and routed flex measurement text-content probes through it, replacing repeated whitespace-only scans while preserving the grid multipass form-feed whitespace rule locally. Verified with `make build`, `make check-int-cast`, `git diff --check`, and layout comparisons for `flex_basis_flex_shrink_row`, `grid_auto_fill_fixed_size`, `grid_min_content_flex_single_item_margin_auto`, and `min_max_percent_no_width_height`.
  ✅ Partial (2026-07-12): added shared `layout_display_is_none()` / block / element helpers and routed the remaining obvious `display.outer == none || display.inner == none` skip predicates in flex multipass, flex in-flow height measurement, grid multipass measurement, table anonymous-run handling, and intrinsic sizing through them. The older visibility-aware `layout_block_is_hidden_or_display_none()` policy remains separate. Verified with `make build`, `make check-int-cast`, `git diff --check`, and layout comparisons for `grid_basic_implicit_tracks`, `grid_min_content_flex_single_item_margin_auto`, `flex_basis_flex_shrink_row`, `table_simple`, and `min_max_percent_no_width_height`.
  ✅ Partial (2026-07-12): routed the remaining layout-side raw `display.outer ==/!= none` skip predicates through the shared display-none helpers, covering flex item creation, flex intrinsic child measurement, grid item initialization, float/block prescans, root-child layout, and the visibility-aware block helper. A focused grep now leaves only `layout_display_is_none()`'s own outer/inner comparison. Verified with `make build`, `make check-int-cast`, `git diff --check`, and layout comparisons for `flex_basis_flex_shrink_row`, `grid_basic_implicit_tracks`, `floats-009`, `table_simple`, and `min_max_percent_no_width_height`; an attempted `inline_001_basic_text` fixture was not present, and `code_001_basic_blocks` still exposes its unrelated preformatted text-node baseline mismatch and was not counted.
  ✅ Partial (2026-07-12): routed the last doc-tracked raw `display.outer == none || display.inner == none` predicate in `grid_utils.cpp` through `layout_block_is_display_none()`. The remaining §4.8 child-iteration work is now the larger collection-shape question (`collect_in_flow_children()`), not skip-predicate cleanup. Verified with `make build`, `git diff --check`, `grid_min_content_flex_single_item_margin_auto`, and `grid_basic_implicit_tracks`.
- ✅ **Delete `.bak` files:** `layout_table.cpp.bak` (127 KB), `render_svg.cpp.bak`, `render_img.cpp.bak`. **Implementation status (2026-07-11): done.** Removed the untracked Radiant backup files named here.
- **Lower confidence, worth a focused diff:** `resolve_htm_style.cpp` (2172 lines) presentational-attribute mapping vs `resolve_css_style.cpp` length resolution.

  ✅ Audit note (2026-07-12): `resolve_htm_style.cpp` is not a second CSS cascade resolver; it owns UA defaults and HTML presentational hints (`parse_html_color()`, table/cell border hints, `dir=auto`, default display/style application), while `resolve_css_style.cpp` owns CSS declarations and cascade policy. The real overlap is smaller: repeated length/percentage attribute parsing for width/height-like hints, border/background/font property assignment, and box-size writes into `given_width`/`given_height`/percent fields. Start with presentational-hint applicator helpers for HTML length/percent attributes and shared property writers; do not merge the files.

---

## 5. Radiant Paint / Display-List / Editing (`radiant/` non-layout side)

Checked and NOT duplicated: view-tree render traversal is properly shared (`render_walk.cpp:240–374` via the `RenderBackend` vtable in `render_backend.h:23`); `rc_*` painter facade is thin delegation; `paint_ir` uses `lib/strbuf`; `graph_theme.cpp:29` already migrated to `lib/color.h` — good precedents.

### 5.1 `dl_replay_tile` is a near-complete clone of `dl_replay` — C3, HIGHEST
`tile_pool.cpp:526–1092` (~566 LOC) vs `display_list_replay.cpp:48–273` (~226 LOC): same item iteration, same skip/cull pre-pass, same backdrop stack + shadow-clip save buffer, same op-switch dispatching identical `rdt_fill_*` calls (compare gradient cases `display_list_replay.cpp:113–129` vs `tile_pool.cpp:652–674`). Tile re-inlines the backdrop stack (`tile_pool.cpp:531–538`) that `DisplayReplayBackdropStack` already encapsulates. ~350 LOC. **Fix:** one replay core parameterized by a coordinate/surface-target strategy (full-surface vs tile-local offset); reuse `DisplayReplayBackdropStack`/`DisplayReplayShadowClip`. Hot path — medium risk, high effort, high payoff.

✅ Partial (2026-07-12): extended `DisplayReplayBackdropStack` with tile-offset save, empty-stack push, and discard helpers, then routed tile replay's backdrop save/composite/blend handling through the shared stack used by full replay. Backdrop scratch cleanup now happens in the shared pop path. Verified with `make build`, `make check-int-cast`, `git diff --check`, and a forced-tile PNG smoke (`temp/cleanup_backdrop_tile.html`) whose normal and tiled decoded pixels match exactly; both render commands still emit the pre-existing debug-build temp memtrack shutdown diagnostic.

✅ Partial (2026-07-12): extended `DisplayReplayShadowClip` with tile-offset save/restore helpers and routed tile replay's shadow-clip save/restore cases through the same helper used by full replay. Verified with `make build`, `make check-int-cast`, `git diff --check`, the forced-tile backdrop smoke, and a forced-tile `box_shadow_01.html` render; normal and tiled decoded pixels match exactly, with only the known debug memtrack diagnostics.

✅ Partial (2026-07-12): extracted shared vector-op replay into `display_list_replay_vector.{hpp,cpp}` and routed both full replay and tile replay through it for fill/stroke path, rectangle, gradient, image, picture, and vector clip operations. Tile replay keeps duplicate-picture ownership and debug draw-count behavior through a helper mode flag. Verified with `make build`, `make check-int-cast`, `git diff --check`, forced-tile backdrop and `box_shadow_01.html` renders, and decoded PNG pixel comparisons; both normal/tiled pairs matched exactly, with only the known debug memtrack diagnostics.

✅ Partial (2026-07-12): moved tile replay's skipped-item state preservation for vector clips, backdrop save/blend/opacity balancing, and shadow-clip reset behind the shared replay helper families (`dl_replay_vector_clip_item()`, `dl_replay_backdrop_skip_item()`, `dl_replay_shadow_clip_discard()`). Tile replay now keeps the culling policy local while sharing the stack-maintenance actions with the full replay helpers. Verified with `make build`, `make check-int-cast`, `git diff --check`, forced-tile backdrop and `box_shadow_01.html` renders, and decoded PNG pixel comparisons; both normal/tiled pairs matched exactly, with only the known debug memtrack diagnostics.

✅ Partial (2026-07-12): added tile-offset variants for the shared replay effect helpers (`dl_replay_apply_filter_at_offset()`, `dl_replay_box_blur_region_at_offset()`, `dl_replay_box_blur_inset_at_offset()`, and `dl_replay_outer_shadow_at_offset()`) and routed tile replay's local filter/blur/shadow switch bodies through them. The shared clip helper preserves tile replay's original edge-specific clamp policy. Verified with `make build`, `make check-int-cast`, `git diff --check`, forced-tile `box_shadow_01.html` normal/tiled PNG byte parity, plus successful normal/tiled render exits for `test_filter.html`, `filter_drop_shadow_01.html`, and `box_shadow_02_text_filter.html`; the filter fixtures are not byte-identical under forced tiling and were not counted as parity proof.

✅ Partial (2026-07-12): moved tile-local direct-pixel raster replay for `DL_FILL_SURFACE_RECT`, `DL_BLIT_SURFACE_SCALED`, and `DL_WEBVIEW_LAYER_PLACEHOLDER` into offset-aware helpers beside the full-surface raster replay helpers. Tile replay now delegates fill/blit/webview raster clipping, clip-shape restoration, and tile-local coordinate translation to `display_list_replay_raster.cpp`, preserving the original asymmetric edge-clamp policy for tile clips. Verified with `make build`, `make check-int-cast`, `git diff --check`, and forced-tile normal/tiled PNG byte parity for `box_shadow_01.html`, `box_border_styles_01.html`, and `image_001_basic_layout.html`; render commands still emit the known debug memtrack shutdown diagnostics.

✅ Partial (2026-07-12): moved tile-local glyph replay into `dl_replay_draw_glyph_at_offset()` beside the full-surface glyph replay helper, removing the final glyph coordinate/clip wrapper from `tile_pool.cpp` and trimming stale direct-render includes. Verified with `make build`, `make check-int-cast`, `git diff --check`, and normal-vs-forced-tile PNG byte parity for `text_flow_701_liberation_sans_regular.html`; render commands still emit the known debug memtrack shutdown diagnostics.

### ✅ 5.2 Glyph rasterization cloned between tile and replay — C3, HIGH
`tile_pool.cpp:338–525` vs `display_list_replay_glyph.cpp:6–256`. `tile_blend_glyph_coverage_pixel` (357–386) is **byte-identical** to `dl_blend_glyph_coverage_pixel` (52–81); mono/gray/LCD samplers and transform/emoji/plain loops are the same logic with only coordinate translation differing. ~180 LOC. **Fix:** shared `glyph_raster.hpp` (sampler + blend); parameterize `draw_glyph` by a dst-pixel addressing lambda.

**Implementation status (2026-07-11): done.** Extended `glyph_sampling.hpp` with shared mono/gray/LCD coverage sampling, bilinear coverage sampling, coverage-alpha blending, plain coverage-bitmap drawing, color-emoji BGRA drawing, and transformed coverage-bitmap drawing. Removed the local `dl_*` and `tile_*` sampler/blender copies, the duplicated non-transformed grayscale/mono/LCD and color-emoji loops, the duplicated transformed glyph loops, and stale unreachable transformed replay code. The transformed helper now takes an explicit page-space surface origin, so full-surface replay and tile replay use the same inverse-sampled destination-bounds policy while preserving their different surface addressing. Verified with `make build`, `git diff --check`, a layout smoke for `text_flow_701_liberation_sans_regular.html`, normal-vs-forced-tile PNG render parity (`cmp -s`) for that text fixture, a small emoji entity fixture rendered in normal and forced-tile modes with byte-identical PNG output, and a text-only transformed glyph fixture rendered in normal and forced-tile modes with byte-identical PNG output. The render/layout fixtures still emit their pre-existing HTML5 recovery or temp memtrack diagnostics while exiting 0.

### 5.3 Paint-op boilerplate across 6 parallel op families — C3, HIGH (structural)
The same op set is enumerated independently in: `paint_ir.cpp`'s 4 giant switches (`paint_ir_validate:129`, `paint_ir_lower_raster_internal:952`, `paint_op_name:1364`, `paint_ir_lower_svg_unchecked:1530` — 108 `case PAINT_*` labels), the record mirror (`display_list_record_vector/raster/effects.cpp`), the replay dispatch, and the tile dispatch. Adding one primitive touches ~6 switch sites. **Fix:** X-macro / op-descriptor table (name, struct, bounds-fn, lower-raster-fn, lower-svg-fn) generating validate/name/lower uniformly. High effort, large maintainability win.

✅ Partial (2026-07-12): introduced `PAINT_OP_LIST` as the single source for `PaintOp` enum values and `paint_op_name()`, then changed the name helper from a generated switch into a generated indexed name table with a local bounds check. This removes the first drift-prone duplicate list while leaving validate/raster/SVG lowering switches explicit for later descriptor-table migration. Verified with `make build`, `make check-int-cast`, `git diff --check`, a PNG render smoke for `test/layout/data/basic/box_shadow_01.html`, and a simple SVG render smoke for `flex_direction_row_reverse.html`. A shadow SVG smoke still hit the known pre-existing raster-fallback lifetime UAF noted in §5.5, so it was not counted as a regression for this enum/name-only slice.

✅ Partial (2026-07-12): added a generated `PAINT_OP_COUNT` sentinel from `PAINT_OP_LIST`, tied the name table to it with a compile-time assertion, and made PaintIR validation/raster lowering reject or log impossible op values explicitly. This stops enum/list drift and corrupted commands from silently passing validation while keeping the stateful validate/raster/SVG lowering switches explicit. Verified with `make build`, `make check-int-cast`, `git diff --check`, a PNG render smoke for `box_shadow_01.html`, and an SVG render smoke for `flex_direction_row_reverse.html`; render commands still emit the known debug memtrack shutdown diagnostics.

### ✅ 5.4 lib reuse gaps — C1, quick wins
- ✅ **XML/SVG escaping hand-rolled** in `paint_ir.cpp:1205–1230`, `render_svg.cpp:497–501, 1590–1592`, `render_svg_inline.cpp:5157–5161`, plus inline blocks in `cmd_layout.cpp`, `state_store.cpp` — `escape_append` + `ESCAPE_RULES_XML_ATTR`/`HTML_TEXT` already exist (and `render_svg_inline` uses them in *some* spots). ~60 LOC. **Implementation status (2026-07-11): done.** Routed PaintIR SVG attr/text lowering, legacy SVG text/marker output, image-document attribute escaping, text-document HTML wrapping, and state-store HTML extraction through `escape_append()` with the existing XML-attr/HTML-text rule sets. Verified clean build plus focused SVG escape and broad HTML render smokes; the broad fixture still emits the known pre-existing JS prototype/memtrack diagnostics while exiting 0.
- ✅ **Hex color parsing** re-done in `render_svg_inline.cpp:647–710` — `lib/color.h:38 color_parse_hex` does exactly this and its own comment notes it was "hand-rolled in 4+ places." ~45 LOC. **Implementation status (2026-07-11): done.** Routed inline SVG `#rgb/#rgba/#rrggbb/#rrggbbaa` parsing through `color_parse_hex()` and removed the local `hex_digit()` parser. Verified clean build plus inline SVG hex-color and HTML render smokes.
- ✅ **Local min/max/clamp sets ×8** (`display_list_replay_backdrop.cpp:6–11`, `_effects.cpp:7–12`, `_shadow.cpp:5–10`, `display_list_record_vector.cpp:11–16`, `_raster.cpp:10–14`, `_effects.cpp:11–16`, `render_composite.cpp:6`, `render_filter.cpp:34`) — `LMB_MIN/MAX/CLAMP`, `clamp_byte`, `clamp_unit` exist. ~32 LOC. **Implementation status (2026-07-11): done.** Removed the local min/max helpers in display-list replay backdrop/effects/shadow and display-list record vector/raster/effects, routed composite clamps through `LMB_MIN`, and routed filter byte/unit clamps through shared `clamp_byte`/`clamp_unit` while preserving nearest-byte rounding. Verified clean build plus SVG render smokes.
- ✅ **3×3 matrix multiply** re-implemented in `transform.hpp:49–63` (lambda) + inline expansions in `render_state.cpp`, `render_pdf.cpp`, `render_painter.cpp`, `render_svg_inline.cpp`, `rdt_vector_tvg.cpp` — `rdt_matrix_multiply` (`rdt_vector.hpp:294`) exists. **Implementation status (2026-07-11): done for the live duplicate helpers.** Removed the local matrix multiply lambda in `transform.hpp` and the `render_state.cpp` wrapper; both now call `rdt_matrix_multiply()`. The other doc-named render/PDF/TVG sites already used the shared helper in the current tree. Verified clean build and transform render smokes; existing transform fixtures still emit pre-existing PaintIR transform-stack diagnostics while exiting 0.
- ✅ **Value-type growable arrays** (`display_list_storage.cpp:18–24`, `paint_ir.cpp:464–465`, `browsing_session.cpp:35–37`, `webview_manager.cpp:45`, `css_animation.cpp`) — one `mem_grow_array()` helper or typed `GrowVec<T>` with `MEM_CAT_*` tagging. **Implementation status (2026-07-11): done.** Added checked `lam::mem_grow_array()` and pool-backed `lam::pool_grow_array()` helpers in `lib/mem_grow.hpp`; routed DisplayList, PaintIR, browsing history, webview handle tracking, and CSS keyframe registry growth through them. Growth failure now leaves existing buffers intact, and webview handle creation unwinds platform handles if tracking cannot grow. Verified clean build plus animation layout/render and DisplayList/PaintIR effects render smokes; the effects smoke keeps the known SVG PaintIR fallback/memtrack diagnostics while exiting 0.

### 5.5 Shared-helper extractions — C2/C4
- ✅ **4-corner transform → bbox** duplicated (`display_list_record_vector.cpp:61–95`, `display_list_replay_glyph.cpp:95–122`, `transform.hpp:127–178`, tile glyph path) — add `rdt_matrix_transform_point/rect_bounds` to `rdt_vector.hpp`. ~90 LOC. **Implementation status (2026-07-11): done for the live affine bbox duplicates.** Added `rdt_matrix_transform_point()` and `rdt_matrix_transform_rect_bounds()` to `rdt_vector.hpp`, then routed vector record bounds and transformed glyph replay bounds through them. The 3D quad/perspective code in `transform.hpp` remains separate because it is not the same affine rect-bounds operation. Verified clean build plus transform and SVG render smokes; transform-path diagnostics are the same pre-existing PaintIR stack warnings.
- ✅ **Dirty-list bbox union ×3** (`display_list_replay_state.cpp:13–29`, `render_output.cpp:258–268`, `retained_display_list.cpp:493+`) — `dirty_tracker_bounding_box()`. ~45 LOC. **Implementation status (2026-07-11): done for the live duplicated union loops.** Added `dirty_tracker_bounds()` beside `dirty_has_regions()` and routed display-list replay dirty clipping plus render selective-clear dirty-union setup through it. The retained display-list path in the live tree performs per-dirty-rect intersection/source checks rather than computing a union, so it remains intentionally separate. Verified clean build plus focused SVG render smoke.
- ✅ **Surface-region clamp + row-memcpy backup ×3+** (`_backdrop.cpp:23–67`, `_shadow.cpp:18–48`, `_effects.cpp:39–77`, + tile equivalents) and the **identical clip-shape restore-mask loop** (`_effects.cpp:64–73` ≡ `_shadow.cpp:62–71`) — `surface_region_save()`/`surface_region_restore_masked()`. ~110 LOC. **Implementation status (2026-07-11): done.** Added `display_list_surface_region.hpp` with shared clamped region save/clear/masked-restore helpers and routed backdrop save, shadow clip save/restore, box-blur clip restore, and the corresponding tile-local replay paths through it. Verified clean build plus focused SVG/PNG effects renders and a blend SVG smoke; broader shadow/filter fixtures still expose pre-existing SVG raster-fallback lifetime UAFs outside this helper path.
- ✅ **`dl_default_*_clip()` + set-clipped-bounds triplicated** (`display_list_record_effects.cpp:6–50`, `_raster.cpp:5–36`, `_vector.cpp:6–9`) — shared `dl_bounds.hpp` + one `DL_UNBOUNDED_CLIP`. ~50 LOC. **Implementation status (2026-07-11): done.** Added shared display-list bounds helpers in `display_list_bounds.hpp`, including `DL_UNBOUNDED_CLIP`/`dl_unbounded_clip()` and one clipped-rect bounds setter used by raster/effects recording. Vector recording now shares the unbounded sentinel while preserving its transform/floor/pad-specific bounds logic. Verified clean build plus SVG, transform, and box render smokes; transform-path diagnostics are the same pre-existing PaintIR stack warnings noted above.
- ✅ **Bilinear sampling** duplicated (`display_list_replay_glyph.cpp:173–234` vs `render_raster.cpp`/`tile_pool.cpp` blits) — `bilinear_sample_rgba()`. ~50 LOC. **Implementation status (2026-07-11): done for color-glyph BGRA sampling.** Added `glyph_sampling.hpp` with `glyph_sample_bgra_bilinear()` and routed both full-surface glyph replay and tile glyph replay through it. The `render_raster.cpp` surface-image samplers remain separate because they operate on ImageSurface RGBA/wrap-vs-clamp semantics rather than color-glyph BGRA bitmap semantics. Verified clean build plus SVG and box render smokes; the box fixture keeps the known pre-existing PaintIR transform-stack diagnostics while exiting 0.
- ✅ **Blend-mode math** — extend the shared `render_composite_blend_pixel` (already used by backdrop replay) to the SVG/effects compositors (`render_svg_inline.cpp:232`, `render_effects.cpp:80`). **Implementation status (2026-07-11): already centralized in the live tree.** Verified that blend formulas now live only in `render_composite.cpp`; `render_effects.cpp`, `render_svg_inline.cpp`, and `render_painter.cpp` record/apply blend operations that replay through the display-list/composite path using `render_composite_blend_pixel()`.
- **Rect/point struct zoo** (C4): `Rect{x,y,w,h}` + `Bound{l,t,r,b}` (`view.hpp:340,345`), `RenderPixelBounds`, `DirtyRect`, `Point2D`, ad-hoc `int region[4]` arrays — consolidate on `Rect`/`Bound` + a named `IRect`, explicit converters.
  ✅ Partial (2026-07-12): replaced the display-list replay surface-region `int region[4]` convention with a named `IRect` and routed backdrop, shadow-clip, box-blur masked restore, and tile-local blur restore through typed `IRect*` helpers. This removes the x/y/w/h index contract from the active replay save/restore paths while leaving the broader Rect/Bound/DirtyRect consolidation for a later pass. Verified with `make build`, `make check-int-cast`, `git diff --check`, forced-tile backdrop and `box_shadow_01.html` renders, and decoded PNG pixel comparisons; both normal/tiled pairs matched exactly, with only the known debug memtrack diagnostics.
  ✅ Partial (2026-07-12): moved `IRect` into shared `render_rect.hpp` and removed the duplicate `RenderPixelBounds` struct from render geometry. `render_geometry_clip_to_pixel_bounds()` now returns the same pixel-rect type used by display-list surface-region save/restore, and background blend backdrop saving reads `.w/.h` from that shared type. Verified with `make build`, `make check-int-cast`, `git diff --check`, and `./lambda.exe render temp/cleanup_render_irect_blend.html -o temp/cleanup_render_irect_blend.png`; the render smoke produced the PNG and still emits the known debug temp memtrack shutdown diagnostics.

### ✅ 5.6 `view.hpp` math templates vs `lib_math` — C1/C2
`view.hpp:36–86` defines `max/min/abs/clamp/sign/lerp`; `lib/math_utils.h:15–33` defines the same and documents the clash. Alias one into the other. Widely included — medium risk.

**Implementation status (2026-07-11): done.** Added mixed-type numeric helpers to `lib_math` and turned Radiant's historical unqualified `view.hpp` helpers into thin delegates, preserving existing call sites while removing the second implementation body. Verified with a clean `make build`; only the pre-existing `event.cpp` function-pointer cast warnings remain.

### 5.7 Flags for dedicated follow-up passes
- **Two SVG serialization paths:** `paint_ir_lower_svg_unchecked` (`paint_ir.cpp:1489–1900+`, with its own cap/join name maps 1246–1260 and path visitor 1307) vs `render_svg.cpp` (81 KB) / `render_svg_inline.cpp` (235 KB). Decide whether `render_svg.cpp` can retire in favor of PaintIR lowering, or at least share the path visitor + name tables.
- **`event.cpp` (444 KB) vs `event_sim.cpp` (260 KB):** the simulator appears to reconstruct hit-testing/caret/selection logic the live dispatch already implements — have `event_sim` call the same low-level entry points. Needs its own audit.

✅ Audit note (2026-07-12): the SVG split is no longer a simple "two serializers" problem. `render_svg.cpp` now delegates the main page/display-list export through PaintIR SVG lowering, while `render_svg_inline.cpp` is primarily an inline-SVG input parser/renderer that lowers SVG content into PaintIR/display-list operations. Remaining duplicate surface is the smaller legacy direct page/export layer in `render_svg.cpp` (text, rounded-rect/path helpers, border/marker output) versus PaintIR's SVG path/name/escape helpers. Next slice: share SVG path serialization, stroke cap/join name maps, and marker/text escaping helpers between PaintIR lowering and the legacy page exporter; do not retire `render_svg_inline.cpp` as part of this item.

✅ Audit note (2026-07-12): `event_sim.cpp` already calls live dispatch/editing bridge entry points (`radiant_dispatch_event_sim_simple_event`, `radiant_dispatch_event_sim_select_change`, `radiant_dispatch_editing_text_drag_drop`), so the broad dispatch loop is not fully duplicated. The duplication is fixture-side target lookup, selector matching, range/hit-coordinate synthesis, and assertion/event parsing. Next slice: expose/share low-level target resolution or hit-test helpers so the simulator stops reconstructing event targets, while keeping fixture parsing and expected-result checks simulator-local.

---

## 6. Lambda Core ↔ JS Runtime (`lambda/js/`)

Already unified (the model to follow): libuv event loop (`lib/uv_loop.h`, both sides), module registry (`module_registry.cpp` with `lang` param), JSON **parsing** (`js_json_parse` → `parse_json_to_item_strict`), `lib/strbuf`, `lib/base64`+`lib/hex` in crypto encode paths, `js_https.cpp` as a thin 705-LOC delegating wrapper over js_http/js_tls (the anti-duplication exemplar). Number parsing (both `strtod`-based) and math builtins (both thin libc dispatch) are fine as-is.

### ✅ 6.1 `js_double_to_string` ≡ `lambda_double_to_shortest` — C4/C2, TOP PRIORITY
`lambda/js/js_runtime_value.cpp:333–470` vs `lambda/lambda-decimal.cpp:62–150`: the *same function* — identical shortest-round-trip search (`for prec 1..21 { snprintf("%.*e"); sscanf; roundtrip check }`), identical digit extraction, identical ES §7.1.12.1 case analysis. Only NaN/Inf spellings and JS-side buffer guards differ. Both on hot paths. ~135 LOC. **Fix:** extract `lib/dtoa.c` → `dbl_to_shortest_digits(double, char[32], int* k, int* e, bool* neg)` + two thin flavor formatters. Mechanical, low risk.

**Implementation status (2026-07-11): done.** Extracted the finite shortest-round-trip formatter into `lambda_finite_double_to_shortest()` and routed both Lambda numeric formatting and JS `Number::toString` through it. JS keeps its own `NaN`/`Infinity` spellings; Lambda keeps `nan`/`inf`.

### ✅ 6.2 Three hand-rolled base64 *decode* tables — C1, HIGH
`js_buffer.cpp:502–540`, `js_buffer.cpp:1630–1656` (base64url variant), `js_globals.cpp:14203–14290` (atob) — all three files `#include lib/base64.h` and use it for *encode*, but hand-roll decode with inline 256-entry tables, duplicating `base64_decode_variant` (`lib/base64.h:58`) that `js_crypto.cpp:1423` already uses correctly. ~90 LOC + 3 subtly-different padding/whitespace surfaces. Mechanical fix.

**Implementation status (2026-07-11): done.** `Buffer.from(..., "base64")`, `buf.write(..., "base64"/"base64url")`, and `atob()` now delegate decode work to `base64_decode_variant()` instead of maintaining local decode tables. `atob()` keeps its DOMException path while using the shared decoder for byte reconstruction.

### ✅ 6.3 Static helper copy-paste across files — C3, HIGH (trivial effort)
- `static Item make_string_item(...)` redefined in **22 files** (js_querystring, js_cssom, js_string_decoder, js_https, js_fs, js_util, js_net, js_crypto, js_fetch, js_http, js_path, js_os, js_globals, js_dns, js_zlib, js_readline, js_child_process, js_buffer, js_tls, js_url_module, js_events, js_stream). ~175 LOC.
- `make_js_undefined` ×21 files; `is_callable` ×8; plus scattered `is_missing_value`, `append_bytes`. ~95 LOC.
**Fix:** promote `js_make_string()`, `js_undefined()`, `js_is_callable()` to `js_runtime.h`.

**Implementation status (2026-07-11): done.** Promoted the common JS undefined/string/callable helpers to shared runtime declarations in `js_runtime.h` with definitions in `js_runtime_value.cpp`, retained compatibility wrappers for existing call sites, and removed the per-file static `make_string_item`, `make_js_undefined`, and `is_callable` definitions. Verified no local static copies remain, `make build` completes with only the known `event.cpp` cast warnings, `temp/cleanup_js_helpers_smoke.js` passes through `lambda.exe js`, and `test_js_coerce_gtest` passes 15/15.

### 6.4 MIR emission layer duplicated between transpilers — C4, HIGH (structural)
`transpile-mir.cpp:434–490` (`new_reg`, `emit_insn`, `emit_label`, `emit_null_item_reg`, `emit_call_0/1/2`, `ensure_import`) vs `js_mir_calls_boxing_types.cpp:168+`/`js_mir_internal.hpp:96–204` (`jm_new_reg`, `jm_emit`, `jm_call_0..6`, `jm_ensure_import`). `emit_call_1` and `jm_call_1` are line-for-line identical except the struct type and one profiling line. ~300–500 LOC. **Fix:** a `MirEmitter` base (MIR ctx, current func, reg counter, import cache) embedded by both `MirTranspiler` and `JsMirTranspiler`; AST lowering stays separate. Medium-high effort.

✅ Partial (2026-07-12): collapsed the JS MIR arity-wrapper bodies behind `jm_call_with_args()` and `jm_call_void_with_args()` in `js_mir_calls_boxing_types.cpp`. The public `jm_call_0..6` and `jm_call_void_0..5` signatures remain stable for existing lowering code, while import setup, profiling, result-register allocation, and `MIR_new_call_insn` emission now live in one return-call path and one void-call path. Verified with `make build`, `make check-int-cast`, `git diff --check`, and JS smokes for Date setter dispatch, class setter/write paths, `path.normalize`/`path.join`/`require` normalization, and constructor builtin registration.

✅ Partial (2026-07-12): mirrored the wrapper collapse on the core MIR-Direct side by routing `emit_call_0/1/2/3/4/6` and `emit_call_void_0/1/2/3` through `emit_call_with_args()` / `emit_call_void_with_args()` in `transpile-mir.cpp`. Existing lowering call sites keep their named arity wrappers, but fixed-arity proto argument setup and MIR call instruction assembly now have one return path and one void path. Verified with `make build`, `make check-int-cast`, `git diff --check`, and MIR smokes for mixed numerics, typed-array vector ops, scalar/map values, unboxed fields, typed params, sized numeric annotations, total equality, join/group/query clauses, and pipe sysfuncs.

✅ Partial (2026-07-12): added `lambda/mir_emitter_shared.hpp` with a shared numbered-register allocation helper and routed core `new_reg()` plus JS `jm_new_reg()` through it. Lambda MIR-Direct keeps its historical pointer/float32-to-i64 register allocation policy through an explicit `coerce_float32` flag, while JS preserves real `MIR_T_F` Float32 register allocation for typed-array lowering. Verified with `make build`, `make check-int-cast`, `git diff --check`, a JS `Float32Array` MIR smoke, and Lambda MIR smokes for mixed numeric operations and typed-array vector operations.

✅ Partial (2026-07-12): extended the shared MIR emitter helper with call-argument proto setup and generic MIR call-instruction assembly, then routed both core `emit_call_with_args()` and JS `jm_call_with_args()` / void-call variants through it. The core and JS import/profiling layers remain separate, but the duplicated `{proto, import, optional result, args...}` instruction construction and call-argument name tables now have one implementation. Verified with `make build`, `make check-int-cast`, `git diff --check`, a JS `Float32Array` MIR smoke, and Lambda MIR smokes for mixed numeric operations and typed-array vector operations.

✅ Partial (2026-07-12): added shared label creation and instruction/label append helpers to `mir_emitter_shared.hpp`, then routed core `new_label()` / `emit_insn()` / `emit_label()` and JS `jm_new_label()` / `jm_emit()` / `jm_emit_label()` through them. JS keeps its opcode histogram and null-label guard locally, but the backend-neutral MIR append operations now share one implementation. Verified with `make build`, `make check-int-cast`, `git diff --check`, a JS `Float32Array` MIR smoke, and a Lambda `mixed_numeric_ops.ls` MIR smoke.

✅ Partial (2026-07-12): moved import key/proto-name formatting plus MIR proto/import pair creation into `mir_emitter_shared.hpp`, then routed core `ensure_import()` and JS `jm_ensure_import()` through the shared helpers. Core keeps its historical name-only cache key, while JS keeps the signature-aware key needed for overloaded runtime helpers. Verified with `make build`, `make check-int-cast`, `git diff --check`, `mixed_numeric_ops.ls`, and `temp/cleanup_js_path_smoke.js`.

✅ Partial (2026-07-12): added shared immediate integer-constant emission in `mir_emitter_shared.hpp` and routed core `emit_null_item_reg()` plus JS `jm_emit_null()` / `jm_emit_undefined()` / `jm_emit_item_error()` through it. This removes another small MIR instruction-construction clone while keeping language-specific sentinel choices local. Verified with `make build`, `make check-int-cast`, `git diff --check`, Lambda MIR smokes for `mixed_numeric_ops.ls`, `value.ls`, and `unboxed_field_access.ls`, plus JS MIR smokes for `temp/cleanup_mir_reg_smoke.js` and `temp/cleanup_js_immediate_consts.js`.

### ✅ 6.5 Date civil-calendar math — C4/C2 (+ perf bug in lib)
`js_globals.cpp:1637–1644` implements the O(1) Hinnant `days_from_civil` (146097-era); `lib/datetime.c:64–86 days_since_epoch` is a **naive O(year) loop**; JS also hand-rolls `MakeDay/MakeTime/MakeDate` (1582–1610) paralleling `datetime_to_unix` (`lib/datetime.c:89`). ~60 LOC. **Fix:** move Hinnant `days_from_civil`/`civil_from_days` into `lib/datetime.c` (or `lib/calendar.h`); both consume it. Shared unit is the integer day/second math, not the struct (JS uses raw doubles).

**Implementation status (2026-07-11): done.** Added shared `datetime_days_from_civil()` in `lib/datetime` and routed `datetime_to_unix()` plus JS Date construction through it. This removes JS's private copy and fixes Lambda's pre-1970 day calculation, where the old forward-only loop produced no negative day offset. Verified clean build, `test_datetime_gtest`, JS `Date.UTC()`/`toISOString()` pre-epoch smokes, and a Lambda pre-epoch datetime script.

### 6.6 Other unification candidates
- ✅ **JSON stringify** (parse is already shared): `js_globals.cpp:13345–13860` duplicates the string escaper + number path of `format-json.cpp`. The tree walk can't merge (toJSON/replacer semantics) but share `json_escape_string(StrBuf*,...)` + the §6.1 dtoa. ~60 LOC. **Implementation status (2026-07-11): done for shared string escaping.** Added `escape_append_json_string()`/`escape_append_json_stringbuf()` in `lib/escape`, routed Lambda JSON formatting and JS `JSON.stringify` through them, and kept JS's well-formed lone-surrogate escaping behind an explicit flag. Verified `make build`, `make build-test`, rebuilt `test_stringbuf_gtest`/`test_strbuf_gtest` escape filters, JS stringify surrogate/control smokes, Lambda JSON escape smokes, and `json_empty_key.ls`.
- **RE2 wrapper glue:** `re2_wrapper.cpp` (802 LOC) vs `js_regex_wrapper.cpp` (2862) + `js_bt_regex.cpp` (1043) — syntax translation legitimately differs, but compile+cache+options+capture-extraction lifecycle is duplicated. Extract `lib/re2_glue`. ~150–250 LOC.

  ✅ Partial (2026-07-12): added `lib/re2_glue.hpp` and routed the shared RE2 default-option setup, compile failure logging/error-copy ownership, release, and Lambda find/replace match-window selection through it. `re2_wrapper.cpp` now uses the helper for pattern AST compile, the C-ABI `re2_compile()`/`re2_release()` boundary, cached unanchored patterns, and option-specific temporary regexes; `js_regex_wrapper.cpp` uses it for lookbehind/assertion helper regexes, main wrapper compile, refined/retry regexes, and compiled-filter cleanup. Syntax rewriting, JS capture remap/filter lifecycle, backtracking fallback, and Lambda pattern cache semantics remain wrapper-local. Verified with `make build`, `git diff --check`, Lambda regex smokes (`string_pattern_ops.ls`, `find_replace_options.ls`, `type_pattern.ls`), and JS regex smokes (`regex_lookahead.js`, `regex_backrefs.js`, `regex_lookbehind.js`, `regex_neg_lookahead.js`, `regex_advanced.js`).
- ✅ **Percent-encoding ×4:** `js_querystring.cpp:88–181`, `js_globals.cpp:14486–14560` (encodeURI*), `js_url_module.cpp`, lowering in `js_mir_expression_lowering.cpp`, plus `lib/url.c`'s own codec — one `url_percent_encode(out,in,len,keep[256])`/`decode` in `lib/url.h`; callers supply keep-sets. ~80 LOC. **Implementation status (2026-07-11): mostly done in the live tree.** `encodeURI`/`encodeURIComponent` and their decode counterparts now delegate their non-fast paths to `lib/url` (`url_encode_uri`, `url_encode_component`, `url_decode_uri`, `url_decode_component`), and URL file-path construction uses `url_from_local_path()`. `querystring.unescape()` also uses `url_decode_component()`. `querystring.escape()` intentionally remains local because Node's historical `querystring.escape()` combines high surrogates with the next code unit instead of throwing like `encodeURIComponent`; merging it would require a Node-flavored encoder policy rather than the ECMAScript URI helper. Verified by source audit during the cleanup pass; no code change needed for the already-shared paths.
- ✅ **UTF-16 surrogate math open-coded** (`js_buffer.cpp:283–284, 620–624, 950–954`, `js_globals.cpp:162, 5569–5601, 5670–5677`, js_dom/js_formdata/js_cssom/js_string_decoder) — `lib/utf.h` has `utf16_decode_pair/encode`, `utf8_encode/decode`, `utf_is_surrogate`. The *string storage model* (UTF-16-semantic JS vs UTF-8 lib) can't unify, but these per-codepoint conversions can. ~60–120 LOC. **Implementation status (2026-07-11): done for the core JS/runtime pair math.** Added `utf_is_high_surrogate()` and `utf_is_low_surrogate()` to `lib/utf.h`, routed `js_buffer.cpp`, `js_globals.cpp`, `js_bt_regex.cpp`, and the touched `js_runtime.cpp` string/regex/TextEncoder/TextDecoder paths through `utf16_decode_pair()`/`utf16_encode()`, and added lib assertions for the high/low split. Follow-up audit found `js_string_decoder.cpp` no longer has surrogate pair math, while FormData's remaining WTF-8 byte-pattern replacement is a deliberate HTML form-submission policy rather than a UTF-16 pair conversion. Verified with `make build`, `temp/cleanup_utf16_smoke.js`, rebuilt `test_str_gtest`, and `UtfCodecTest.*` (42 tests).
- **`js_path` normalize/join** (`js_path.cpp:71–260`) is pure string path manipulation (distinct from `lambda/path.c`'s Url/Target abstraction); `js_require` partially re-derives it (`js_mir_entrypoints_require.cpp:508–524, 1305–1432`) — extract `lib/path_str.c`. ~100 LOC.
  ✅ Partial (2026-07-12): extracted shared path-string helpers into `lib/path_str.h` for separator normalization, bounded copy/join, POSIX lexical `.`/`..` normalization, POSIX and Win32 absolute checks, POSIX and Win32 dirname/basename/extname span discovery, and small Win32 separator/drive-letter predicates. `path.normalize()`/`path.join()` now route through the shared lexical normalizer with Node's empty-relative `"."` behavior, `path.resolve()` shares bounded POSIX join/absolute handling before its filesystem `realpath()` policy, CommonJS `require()` uses the same lexical normalizer with its existing empty-relative policy, POSIX `path.parse()` and POSIX/Win32 `path.dirname()`/`path.basename()`/`path.extname()` share directory/base/extension scans while keeping JS return-item policy local, and Win32 path routines share the basic lexical predicates. Verified with `make build`, `make check-int-cast`, `git diff --check`, and `temp/cleanup_js_path_smoke.js` covering `path.normalize`, `path.join`, `path.resolve`, `path.parse`, POSIX and Win32 `dirname`/`basename`/`extname`, Win32 `isAbsolute`, and `require("./dir/../dir/./value.js")`.
  ✅ Partial (2026-07-12): moved POSIX relative-path string construction into `path_str_relative_posix()`, leaving `js_path_relative()` responsible only for argument validation and its existing `file_realpath()` fallback policy. `path.win32.relative()` continues to reuse the POSIX result and convert separators. Verified with `make build`, `make check-int-cast`, `git diff --check`, and `./lambda.exe js temp/cleanup_js_path_smoke.js` covering same-path, sibling, child, Win32 separator conversion, and the existing require-normalization cases; an initial bare `./lambda.exe temp/cleanup_js_path_smoke.js` run hit the Lambda parser and was not counted.
  ✅ Partial (2026-07-12): moved Win32 lexical normalization into `path_str_normalize_lexical_win32()`, removing the local segment-stack implementation from `js_path_win32_normalize()` while preserving drive/root/UNC-ish prefix handling and `.` fallback for empty relative results. Verified with `make build`, `make check-int-cast`, `git diff --check`, and `./lambda.exe js temp/cleanup_js_path_smoke.js` covering drive, UNC-ish, and relative Win32 normalization alongside the existing path/require cases.
  ✅ Partial (2026-07-12): routed the remaining `path.join()` segment append loop through `path_str_copy()` / `path_str_join_posix_into()` instead of locally redoing bounded copy, separator insertion, and truncation math. Verified with `make build`, `make check-int-cast`, `git diff --check`, and `./lambda.exe js temp/cleanup_js_path_smoke.js`.
- **Builtin registration fragility (internal):** flat name/arity/id tables (`js_runtime_builtin_registry.cpp:150+`) + giant `switch(builtin_id)` (`js_runtime.cpp:11421+`) + `strncmp` chains in `js_mir_expression_lowering.cpp` (e.g. 2720–2721, 9735–9760) — adding a builtin means editing ≥3 places. X-macro/codegen table.
  ✅ Partial (2026-07-12): collapsed the constructor-name dispatch ladders in `js_runtime_builtin_registry.cpp` into static constructor/spec maps for static methods, prototype methods, prototype accessors, plus a small constructor-name table for `Symbol.species` installation. The legacy method lookup now also reuses the Object.prototype spec table for unconditional Object methods while leaving receiver-sensitive `toString`/`valueOf` policy explicit behind named receiver-policy helpers, and prototype-name enumeration uses a TypeId/spec table instead of a local predicate chain. This does not yet solve the cross-file builtin-id/codegen issue, but it removes the local `strncmp` ladders and TypeId run-on predicates from the registry entry path. Verified with `make build`, `make check-int-cast`, `git diff --check`, and `temp/cleanup_builtin_registry_smoke.js` covering constructor statics, prototype methods/accessors, Object.prototype lookup, receiver-sensitive prototype lookup, prototype-name enumeration, and `Array[Symbol.species]`.
- ✅ **`util.inspect` vs `print.cpp`:** parallel Item-graph debug printers (cycle detection, depth limits) — formats genuinely differ (Node-style); architectural note only, don't force-merge. **Implementation status (2026-07-11): no merge by design.** Source audit confirms `print.cpp` is Lambda's native internal/debug printer with a very high recursion guard and Lambda syntax, while `js_util_inspect()` is Node-observable API surface with `util.inspect.custom`, `showHidden`, `colors`, configurable depth, constructor-specific renderers, AssertionError special handling, and `[Circular]` detection. Keep only low-level scalar helpers shared where semantics already align; do not route either printer through the other.

## Tooling Cleanup Notes

- ✅ **Dead-code lint script empty-output failure:** `utils/lint/dead-code/run_unused_function.sh` could reach its final sort step without materializing `defs_full.tsv` when ast-grep produced no candidate definitions, causing a misleading "No such file or directory" failure on an otherwise empty result set. The script now materializes the expected intermediate files before the final dedupe/sort step. Verified with `utils/lint/dead-code/run_unused_function.sh`.

---

## 7. Proposed New / Extended `lib/` Modules

| Module | Contents | Consumers |
|--------|----------|-----------|
| `lib/dtoa.{h,c}` | `dbl_to_shortest_digits()` + Lambda/ES flavor formatters | lambda-decimal, print.cpp, format-json/number, js_runtime_value, js JSON.stringify |
| `lib/datetime.c` (extend) | Hinnant `days_from_civil`/`civil_from_days`; fix O(year) `days_since_epoch` | lib/datetime itself, js Date |
| `lib/url.{h,c}` (extend) | `url_percent_encode/decode` with keep-set param | js_querystring, encodeURI*, js_url_module, lib/url |
| `lib/path_str.{h,c}` | normalize/join/dirname/basename/extname (POSIX+Win) | js_path, js_require |
| `lib/re2_glue` | RE2 compile+cache+options+capture extraction | re2_wrapper, js_regex_wrapper |
| `lib/escape.h` (extend) | Markdown/RST/Wiki/DOT/Mermaid/D2 `EscapeRule[]` tables | format-utils, format-graph, retire two bespoke escapers |
| `lib/utf` (extend) | utf8proc normalization wrappers from `utf_string.cpp` | lambda core, potential js use |
| `lib/` or `lambda/text/` | `html_entities` (WHATWG table + reverse map) | input, css/dom, html5 tokenizer, formatters |
| `lib/math_utils.h` (extend) | `nonneg()`; reconcile `view.hpp` clamp/sign/lerp clash | radiant layout + paint |

Radiant-internal (not lib, but new shared homes): `layout_percentages.cpp`, `glyph_raster.hpp`, `dl_bounds.hpp`, `surface_region_save/restore`, paint-op X-macro descriptor table, `rdt_matrix_transform_point/rect_bounds` in `rdt_vector.hpp`.

---

## 8. Suggested Sequencing

**Phase 0 — zero-risk deletions (hours)**
Delete `.bak` files (`layout_table.cpp.bak`, `render_svg.cpp.bak`, `render_img.cpp.bak`, `mark_editor.cpp.bak`, `runner.cpp.bak`); delete dead `input-utils` helpers (§2.5) and dead `markdown/wiki/rst_escape` (§3.6); fix the unreachable `LMD_TYPE_ARRAY` branches in `lambda-eval.cpp` (§1.7 — possible latent bug); delete the two validator TypeId→name copies (§3.4).

**Phase 1 — mechanical low-risk wins (days)**
`lib/dtoa` extraction (§6.1); base64 decode tables (§6.2); js static-helper dedup (§6.3); move 5 transpiler analysis helpers to `transpile_shared.cpp` (§1.1); `arraynum_read_item` (§1.3); `utf_string.cpp` collapse (§1.6); `createFloat` adoption + `str_rtrim`/ctype fixes in input (§2.5, 2.6); radiant lib-reuse batch (escape/color/clamp/matrix, §5.4); `is_out_of_flow`/`is_floated` canonicalization (§4.3); formatter pool-ceremony helper + datetime helper (§3.5, 3.8).

**Phase 2 — mechanical but broad migrations (1–2 weeks, needs layout re-baselining)**
Radiant box-metrics + min/max-clamp + box-sizing migration (§4.1, 4.2, 4.8); resolve the INI/Properties triple (§2.1); XML escaping onto lib (§3.3); number formatting through `format_number` (§3.5); percent-encoding + UTF-16 primitives in js (§6.6).

**Phase 3 — shared-module extractions (weeks, deliberate)**
`ElementReader::attrs()` + formatter dispatch visitor (§3.1, 3.2) + map-field iterator in core (§1.4); quoted-string/escape unification in input (§2.2, 2.3); `layout_percentages.cpp` + baseline walker + flex→layout_alignment (§4.4–4.7); glyph raster + surface-region helpers (§5.2, 5.5); `lib/calendar` + JSON-stringify escaper (§6.5, 6.6).

**Phase 4 — structural (dedicated efforts, own design pass each)**
`MirEmitter` shared base (§6.4); replay-core unification `dl_replay`/`dl_replay_tile` (§5.1); paint-op X-macro table (§5.3); scalar-type predicate canonicalization (§1.5); builtin-table codegen (§6.6); the two flagged deep audits — SVG dual path (§5.7) and `event.cpp` vs `event_sim.cpp` (§5.7).

**Testing gates per phase:** `make test-lambda-baseline` must stay 100% for Phases with lambda-engine changes; `make test-radiant-baseline` + `make layout suite=baseline` for radiant changes; node baseline (`make node-baseline`) should not regress for lambda/js changes. Phase 2's radiant migrations and §4.4's int→float spacing change may shift sub-pixel output — re-baseline deliberately, one engine at a time.
