# Lambda Impl Plan: Structural Type Inference (Decided-but-Unbuilt Backlog)

- **Date:** 2026-08-18
- **Status:** IP0–IP3 IMPLEMENTED (2026-08-18); IP5 ATTEMPTED — §10 records what it proved and why TIG1 needs a bounded sweep rather than more special cases. IP4/IP6/IP7 pending.
- **Design authority:** `vibe/Lambda_Design_Type_Infer.md` (TI1–TI8, TIG1–TIG17); this doc implements only its *decided* items and never resolves an open design question in code — where an open question gates a slice, the interim disposition is stated and the slice stays inside it
- **Formal authority:** D2.4.1, D2.5.3, D3.2.1, D3.2.3, D3.3.1v2–D3.3.4, D8.6.1–D8.6.3; S4.1.1–S4.1.2, S5.5.2, S7.1, S7.2.1, S11.4.1–S11.4.4; SI3v2, SI14
- **Related:** `vibe/Lambda_Impl_Tune19.md` (T19-1 ValueRep — the representation twin of IP5), `vibe/Lambda_Design_Compiling_Lane.md`, `doc/Lambda_Formal_Design.md` §D3.3
- **ID series:** `IP#` (impl phases), sub-items `IP#.#`

## 0. Ground truth this plan builds on (verified 2026-08-18)

- The union/contract kernel already exists and is tested:
  `lambda/runtime/type_contract.cpp` — `lambda_type_union_normalized`,
  `lambda_type_remove_error`, `lambda_type_remove_error_and_null`,
  `lambda_type_has_proven_error`, `TYPE_ANY_NO_ERROR` family
  (`TypeContractMetadataTest` suite green).
- The registry `success_type`/`may_return_error` tail WORKS end to end: 6 of
  228 rows carry it today (`int`→`number|error`, `int64`, `float`, `decimal`,
  `binary`, `parse_html_fragment`), `sys_func_call_result_type`
  (`build_ast.cpp:~840`) builds the union, and `--emit-ast-dump` asserts it
  (`(AST_NODE_SYS_FUNC (name "int") (success_type "number") (may_return_error
  true))`). TIG4 is therefore *row data entry + result-kind rules*, not new
  machinery.
- `--emit-ast-dump` (`lambda/runtime/emit_sexpr.cpp`, `main.cpp:4751`) is the
  established vehicle for asserting static types in tests — every phase below
  uses it instead of inventing a new probe.
- Strictness policy is live: Lambda strict by default, `--static-warning`
  relaxed mode (warnings + run + diagnosed-contract-drops-to-inferred), JS
  warn-only planned. E208 fires straightaway — **each phase that makes new
  error components visible must fix the corpus scripts it rejects in the same
  change** (TI6, no staging).
- The E208 checker keys on `lambda_type_has_proven_error(body->type)` and the
  implicit `any \ error` return firewall (S11.4.2/S11.4.3), with
  `lambda_type_remove_error`-based discharge for `or`/`^`/handlers. Every
  union-producing slice below interacts with it; the per-slice corpus gate is
  how that interaction stays controlled.

Standing gates for every phase (house rules): `make test-lambda-baseline`
at its current pass set (the 11 pre-existing conc/proc E221 failures are the
known baseline; nothing new may fail) and `make test262-baseline` fully green;
SI3v2 differential (`python3 test/interp/tier_sweep.py` mismatch count not
above its current value); emission ratchet not growing (D8.6.1); release
paired A/B on the named benchmark rows when a phase claims a perf effect;
no benchmark-source edits to absorb a compiler gap.

---

## IP0 — ANY-census instrumentation (TI3) — prerequisite, no behavior change

**Goal:** every ANY assignment in both builders names its reason; a per-reason
counter makes "where does ANY come from" a report, and later phases prove
their effect by census delta.

1. **IP0.1** Add `AnyReason` enum to `lambda/runtime/ast-core.hpp`:
   `ANY_OPEN_PARAM, ANY_OPEN_MAP, ANY_DYNAMIC_NAME, ANY_EXPLICIT, ANY_SYSFUNC_ROW,
   ANY_INDEX_ELEM (TIG1), ANY_MEMBER_SHAPE (TIG2), ANY_JOIN (TIG7/8),
   ANY_LOGICAL_AND (TIG5), ANY_COMPARE (TIG6), ANY_LIST (TIG11),
   ANY_UNARY (TIG12), ANY_LOOP_SRC (TIG10), ANY_DECOMPOSE (TIG15),
   ANY_PIPE (TIG16), ANY_JS_BINARY (TIG13), ANY_JS_CALL_MEMBER (TIG14),
   ANY_LEGACY_UNCLASSIFIED` (the last one must trend to zero).
2. **IP0.2** `tp->any_census[ANY_REASON_COUNT]` on Transpiler (and the JS
   transpiler); a `set_type_any(tp, node, reason)` helper replaces bare
   `node->type = &TYPE_ANY` at every site — mechanical sweep of the ~90
   assignment sites in `build_ast.cpp` and ~40 in `build_js_ast.cpp`,
   classified per the TIG table. Grep-ratchet: a CI check (extend
   `make lint`) forbids new bare `= &TYPE_ANY;` in the two builders.
3. **IP0.3** Emit the census: one `log_info("any_census: …")` line per
   compile, plus a `(any_census (reason n)…)` block in `--emit-ast-dump`.
   Baseline capture: run the 59-benchmark corpus + `test/lambda` and check in
   `temp/`→`test/type_infer/any_census_baseline.tsv` (committed under
   `test/`, regenerated by `test/type_infer/refresh_census.sh`).

**Acceptance:** census identical AST/behavior (dump diff empty except census
block); baseline file checked in; lint rule active.
**Effort:** ~2 days. **Risk:** none (pure bookkeeping).

---

## IP1 — cheap precision one-liners (TIG5, TIG6, TIG11, TIG12, TIG16)

All in `build_ast.cpp`; each is a localized type computation with the kernel
that already exists. These may surface E208s — run the corpus sweep per item
and fix rejected scripts in-change.

1. **IP1.1 TIG5 `and`** (`:~5325`): mirror the `or` branch —
   `lambda_type_union_normalized(pool, remove_error_and_null(left), right)`?
   No: `and` returns the *left* when falsy, else right; falsy left values are
   `null/false/error/""` (SI9) — the sound cheap join is
   `union(left, right)` normalized, minus nothing (left CAN flow through).
   Implement as plain normalized union of both operand types; refine later
   only if census shows it matters.
2. **IP1.2 TIG6 comparisons** (`:~5348`): `< <= > >=` type as `bool` for
   every `known_magnitude_comparable_type_set` pair (string/string,
   dtime/dtime, numeric/numeric — helper already exists at `:~4815`);
   `bool | error` when either side is ANY/unproven-mixed; keep ANY only for
   the null-typed operand case pending SO-level ruling on null comparison.
3. **IP1.3 TIG11 list/content** (`:5817`, `:10581`): stop discarding the
   built `TypeList` — set `ast_node->type = (Type*)ast_node->list_type` and
   teach the two consumers that asserted "returns Item, not List" (grep for
   the comment; the boxed representation is unchanged, only the static type
   is retained — D3.3.4 representation follows contract, not vice versa).
4. **IP1.4 TIG12 unary +/-** (`:4715`): non-numeric operand types as
   `number | error`; `T?` numeric operand as `T | error`.
5. **IP1.5 TIG16 pipe with `~`** (`:5378`): result is `TypeArray` with
   `nested = right-side expression type` instead of bare `LMD_TYPE_ARRAY`.

**Fixtures:** one `--emit-ast-dump` gtest per item in
`test_lambda_errors_gtest.cpp` (pattern: `OrNarrowingRetainsTheCleanInternalTop`);
negative twins assert the surviving ANY on genuinely-open operands.
**Acceptance:** census drops in ANY_LOGICAL_AND/ANY_COMPARE/ANY_LIST/
ANY_UNARY/ANY_PIPE; corpus fixed in-change; no benchmark row regresses >5%.
**Effort:** ~3 days.

---

## IP2 — sys-func registry precision (TIG4/TI4) — data entry at scale

**Mechanism exists; this is ~155 rows + a result-kind column.**

1. **IP2.1** Add `SysFuncResultKind` to `sys_func_registry.h`:
   `RESULT_FIXED (default), RESULT_SAME_AS_ARG0, RESULT_ELEM_OF_ARG0,
   RESULT_ARRAY_OF_ARG0_ELEM, RESULT_ARG0_NUMERIC_WIDEN`. One new column;
   `sys_func_success_result_type` (`build_ast.cpp:~796`) consumes it —
   the floor/ceil special-case switch collapses into `RESULT_SAME_AS_ARG0`
   rows (dedup rule 13).
2. **IP2.2** Row sweep, in cohorts with a corpus run between each:
   - **math scalars** (~40 rows: sqrt/sin/…/pow/hypot): success `float`
     (`abs`/`min`/`max`/`sign` = `RESULT_SAME_AS_ARG0`); domain-error funcs
     (sqrt/log/asin/acos/atanh…) get `may_return_error=true` **only when the
     argument is not a proven non-negative/in-domain literal — NOT
     expressible per-row**, so per the open-question firewall: these rows
     ship `success=float, may_return_error=false` and S4.2 poison (`nan`)
     carries the failure, which is the current runtime semantics — no new
     E208 exposure. Explicitly documented per row.
   - **string funcs** (~20: upper/lower/trim*/replace/…): `string`;
     `split` → array-of-string (needs one static `TypeArray` instance —
     heed the registry's `TYPE_LIST` ASan note: allocate a real
     `TypeArray` global, never the bare singleton).
   - **collection ops** (~30: slice/reverse/sort/unique/take/drop/set):
     `RESULT_SAME_AS_ARG0`; `min1/max1/sum/avg` → `RESULT_ARG0_NUMERIC_WIDEN`
     with `may_return_error=true` (empty/heterogeneous input).
   - **IO/parse** (~15: parse/input/fetch/cmd/output): keep success `any`
     where genuinely dynamic but set `may_return_error=true` (can_raise
     already true — the union finally becomes visible). **This is the cohort
     with real E208 exposure**; budget the corpus fix here.
   - **matrix/image** (~40): element-preserving kinds; low priority, last.
3. **IP2.3** Refresh the AST-dump metadata test with 5–6 representative new
   rows; census counter ANY_SYSFUNC_ROW target: <30 remaining (the genuinely
   dynamic rows), each carrying a row comment naming why.

**Acceptance:** census ANY_SYSFUNC_ROW from ~160 → <30; corpus green
in-change per cohort; `make test-lambda-baseline` unchanged.
**Effort:** ~4 days (cohort-gated). **Risk:** E208 exposure on the IO cohort —
contained by cohort-at-a-time landing.

---

## IP3 — indexed reads, member chains, destructure (TIG1, TIG2, TIG15)

The highest-leverage phase: kills the ANY that causes T19-C double-lowered
compares and dead chained access.

1. **IP3.1 TIG1** `build_field_expr` (`build_ast.cpp:~2600`): for
   `ARRAY`/`ARRAY_NUM` objects with a known `TypeArray.nested`, type the read
   `lambda_type_nullable_normalized(pool, nested)` — same call the
   declared-destination path already makes at `:2591`. Public type stays
   `T?` (D2.5.3 unchanged); ANY remains only when `nested` is null/unknown
   (census ANY_INDEX_ELEM).
2. **IP3.2 TIG2** map/element field reads (`:2643`): return the shape's
   recorded field `Type*` for ALL field types, not just scalar — delete the
   `is_native_numeric_type_id(rid) || bool || string` filter; container
   results reuse the field's `Type*` directly (it lives in the shape pool —
   no clone). The old filter existed for unbox-function coverage: consumers
   that can't unbox a container read the type but keep boxed representation
   (D3.3.4) — audit the three call sites of the resolved type before
   deleting the filter.
3. **IP3.3 TIG15** decompose (`:6167`): project the source type through the
   pattern — array source: element type per position (`item_patterns` when
   lengths known, else `nested?`); map source: shape field lookup per name;
   fallback ANY_DECOMPOSE.
4. **IP3.4** MIR consumption check (ties to Tune19 T19-2/T19-1 but is
   verifiable now): with TIG1 landed, the int-compare on `int[]` reads must
   stop lowering through double. `mir-check` fixture asserting no
   `lambda_int_lane_to_double_c` in a `bxv[j] < 0` body; bounce2/fannkuch2
   A/B re-timed (categorical-bar rows from Result32 §2).

**Acceptance:** census ANY_INDEX_ELEM + ANY_MEMBER_SHAPE + ANY_DECOMPOSE
drop ≥80% on the corpus; bounce typed ≤0.35 ms (from 0.805), fannkuch typed
≤0.40 ms (from 0.774) on paired A/B vs `lambda-v32-a6192c1086`; `a.b.c`
chained-access dump fixture shows typed intermediate nodes.
**Effort:** ~4 days. **Risk:** shape-pool `Type*` aliasing across contexts —
reuse without clone is safe only for pool-interned shapes (D3.4.3); the
non-interned runtime-map path must clone or stay ANY.

---

## IP4 — stricter-of binding (TI7)

`build_assign_expr` (`build_ast.cpp:~6115`) and the loop-variable path
(`:8322+`).

1. **IP4.1** Compatible annotation + literal-known init: binding's effective
   type for *reads* is the meet — record it beside the declaration
   (`declared_type` keeps the contract per D3.2.3; add
   `effective_type` on AstNamedNode, defaulting to declared). `let` always
   meets; `var` meets only when the body never reassigns the binding
   (reuse the existing reassignment scan that feeds `param_reassigned`).
2. **IP4.2** Incompatible fully-known scalar pairs already reject
   (`check_declaration_static_boundary`); extend the static reject to
   fully-known container-literal element mismatches (`let a: int[] =
   ["x"]`) — currently deferred to runtime. In `--static-warning` mode the
   established drop-to-inferred recovery applies unchanged.
3. **IP4.3** For-loop key/value annotations meet the source element type from
   IP3's now-precise loop-source typing (TIG10's structural answer rides in
   here: loop var over `TypeArray` source takes `nested`, over shaped map
   takes the value union, LOOP_KEY_ALL keys take `int | symbol`).
4. **IP4.4** Consumers read `effective_type` through one accessor
   (`binding_read_type(node)`) — identifier resolution (`:3508`) is the
   single choke point; do not fan the meet logic into consumers.

**Acceptance:** dump fixtures: `let a: number = 1` reads as `int`,
writes/boundary still check `number`; corpus sweep for newly-rejected
programs reviewed in-change; no census regression.
**Effort:** ~3 days. **Risk:** meet must never *widen* a proven literal type
(is_literal preservation); `var`+closure-capture reassignment via captured
frames must count as reassignment — reuse the capture scan, don't rescan.

---

## IP5 — delete the whitelists (TI8, joint with Tune19 T19-1)

Not a new mechanism here: this phase CONSUMES IP1–IP4's types in the MIR
emitter and retires the shape-whitelist helpers as each consumer converts.

1. **IP5.1** Store edge (`transpile-mir.cpp:19744/19891/20054`): the value
   operand's static type (now precise for index/member/call results) decides
   the inline lane; `mir_expr_proves_native_return_lane` callers convert
   first (they are the store edge's gatekeepers).
2. **IP5.2** Convert `mir_direct_native_scalar_lane_proven` /
   `mir_direct_native_scalar_item_can_unbox` call-edge users; then
   `mir_native_int_bitwise_tree`.
3. **IP5.3** Each conversion deletes the helper when its caller count hits
   zero; a converted edge gets a `mir-check` fixture asserting the elided
   check AND the surviving check on the unproven twin (D8.6.2).

**Acceptance:** the four helpers have zero callers; `fn_array_set` gone from
navier_stokes2/hashmap2/fannkuch2 hot bodies (Tune19 T19-5 acceptance);
forced-GC sweep green (D8.6.3 — representation change class).
**Effort:** ~5 days, interleavable with Tune19 work. **Risk:** highest of the
plan — every elision is a potential SI14/SI3v2 bug; land edge-by-edge behind
the differential harness.

---

## IP6 — JS structural pass (TIG13, TIG14, TI2)

`JsAstNode = AstNode` (`js_ast.hpp:195`) — the slots exist; fill them.

1. **IP6.1 TI2 kernel seam:** extract the operator-typing core from
   `build_ast.cpp`'s binary branch into `lambda/runtime/type_infer_kernel.{hpp,cpp}`
   (numeric classify + comparison + join entry points), parameterized by a
   `TypeInferPolicy` (Lambda policy = current behavior bit-for-bit; assert
   via dump-diff on the corpus — zero change).
2. **IP6.2 TIG13** `build_js_ast.cpp:737`: replace `binary->type =
   &TYPE_FLOAT` with kernel calls under the JS policy: comparisons/`in`/
   `instanceof` → bool; `+` → float | string by operand types (ANY-pair →
   ANY, never FLOAT); other arithmetic → float (bigint operand → decimal);
   bitwise → int (ToInt32 lane); `&&`/`||`/`??` → operand union; `typeof` →
   string; `void` → null (already right). JS truthiness/coercion lives in
   the policy table, not in a forked kernel.
3. **IP6.3 TIG14** calls/members: type known builtin calls from
   `js_builtin_catalog.def` signatures (the catalog exists — add a
   `result_type` column mirroring IP2's pattern); member access on
   inferred-class receivers (JsClassMeta, D3.4.7) resolves the property's
   recorded type; subscript on known-element arrays → `elem?`. Everything
   else stays ANY_JS_CALL_MEMBER — measured, not guessed.
4. **IP6.4** Retire per-emitter re-derivation ONLY where the new types are
   consumed and proven equivalent (the P4/P6 `FnParamEvidence` walk stays —
   its retirement is TI1-step-5 territory, deliberately out of this plan).
5. **IP6.5** Reporting seam: JS lane emits static findings as warnings only
   (TI6) — wire the JS transpiler's diagnostics through the
   `record_as_static_warning` disposition unconditionally.

**Acceptance:** `make test262-baseline` 40,261/40,261 (the hard gate — any
typing change that shifts a JS result is a bug by definition); js262
benchmark rows non-regressing; census ANY_JS_BINARY → ~0,
ANY_JS_CALL_MEMBER measured baseline established.
**Effort:** ~6 days (kernel extraction 2, TIG13 1, TIG14 3).
**Risk:** silent JS semantic drift via the kernel seam — mitigated by the
IP6.1 zero-diff assertion and the test262 gate.

---

## IP7 — arm-join unions (TIG7, TIG8) — LAST, largest E208 exposure

Blocked on the open union-normalization-ordering question (DO22/S5.5.2) only
if arm joins produce unions the normalizer cannot canonicalize; the interim
disposition is: **join through `lambda_type_union_normalized` exactly as
`or` does today** (that path is shipped and tested), and defer any join the
normalizer returns NULL for back to ANY_JOIN (censused, not silent).

1. **IP7.1** If-expr (`build_ast.cpp:~5683`): replace the plain-differing-arms
   ANY with the normalized union (the diverging-arm `T | error` join already
   works — same allocation shape).
2. **IP7.2** Match (`:5800`): fold arm types left-to-right through the
   normalizer; default-arm absence contributes `null` (mirrors if-без-else).
3. **IP7.3** Corpus repair, in-change: re-run the historical experiment; the
   ~95 E208-class scripts get their real fixes (`or` containment, `| error`
   disclosure, or `^`) — this is the phase budgeted for it. Fixes reviewed
   as a language-surface change under S11.4.1 discipline.
4. **IP7.4** Census ANY_JOIN target <10; benchmark A/B (record-heavy rows
   deltablue/richards read if-join types — watch them).

**Acceptance:** corpus green with real fixes; ANY_JOIN <10; no row >5%
regression; tier-sweep mismatches not above baseline.
**Effort:** ~2 days code + ~3 days corpus repair.

---

## Sequencing and dependency graph

```
IP0 ──► IP1 ──► IP2 ──► IP3 ──► IP4 ──► IP5
 │                        │              ▲
 └──────────► IP6 (kernel seam after IP1; TIG14 after IP2's pattern)
                          └────► IP7 (last: E208 budget + needs IP1–IP4 types)
```

IP0 is strictly first (census is every later phase's acceptance metric).
IP5 and IP6 can proceed in parallel once IP3 lands. IP7 is deliberately
last: it has the largest acceptance-tightening blast radius and benefits
from every earlier precision improvement having already shaken out the
corpus.

Total estimate: ~4 weeks of focused slices, each independently land-able
and revertible, each gated by the standing gates in §0.

## Out of scope (open design questions — do not implement here)

Union canonical ordering ownership (DO22); flow narrowing TI5 (needs its
S-level ruling first); conditional error components on conversions beyond
the IP2 poison-carve-out disposition; closed-caller scalar propagation
(TI1 step 5 / Tune19 T19-4 — perf-plan territory); `FnParamEvidence`
retirement; relaxed-mode recovery enumeration beyond declarations; sized-lane
SO1–SO3 interactions; generics seam (TG4 × TI7).


---

## 9. As-landed record and the re-ordering finding (2026-08-18)

### 9.1 What landed

| Phase | Landed | Evidence |
|---|---|---|
| **IP0** | `AnyReason` catalog (23 reasons), `set_type_any`/`set_lit_type_any`/`census_any_type_id` recorders replacing all 61 Lambda + 44 JS bare `&TYPE_ANY` assignments, per-compile `any_census:` log line, `(any_census …)` dump block, `test/type_infer/refresh_census.sh` + committed baseline, and the `no-bare-type-any-assign` lint rule | census 27,569 nodes / 422 scripts; `legacy_unclassified` = 0; behavior-neutral (726/726 + 106/106 at the time) |
| **IP1** | TIG5 `and` normalized union; TIG6 relational `bool` for every statically comparable pair; TIG11 `build_list` block-collapse typing; TIG12 unary `number \| error`; TIG16 pipe element type | `logical_and` 194→0, `unary` 38→11, `compare` 514→497; fixtures `test/lambda/type_infer_ip1.ls` + `IP1OperatorsPublishPreciseTypes` |
| **IP2** | `SysFuncResultKind` column (6 kinds) + rows: 26 real-scalar transcendentals, 4 carrier-preserving rounding, 5 text transforms, 7 order-preserving collection ops | `sysfunc_row` 5045→4699; fixtures `test/lambda/type_infer_ip2.ls` + `IP2SysFuncRowsResolvePreciseResults` |
| **IP3** | TIG2 container-valued map fields (chained `a.b.c`); TIG15 destructure projection for container shapes; TIG10 `LOOP_KEY_SYMBOL` → symbol | `member_shape` 4574→4564, `decompose` 64, `loop_src` 259→252 |

Gates at close: `test_lambda_gtest` 728/728, `test_lambda_errors_gtest` 108/108,
`test_mir_emission_gtest` 59/59, `test_js_mir_emission_gtest` 21/21,
`test_mir_gc_stress_gtest` 88/88, lint clean. (`test_js_gtest`'s
`lib_tabulator` failure and the 11 conc/proc E221 baseline failures pre-exist
at HEAD — verified by stash-and-rebuild.)

Corpus repair, per TI6 no-staging: `lambda/package/editor/mod_commands.ls` —
six boolean predicates (`sel_collapsed`, `sel_single_leaf`,
`sel_same_parent_leaves`, `jlist_is_list`, `is_list_node`, `same_list_kind`)
gained `or false` containment. IP1's `and` union stopped hiding a real error
component: an `any`-typed argument reaching an untyped parameter makes the
call `T | error` (S11.4.3), which is pre-existing behavior the `and` ANY was
masking. Blast radius was 3 library files; the 11 failing editor tests were
all downstream imports.

### 9.2 The finding: type precision is gated on representation, not on typing rules

Five separate attempts to publish a **numeric-lane** type were reverted, each
after a corpus regression, and each for the same reason:

| Attempt | Symptom | Root cause |
|---|---|---|
| TIG1 `a[i]` → `T?` | `dmov: got 'int', expected 'double'` on cross-module `float[]`; typed_array_mask, element queries, cd2, triangl2 (9 scripts) | consumer's carrier unproven without a declared destination |
| math scalars → `float` | `nan` for complex and vector arguments (19 scripts) | rows are polymorphic; the emitter took the native scalar lane |
| text transforms → `string` | `trim('sym')` lost its symbol family; `replace` printed `"<error>"` | same polymorphism, plus a hidden error value |
| `slice` → arg0 type | a sliced string read back as `inf` (3 scripts) | view/slice rows return a different carrier than arg0 |
| ARRAY_NUM iteration → element type | 113 scripts | same numeric-lane dependency as TIG1 |

The pattern is uniform: **wherever the new static type implies a machine
carrier, publishing it before `ValueRep` carries that carrier violates SI14**
(the binding's type stops describing its bits). Where the type implies only a
boxed pointer — map fields, destructured containers, symbols, unions, bools
from comparisons — publishing is safe and landed without incident.

This is the same conclusion Tune19 §4 reached from the performance side
("expression results carry no representation"), arrived at independently from
the typing side.

**Consequence for the plan: IP5 is not the last consumer of IP1–IP4's types,
it is a PREREQUISITE for the numeric-lane half of them.** The corrected
ordering is:

```
IP0 ─► IP1 ─► IP2 ─► IP3(boxed half) ─► IP5/T19-1 ─► IP3'(numeric half) ─► IP4 ─► IP7
                                     └─► IP6 (JS; independent)
```

`IP3'` is the deferred set, each already written and reverted with its
evidence in place: TIG1 indexed-read element types, ARRAY_NUM iteration,
`slice`/view rows, and the polymorphic-row narrowing that needs a per-argument
shape test rather than a fixed success type.

### 9.3 Residue with named blockers

- **TIG11 content blocks** (`list` = 5,388, the largest single reason): the
  content node doubles as the function-body node, so its type feeds the return
  contract; publishing the container type broke 197 scripts. Needs IP4/TI7's
  return-contract seam.
- **`replace` / conditional error components**: correct type is
  `string | error`, but an error is impossible when the pattern argument is a
  proven plain string. Without that conditional rule the honest type breaks two
  benchmark sources — and the plan forbids editing benchmarks to dodge it. Row
  left open; this is the open design question named in §8.
- **LOOP_KEY_ALL** `int | symbol`: honest, but the union has no magnitude, so
  `k < n` on an unfiltered key becomes a static E312 across 113 scripts. Needs
  TI5 narrowing to separate the two per branch.
- **`arith_operand` (1,913) and `join_op` (1,001)**: arithmetic and `++` where
  an operand is not statically numeric. Both are genuinely open until
  closed-caller parameter propagation (TI1 step 5 / Tune19 T19-4) narrows the
  parameters feeding them.


---

## 10. IP5/T19-1 attempt — what it established (2026-08-18)

Attempted as one combined typing+representation change, per the §9.2
re-ordering. One fix landed; the rest converged on a single, now precisely
located blocker.

### 10.1 Landed

**`get_effective_type`'s INDEX_EXPR carrier rule is now unconditional.** It
already returned `LMD_TYPE_ANY` for an index read with no surviving element
witness — but only when the OBJECT's effective type was `array`/`array_num`.
Any other object (notably a cross-module binding whose object witness is
unproven) fell through to the AST type and let the emitter pick a lane the
load does not produce. The rule now holds for every object except `binary`,
whose u8 element lane comes from a dedicated load rather than the generic
helper. Green on the full gate set; it is a latent-bug fix independent of
TIG1.

### 10.2 What the attempt proved about TIG1

`get_effective_type` is the emitter's representation oracle in all but name —
every one of its special cases is a "the semantic type says X but the carrier
is Y" correction (match → ANY, handler → ANY, can-raise → ANY, unproven index
→ ANY). Publishing an index read's element type is therefore safe *for every
consumer that asks the oracle*.

Three successive hypotheses were tested and eliminated:

| Hypothesis | Test | Result |
|---|---|---|
| The element type itself is wrong | publish plain `T` | `float_arr[2]` works |
| The nullable LANE encoding is the problem | publish `T?` vs `T \| null` union | both crash identically |
| Only module bindings carry a false witness | suppress the AST-only element witness for module slots in `mir_known_index_element_type` | fixes the read, breaks `pdf_phase29_font_encoding_round` — other paths depend on that witness |

Minimal repro: `import .mod_arrays` + `int_arr[0]` segfaults (exit 139) when
the read publishes ANY concrete element type, while the oracle correctly
answers "boxed Item" for it.

**Conclusion: the blocker is neither the type, nor the nullability, nor the
witness — it is that consumers read `node->type` directly instead of asking
the oracle.** Each such consumer is a latent carrier bug that only becomes
observable once the type gets precise. Fixing them one at a time is the
whitelist antipattern TI8 exists to end.

### 10.3 The bounded sweep TIG1 actually needs

Not "delete four helpers" (§IP5 as originally written) but:

1. **Name the oracle.** Rename `get_effective_type` → `mir_expr_carrier_type`
   (or add that alias and deprecate the old name) so its contract is legible:
   it answers *which carrier*, never *which semantic type*.
2. **Inventory the direct readers.** Every `node->type->type_id` /
   `mir_decl_type_id(node->type)` in `transpile-mir.cpp` that feeds a lane,
   unbox, move, or store decision. Convert each to the oracle; leave the ones
   that genuinely want the semantic contract (diagnostics, boundary checks)
   and mark them.
3. **Ratchet.** An ast-grep rule forbidding new `->type->type_id` reads in
   carrier-deciding functions, mirroring `no-bare-type-any-assign`.
4. **Then** re-land TIG1, ARRAY_NUM iteration, `slice`, and the polymorphic
   registry rows — all of which are already written and reverted with their
   evidence in place.

Step 2 is the real cost and the reason this is its own gated change: the
failure mode is a segfault, not a diagnostic, so it needs the forced-GC and
differential harnesses running against each converted consumer group rather
than one big flip.
