# Radiant Implementation Clean-Up — LOC Reduction Proposal

**Date:** 2026-07-13
**Baseline commit:** `028a75323`
**Method:** Five parallel code-survey agents, one per subsystem (layout, CSS resolution, render/paint, event/editing/state, shell/misc). Every finding below was verified by reading the cited source bodies and tracing callers — not inferred from file names. Line numbers are current-tree at the baseline commit.

**Relationship to `vibe/Lambda_Code_Clean_Up.md`:** that ledger's radiant items (§4.x layout, §5.x paint) are largely done or quantified-partial. This proposal picks up its open remainders (§4.5 baseline walkers, §4.6 flex percentages, §5.1 replay switches, §5.3 paint-op table, §5.7 audit slices) and adds a fresh sweep of subsystems the ledger never covered: CSS property resolution, event/state logging, view_pool dumpers, cmd_layout, and the form/background painters.

**Progress markers:** ✅ means the item has landed and passed its recorded focused gates; partial/open items keep their plain ID and are described in the phase status notes below.

---

## 1. Current shape of the code

`radiant/` = **197,966 LOC** across 283 files (`.cpp/.hpp/.c/.h`), essentially flat (only `webdriver/` is a subdirectory).

| Subsystem | LOC | Files | Biggest files |
|---|---|---|---|
| Layout (block/inline/flex/grid/table/intrinsic) | 64,750 | 65 | layout_table 9,842 · layout_block 9,004 · intrinsic_sizing 6,294 · layout_flex 5,461 |
| Event / editing / state | 45,255 | 41 | event 9,644 · state_store 8,524 · event_sim 5,937 · dom_range 4,551 |
| Render / paint / display-list | 35,613 | 111 | render_svg_inline 5,596 · render_pdf 2,381 · render_background 2,108 · paint_ir 1,963 |
| Shell / misc / tooling | 24,784 | 33 | cmd_layout 7,200 · view_pool 3,459 · script_runner 2,574 |
| CSS resolution | 19,345 | 11 | **resolve_css_style 13,889** · resolve_htm_style 2,152 · css_animation 1,668 |
| Graph layout | 2,532 | 8 | |
| Other | 5,687 | 14 | |

Structural observations that drive the plan:

- **`resolve_css_style.cpp` is 7% of all of radiant in one file**, and 67% of that file (lines 4590–13889) is a *single function* — `resolve_css_property`, a ~180-case switch. The wins there are mechanical boilerplate, not replacing the switch.
- **The paint-op set is switched over in 5–6 places** (validate, lower-raster, lower-svg, plus two switches inside `render_pdf.cpp` — an undocumented fourth lowering backend the §5.3 audit missed), and the display-list op set in **11 switch sites across 6 files**. Adding one primitive touches all of them.
- **`intrinsic_sizing.cpp` re-implements grid, flex, and table measurement — twice each** (once for widths in a 3,050-line function, again for heights in a 1,040-line function). This is the single largest duplication mass in radiant, and also the most fragile.
- **Copy-paste-per-side / per-type families** are everywhere at small scale: 4-side border blocks, 9 state-transition loggers, 9 event-dispatch scaffolds, 7 form-control painter prologues, 5 table border getters, 4 cloned nth-of-type selector builders, 25 trivial teardown wrappers.

### 1.1 Verified non-findings (do NOT re-audit these)

Recording the negatives is as valuable as the positives — each was a plausible hypothesis that reading the code disproved:

- **Flex is layered, not parallel.** `layout_flex_multipass.cpp` wraps and drives `layout_flex_container` (layout_flex.cpp:575); it does not re-implement the line algorithm. Not a merge candidate.
- **There is no dead grid engine.** `grid_enhanced_adapter.hpp` *is* the live sizing path (layout_grid.cpp:282, 556), not legacy.
- **Multicol reuses block primitives** (`layout_flow_node`, `finalize_block_flow`, float prescan); its own code is genuinely fragmentation-specific.
- **`render_svg.cpp` vs `render_svg_inline.cpp` are opposite directions**, not clones: inline-SVG *parses* SVG → PaintIR; render_svg *serializes* PaintIR → SVG text, and already delegates to `paint_ir_lower_svg_stream`. Only residual dup: color-string helper (§P3) and the legacy border/text direct-emission layer (§D4).
- **`graph_to_svg.cpp` shares nothing with `render_svg.cpp`** — it builds a Lambda `Element` tree via `ElementBuilder`, not SVG text.
- **`state_store.cpp` is not a read/write serializer pair** — `state_dump_*` is write-only (consumed by event_sim text-diff asserts); there is no read-back walker to unify against. `state_schema.cpp` is a transition-rule table, not a deserializer.
- **E5 hit-test/text sharing is narrower than proposed.** The radio-group DFS is shared, but simulator `assert_element_at` intentionally uses a simple reverse-child, z-unaware walk while live event targeting goes through `target_html_doc`, editing margin hits, iframes, webviews, and rich-editing boundary logic. Simulator text assertions concatenate raw DOM text; StateStore selection text extracts laid-out text runs and block separators. Keep these as policy splits unless a new shared API exposes both modes explicitly.
- **calc()/unit conversion is already centralized** (`resolve_length_value`:2454, `evaluate_calc_expression`:2348) — no per-property calc duplication.
- **`resolve_htm_style.cpp` overlap is resolved** — it is now tag-dispatch over shared `apply_html_*` helpers; the ledger's §4.8 verdict still holds.
- **The §5.5 "rect zoo" is negative ROI** — `Rect`/`Bound`/`TextRect`/`DirtyRect`/`Point2D` are ~4 lines each and genuinely differ; consolidation touches hundreds of call sites to save ~a dozen lines. Close §5.5 as won't-fix.
- **Platform shells are properly abstracted** — window/surface/webview platform variance lives in separate per-OS files over different OS APIs, not near-identical `#ifdef` blocks.
- **`css_animation.cpp` interpolation does not duplicate resolver logic** (only 4 properties, own code path). Its *name→id map* does duplicate the parser's, though (§C4).
- **Dead code is rare.** No `.bak` files remain in radiant/; the only `#if 0` block is resolve_css_style.cpp:6065 (46 lines); event/editing/state has essentially zero dead functions.

---

## 2. Verification harness (prerequisite for every phase)

**Rule: a phase is only "done" when all three gates pass.**

1. **LOC gate** — `utils/verify_loc_reduction.sh` (new, committed with this proposal) compares the phase's file set between a git ref and the working tree and **fails unless total LOC strictly decreased**. New helper files created by the phase must be listed too, so moving code doesn't masquerade as deleting it:

   ```bash
   # example: phase C1, run before committing
   ./utils/verify_loc_reduction.sh --ref master \
       radiant/resolve_css_style.cpp radiant/resolve_style_helpers.hpp
   # prints per-file before/after/delta table; exit 1 if total didn't shrink
   ```

2. **Behavior gate** — the phase's listed test suites pass 100%:
   - always: `make build && make test-radiant-baseline`
   - layout-touching phases: `make layout suite=baseline`
   - editing/event/state phases: `make editor-4c-js && make editor-4c-view`
   - render phases: SVG-export / PDF fixtures per the phase notes
3. **Whole-tree sanity** — `make lint` stays clean; `./utils/count_loc.sh` recorded in the phase's commit message (running total).

Byte-identical output is the target for all "mechanical" phases: dumpers, loggers, and painters must produce identical snapshots/golden files, which the suites diff automatically.

---

## 3. Findings catalog

Grouped by subsystem; each entry: **what / where / how to unify / est. net LOC saved / risk**.

### CSS resolution (resolve_css_style.cpp and friends) — est. ~790–840

| ID | Finding | Where | Save | Risk |
|---|---|---|---|---|
| ✅ C1 | Missing `ensure_bound`/`ensure_border` helpers: the 3-line `if (!span->bound) … alloc_prop` block repeats ×85, border variant ×40, `if (!span->blk)` ×19. resolve_htm_style.cpp already has exactly these helpers (`ensure_html_border_prop`:156) — the pattern is sanctioned, this file never got them. | resolve_css_style.cpp, throughout the switch | ~250 | very low |
| ✅ C2 | 4-side border expansion: `BORDER_{TOP,RIGHT,BOTTOM,LEFT}_WIDTH` are four ~56-line blocks identical except the side suffix (8120–8344); same for `*_STYLE` (8345–8431) and `*_COLOR` (8433–8488). One `resolve_border_side_{width,style,color}(span, side, value, specificity)` each. | resolve_css_style.cpp:8120–8488 | ~235 | low |
| ✅ C3 | Margin/padding/inset per-side clones + logical-property fan-out: 4 margin sides (5918–5965) + 6 logical margin cases re-expanding to the same fields (5966–6056); padding mirrors; ~10 inset/top/left/right/bottom clones (9609+). Drive via `{prop_id → side}` over shared `resolve_box_side()`. Also extract the 15× repeated keyword/percent type-ternary into `css_value_axis_type()`. **Bonus: fixes live drift — `TOP` (9629) has isnan/calc handling that `LEFT`/`RIGHT`/`BOTTOM` lack.** Preserve the hard-wired LTR/horizontal-tb logical mapping (comments at 6011, 6023). | resolve_css_style.cpp:5918–6056, 9609+ | ~200 | low-med |
| ✅ C4 | `property_name_to_id` in css_animation.cpp (123–152) reimplements the parser's `css_property_id_from_name()` (css_properties.cpp:448) as a 26-entry strcmp chain. Delegate. | css_animation.cpp:123–152 | ~28 | low |
| ✅ C5 | 5 no-op cases that only `log_debug` and store nothing — `TABLE_LAYOUT` (12114), `BORDER_COLLAPSE` (12130), `BORDER_SPACING` (12145), `CAPTION_SIDE` (12156), `EMPTY_CELLS` (12172); consumers read raw declarations directly (layout_table.cpp:2876). Fold into `default:`. Plus the tree's only `#if 0` block (6065, 46 lines). | resolve_css_style.cpp | ~120 | low |
| C6 | *(optional, policy)* logging macro: `info ? info->name : "unknown"` idiom ×42, 957 `log_debug` lines total. `LOG_ENUM_PROP` macro slice only. | resolve_css_style.cpp | ~100–150 | med (debug-value tradeoff) |

### Layout — est. ~480–740 safe, +800–1500 structural

| ID | Finding | Where | Save | Risk |
|---|---|---|---|---|
| ✅ L1 | Dead computation: `calculate_initial_grid_extent` is pure and its only call discards the result (`(void)…`); the next step recomputes independently. | grid_enhanced_adapter.hpp:520–558, 635 | ~40 | very low |
| ✅ L2 | 5 near-identical border-source getters (`get_cell_border`, `get_table_border`, `get_row_border`, `get_rowgroup_border`, `get_column_border`) — same shape, different source struct. One parameterized getter. | layout_table.cpp:1880–2120 | ~80 | low-med |
| L3 | 3 recursive first-baseline walkers (ledger §4.5, still open): the shared `compute_element_first_baseline` (layout_alignment.cpp:255) is used by grid; flex has its own ~200-LOC family (layout_flex.cpp:3131–3330), table a ~290-LOC third (layout_table.cpp:894–1182). Fold flex + table onto the shared walker, parameterizing the row/cell strut case. | layout_flex.cpp, layout_table.cpp | ~300 | med |
| ✅ L4 | Flex justify-content distribution (`align_items_main_axis`, layout_flex.cpp:4123–4294) still partially hand-rolls what `compute_space_distribution` owns; the rest of the alignment layer is already shared. | layout_flex.cpp:4123–4294 | ~60 | med |
| L5 | Parallel flex height walker: `measure_content_height_recursive` (layout_flex_measurement.cpp:402–520) + parts of `measure_flex_child_content` (609–1382) predate the file's later delegation to the intrinsic-sizing API and run their own math (the §4.6 "flex bypasses re-resolver" symptom). Route through the intrinsic height API. | layout_flex_measurement.cpp | ~120–250 | med-high |
| L6 | **intrinsic_sizing.cpp mirrors real layout, twice per engine**: `measure_element_intrinsic_widths` (1970–5027) and `calculate_max_content_height` (5122–6163) each re-implement grid track selection (incl. `repeat()` expansion that also lives in grid_placement.hpp — written 3×), flex line packing (vs `create_flex_lines`/`calculate_flex_basis`), and table column min/max. Extract per-engine "intrinsic contribution" helpers shared by real layout and the intrinsic pass. **Attack incrementally, starting with the self-contained `repeat()` expansion.** | intrinsic_sizing.cpp | ~800–1500 | HIGH |
| L7 | *(restructure, modest net)* `table_auto_layout` (layout_table.cpp:6089, 3,215 LOC, 109 row/col/cell loops) — extract a cell/row/column iterator. | layout_table.cpp | ~150–300 | med |

Test coverage is strong: 494 flex + 344 grid + 703 table layout fixtures, 12,310 reference snapshots.

### Render / paint — est. ~330–450 safe, +250–300 structural

| ID | Finding | Where | Save | Risk |
|---|---|---|---|---|
| ✅ P1 | 7 form-control painters open with a byte-identical geometry prologue (scale/x/y/w/h), plus the CSS-override query block ×4 and the DocState/disabled block ×7. One `FormControlBox` struct + `form_control_box()` helper. | render_form.cpp:452–1577 | ~60 | low |
| ✅ P2 | Rounded-rect clip-path computed 3–4× (`background_gradient_clip_path`:289, `background_image_clip_path`:1920, `background_image_clip_shapes`:1888, inline in radial:450+). One `background_rounded_clip_path()`. | render_background.cpp | ~35 | low |
| ✅ P3 | Gradient/image tile loops are near-clones (`render_linear_gradient_layer`:1837 vs `render_radial_gradient_layer`:1858 + image tiling): same plan→row/col→tile-rect→skip→leaf shape. One `background_for_each_tile(…, callback)`. Preserve radial's deliberate position_rect ignore (:1862). Plus: `svg_color_to_string` (render_svg.cpp:282) ≡ `paint_svg_append_color` (paint_ir.cpp:1205) — share one. | render_background.cpp, render_svg.cpp | ~45 | low-med |
| ✅ P4 | DL descriptor table, safe slice (extends ledger §5.1): the ~26-op `DL_` set is hand-enumerated in **11 switch sites / 6 files** — storage serialize/deserialize/free (:114/:221/:506), retained dup/free (:77/:148/:431), bounds (:16), tile (:321/:350), replay (:90). An X-macro `DL_OP_LIST` descriptor {size, serialize, free, bounds} drives storage+retained+bounds first; **defer the replay-core merge** (the risky half). | display_list_storage.cpp, retained_display_list.cpp, display_list_bounds.cpp | ~80–140 | med (storage/retained low) |
| P5 | Paint-op descriptor table incl. PDF (extends ledger §5.3): `render_pdf.cpp` is a **fourth** parallel lowering backend with its own 21-case switches (:119, :1019, :1126), parallel to paint_ir validate (:130), lower-raster (:958), lower-svg (:1504). Extend `PAINT_OP_LIST` into a descriptor table {bounds-fn, per-backend lower fn}; scaffolding and validate/bounds boilerplate collapse, bodies stay. | paint_ir.cpp, render_pdf.cpp | ~120–180 | med-high |
| P6 | *(parity work, defer)* render_svg legacy direct-emission layer: borders still hand-emit SVG polygons (~140 LOC, `svg_emit_border_side`:688 etc.) while backgrounds already route through paint ops; decorated/shadowed text has a dual path (:491–566, ~75 LOC). Requires PaintIR SVG lowering to reach feature parity first — the §5.7 deferred slice. | render_svg.cpp | ~140–215 | med-high |

### Event / editing / state — est. ~275–375

| ID | Finding | Where | Save | Risk |
|---|---|---|---|---|
| ✅ E1 | 9 near-identical ~24-line state-transition JSON loggers (bool/int/float/scroll/dropdown-owner/ctx-menu-target/ctx-menu-hover/view-target/doc-bool). Two helpers: scalar-transition + view-ref-transition; scroll stays bespoke. | state_store.cpp:3212–3460 | ~90 | low |
| ✅ E2 | 9 `radiant_dispatch_*_event` functions repeat an identical 8–11-line scaffold (dom-resolve, JsCtxScope enter, timer, wrap, dispatch, read-prevented, exit); re-scaffolded again in 2 sim bridges. RAII `JsDispatchScope` + `radiant_dispatch_built_event()`; each function shrinks to its builder line. Watch the void-returning composition/focus variants. | event.cpp:4757–5126 | ~65 | low-med |
| ✅ E3 | state_machine `*_transition` family shares the enter/guard/leave/commit/assert epilogue ×6; `hover_transition` (240) and `active_transition` (277) are line-for-line identical except family/event/setter — merge into `single_target_transition()`. Transition ordering is load-bearing; preserve guard early-returns. | state_machine.cpp:57–385 | ~40 | low-med |
| ✅ E4 | 3 file-local "editing surface" JSON writers emit the same core object (event.cpp:229, editing_dispatch.cpp:524, state_machine.cpp:1722). One shared `editing_log_write_surface_core_fields()` in event_state_log; callers keep their extra fields. Removes a 3-way format-drift risk. | 3 files | ~18 | low |
| E5 | event_sim reimplements live-pipeline logic (the §5.7 flagged next slice): `find_element_at`:857 vs event.cpp hit-test, radio-group DFS (908–937) ≡ `uncheck_radio_group` (event.cpp:5443), `sim_extract_text`:946 ≡ state_store text extraction. Promote the event.cpp statics to a header and call them. Sim's hit-test is *deliberately* z-unaware — share without changing live behavior. | event_sim.cpp, event.cpp | ~75 | med |
| ✅ E6 | Caret/selection snapshot accessors overlap (caret_get_render_snapshot ⊇ visual_snapshot etc.); delegate narrow accessors to the widest one. | state_store.cpp:6871–7010, 7483–7660 | ~35 | low (broad callers) |

### Shell / misc / tooling — est. ~470–630 safe, + large decision items

| ID | Finding | Where | Save | Risk |
|---|---|---|---|---|
| ✅ M1 | nth-of-type selector-builder block copy-pasted ×4 (sibling-count loop + snprintf). One `build_element_selector()`. Output is consumed by layout JSON snapshots — must stay byte-identical. | view_pool.cpp:~2000, 2160, 2432, 3120 | ~115 | low-med |
| ✅ M2 | JSON field-emit boilerplate across the ~2,200-line view-tree dumper: every field is a 3-line indent/key/comma sequence; `#null` tag-name workaround repeats (~28 lines). `json_field()` + `resolve_tag_name()` helpers. | view_pool.cpp:1246–3459 | ~150–250 | low |
| ✅ M3 | 15 identical `clear_X_prop` one-liners + 10 trivial `free_X_prop` wrappers behind the `VIEW_PROP_TEARDOWN[]` table — store a member offset in the table row, keep the 4 non-trivial entries custom. Covered by memtrack/poison. | view_pool.cpp:160–486 | ~100 | med |
| ✅ M4 | `load_{text,markdown,wiki,latex,xml}_doc` share a ~30-line parse prologue (url→read→type-str→input_from_source→free) + a repeated katex/math CSS-load tail. Two helpers. Preserve mem_free ownership exactly. | cmd_layout.cpp:3870–4941 | ~115 | med |
| ✅ M5 | Trivial deletions: `radiant.cpp` (27-line legacy standalone main, excluded from build, superseded by lambda/main.cpp). | radiant/radiant.cpp | ~27 | very low |
| ✅ M6 | *(source-hygiene only — file not compiled)* webdriver_server endpoint boilerplate: ~20 handlers × ~8-line session/element lookup. Only worth doing if the server is kept (see D2). | webdriver/webdriver_server.cpp | ~150 | low |

### D. Decision-needed items (owner call, not scheduled)

| ID | Item | LOC | Question |
|---|---|---|---|
| D1 | `rdt_vector_cg.mm` — complete CoreGraphics vector backend, in `exclude_source_files`, referenced nowhere; but whole-file `__APPLE__`-guarded and edited as recently as Jul 6. | 1,337 | Intentionally-maintained alternate backend, or replaced by ThorVG for good? Delete or document. |
| D2 | `webdriver/webdriver_server.cpp` + `cmd_webdriver.cpp` — both excluded from build; live paths use stubs. | ~1,230 | Pending the lambda/serve HTTP router, or abandoned? |
| D3 | view_pool text dumper (`print_view_block` family, 881–1243) — debug-only (log + NDEBUG-gated side file); the JSON dump is the real test artifact. | ~360 | Is the human-readable dump still used for debugging? |
| D4 | render_svg border/text parity slice (P6) — needs PaintIR SVG lowering feature-parity work first. | ~140–215 | Schedule after P5 proves the descriptor table. |
| D5 | resolve_css_style logging slice beyond the macro (C6 full version). | up to ~900 | Debug-value vs LOC policy call. |

---

## 4. Phased execution plan

Each phase is a self-contained PR-sized change: **small enough to review, gated by the three checks in §2**. Order is strictly ratio-first — pure deletions, then mechanical extractions, then table-driving, then structural work. Later phases benefit from the confidence (and helper patterns) established by earlier ones.

Every phase ends with:

```bash
./utils/verify_loc_reduction.sh --ref <pre-phase-commit> <phase file list>   # must PASS
make build && make test-radiant-baseline                                    # must be 100%
<phase-specific suites>
```

### ✅ Phase 0 — Harness + pure deletions (~230 LOC, ~1 day, near-zero risk)
- Commit `utils/verify_loc_reduction.sh`; record `./utils/count_loc.sh` baseline.
- Delete: `radiant.cpp` (M5, 27), `calculate_initial_grid_extent` + call (L1, 40), `#if 0` block resolve_css_style.cpp:6065 (46), 5 no-op CSS cases (C5, 75), plus close ledger §5.5 as won't-fix (doc-only).
- Files: `radiant/radiant.cpp`, `radiant/grid_enhanced_adapter.hpp`, `radiant/resolve_css_style.cpp`.
- Tests: radiant baseline + `make layout suite=baseline` (grid + table fixtures cover L1/C5).

**2026-07-13 status:** code slice implemented. `utils/verify_loc_reduction.sh` already existed at the start of the slice; the Phase 0 deletion set removed 200 LOC (`radiant.cpp` -27, `grid_enhanced_adapter.hpp` -53, `resolve_css_style.cpp` -120) and `./utils/count_loc.sh` now reports `./radiant` at 197,766 LOC. The stale `radiant/radiant.cpp` exclusions were also removed from `build_lambda_config.json` and the no-printf lint allowlist after the file deletion.

Focused verification passed: `make build` and `make layout suite=baseline` (`4325/4325`, 7 skipped). The latest build-config residue cleanup additionally passed JSON validation (`python3 -m json.tool build_lambda_config.json`), `git diff --check`, `make check-int-cast`, `make build`, and `make layout suite=baseline` (`4325/4325`, 7 skipped). The latest lint-residue cleanup also passed `git diff --check`, stale-reference search, `make check-int-cast`, and `make layout suite=baseline` (`4325/4325`, 7 skipped). The stricter whole phase gate is still **not fully green**: `make test-radiant-baseline` exits nonzero on broader existing failures (puppertino 2, UI automation 3, render visual 4), and `make lint` exits nonzero on existing tree-wide lint findings outside the Phase 0 files. Do not start Phase 1 until those global gates are either fixed or explicitly re-baselined.

### ✅ Phase 1 — CSS mechanical helpers (~510 LOC, low risk)
- C1 `ensure_bound`/`ensure_border`/`ensure_blk` (~250) — mirror resolve_htm_style's accepted pattern.
- C2 4-side border width/style/color resolvers (~235).
- C4 css_animation name→id delegation (~28).
- Files: `resolve_css_style.cpp`, new `resolve_style_helpers.hpp` (or file-top statics), `css_animation.cpp`.
- Tests: radiant baseline + layout baseline (border fixtures). Behavior must be bit-identical; these are pure hoists.

**2026-07-13 status:** implemented as file-top statics in `resolve_css_style.cpp` plus `css_animation.cpp` delegation to `css_property_id_from_name()`. The leftover C4 wrapper has also been removed so the animation parser now calls the shared property-name API directly. C6's narrow enum-name logging cleanup is also implemented: 30 repeated `info ? info->name : "unknown"` fallbacks now call one helper while preserving log text. Verified after the slice with `make build` and `make layout suite=baseline` (`4325/4325`, 7 skipped); the CSS file LOC gate still passes at `-516` versus `HEAD` after the C6 helper, and the latest C4 wrapper deletion passes at `-5` over `css_animation.cpp` with `git diff --check`, `make check-int-cast`, `make build`, and clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

### ✅ Phase 2 — CSS box-side unification (~200 LOC, low-med risk)
- C3 margin/padding/inset side + logical fan-out over one `resolve_box_side()`; extract `css_value_axis_type()`.
- ⚠ This one is *not* byte-neutral by construction: it deliberately fixes the TOP-vs-LEFT calc/isnan drift. Run layout baseline before/after and review any diffs as bug-fixes, not regressions. Preserve the hard-wired LTR/horizontal-tb mapping.
- Files: `resolve_css_style.cpp`.
- Tests: radiant baseline + full `make layout suite=baseline`; inspect positioned/margin fixture diffs individually.

**2026-07-13 status:** implemented for margin, padding, inset, and physical `top/right/bottom/left`; logical inset mapping remains hard-wired to LTR/horizontal-tb. The NaN inset invariant is centralized so raw percentages remain re-resolvable and non-percentage NaN behaves as `auto`. Verified with `make build`, `make layout suite=baseline` (`4325/4325`, 7 skipped), and LOC gate over Phase 0-2 files: `-626` total versus `HEAD`.

### ✅ Phase 3 — Dumpers and loaders (~480–600 LOC, low-med risk)
- M2 view_pool JSON field helpers (~150–250), M1 selector builder ×4→1 (~115), M4 cmd_layout `load_*_doc` prologue + CSS-bundle helpers (~115).
- All output-facing: layout JSON snapshots must be **byte-identical** (the snapshot suite is the oracle).
- Files: `view_pool.cpp`, `cmd_layout.cpp`.
- Tests: radiant baseline + layout baseline + markdown/latex/xml layout suites.

**2026-07-13 status:** implemented the shared nth-of-type selector builder call sites, introduced JSON key/field emitters for repeated `view_pool.cpp` object headers, and consolidated `cmd_layout.cpp` temp-path, typed `input_from_source`, pool-backed stylesheet loading, document stylesheet-bundle storage for Markdown/LaTeX loader tails, shared Markdown/Wiki parsed-root extraction, and shared Markdown/Wiki read/parse/free prologues. Verified with `make build`, `make layout suite=baseline` (`4325/4325`, 7 skipped), and LOC gate over Phase 0-3 files: `-863` total versus `HEAD`; the latest M4 loader helper slices pass at `-12` over `cmd_layout.cpp` and passed `git diff --check`, `make check-int-cast`, `make build`, and `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up M4 loader cascade cleanup: text, Markdown, Wiki, LaTeX, XML, Lambda-script, load-time HTML cascade, and runtime subtree recascade paths now share one non-empty stylesheet application helper while preserving each loader's matcher lifetime, timing/log scaffolding, and stylesheet ownership. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

### ✅ Phase 4 — Event/state/paint scaffolds (~270–330 LOC, low-med risk)
- E1 state-transition loggers 9→2 helpers (~90), E2 dispatch scaffold RAII (~65), E3 SM transition epilogue + hover≡active merge (~40), E4 shared editing-surface writer (~18).
- P1 form-control prologue (~60), P2+P3 background clip/tile + color-string (~80 combined).
- Files: `state_store.cpp`, `event.cpp`, `state_machine.cpp`, `editing_dispatch.cpp`, `event_state_log.*`, `render_form.cpp`, `render_background.cpp`, `render_svg.cpp`, `paint_ir.cpp`.
- Tests: radiant baseline + `make editor-4c-js` + `make editor-4c-view` (golden event/state logs) + form/background/border-radius layout fixtures.

**2026-07-13 status:** implemented. P1 introduced a shared `FormControlBox` prologue for form-control painters and a shared rectangular focus-ring painter for checkbox/button controls; P2/P3 centralized background rounded-rect paths, background tile iteration, image tile planning, and SVG color formatting; E1 collapsed scalar/view-ref transition loggers; E2 added the JS dispatch scope and event-builder scaffold; E3 added a transition-depth scope plus common transition completion path and merged hover/active single-target transitions; E4 moved shared editing-surface JSON fields into `event_state_log`.

Focused verification passed after each slice with `make build` and `make layout suite=baseline` (`4325/4325`, 7 skipped). The Phase 0-4 LOC gate passed at `-804` total versus `HEAD` over the touched file set; the latest P1 focus-ring helper slice passes at `-7` over `render_form.cpp` and passed `git diff --check`, `make check-int-cast`, `make build`, and `make layout suite=baseline` (`4325/4325`, 7 skipped). The proposal's editor gates are still not fully green in this checkout: `make editor-4c-js` passes the core suite (`pass=218 fail=0 skip=0`) but then fails because esbuild cannot resolve `react`; `make editor-4c-view` is broadly red (`3/54 passed`) with multiple headless `Abort trap: 6` runs. Treat those as unresolved broader editor/harness blockers, not as accepted Phase 4 regressions.

Follow-up P1 form-focus cleanup: text input, checkbox, radio, and button painters now share one form-control focus predicate while preserving each control's state/checked/selection behavior. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up P2/P3 inset-shadow clip cleanup: inset box-shadow rendering now reuses the existing rectangular background path helper for full-rect fill and clip paths, preserving the previous clip/fill/free order. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

### Phase 5 — Layout targeted unification (~440 LOC, med risk; partially complete)
- L2 table border getters 5→1 (~80), L3 baseline walkers 3→1 (~300), L4 flex justify onto `compute_space_distribution` (~60).
- Files: `layout_table.cpp`, `layout_flex.cpp`, `layout_alignment.*`, `grid_baseline.hpp`.
- Tests: radiant baseline + full layout baseline (flex 494 / grid 344 / table 703 fixtures); L3 diffs reviewed case-by-case — baseline math is subtle.

**2026-07-13 status:** partially implemented and regression-gated. L2 now routes table/cell/row/rowgroup/column border lookups through one `BoundaryProp` side getter. L4 extracted flex main-axis outer-size, auto-margin counting, and physical justify-value helpers while preserving `compute_space_distribution` as the spacing authority, then removed the ignored `existing_gap` parameter from `compute_space_distribution` so physical row/column gaps remain explicitly owned by flex/grid callers. L3 is implemented for the table side: `layout_table.cpp` delegates its recursive first-text-baseline traversal to `radiant::compute_view_first_text_baseline()` with a table-row callback. The flex first-baseline walker remains intentionally open because it carries extra policy not present in the shared walker yet: stored flex-container baselines, inline-child skip to avoid first-line double-counting, positioned-child skip, direct text fallback, and `blk->first_line_baseline` fallback. Safe L3-adjacent flex cleanup did land: `layout_flex.cpp` now centralizes flex baseline margin/top-offset/direct-text helpers, the direct-text baseline helper, the repeated baseline-participation predicate, the laid-out child traversal helper, the repeated absolute/fixed positioning predicate used by shrink-to-fit, baseline, and fallback child measurement checks, and the repeated flex-line baseline extent collector used by initial line sizing, Phase 9a resizing, and post-layout baseline repositioning, while preserving the flex-specific traversal order. It also removed an unreachable duplicate direct-text fallback after the main direct-text branch. A guarded attempt to route flex child/text traversal through the shared walker was reverted after `make layout suite=baseline` produced 23 baseline-alignment failures; any future L3 flex slice needs a policy-preserving flex baseline adapter, not the generic text walker.

Verification passed after each Phase 5 slice with `make build` and `make layout suite=baseline` (`4325/4325`, 7 skipped). LOC gate over the Phase 5 layout files passed at `-73` versus `HEAD`; the flex baseline cleanup separately passes at `-23` over `layout_flex.cpp`, the latest flex absolute/fixed predicate cleanup brings the flex file set to `-105` over `layout_flex.cpp` plus `layout_flex_measurement.cpp`, and the latest L4 alignment API cleanup passes at `-27` over `layout_alignment.*`, `layout_flex.cpp`, and `grid_positioning.cpp`. The latest flex line-baseline metrics cleanup passed `git diff --check`, `make check-int-cast`, `make build`, and `make layout suite=baseline` (`4325/4325`, 7 skipped).

### Phase 6 — Descriptor tables (~200–320 LOC, med risk; partially complete)
- P4 `DL_OP_LIST` descriptor driving storage serialize/deserialize/free + retained dup/free + bounds (**not** the replay-core merge) (~80–140).
- P5 paint-op descriptor table folding validate/bounds scaffolding and bringing `render_pdf.cpp`'s two switches into the table (~120–180).
- Files: `display_list.h`, `display_list_storage.cpp`, `retained_display_list.cpp`, `display_list_bounds.cpp`, `paint_ir.h/cpp`, `render_pdf.cpp`.
- Tests: radiant baseline + retained/tiled parity + SVG-export + PDF fixtures — **all three lowering backends diffed**.

**2026-07-13 status:** partially implemented and regression-gated. P4 safe slice introduced `DL_OP_LIST`, `DisplayOpDescriptor`, shared replay-state flags, and a shared owned-payload free helper used by both display-list storage and retained-list rollback. P4b-adjacent replay slices now drive dirty-clip skippable replay ops, vector/no-op replay fallthrough, and raster-side vector-batch flushing through `DisplayOpDescriptor` flags, removing the replay-local skip switch, empty replay switch cases, and repeated replay-arm flush calls while leaving the full replay-core merge deferred; the latest descriptor slice folds the three single-purpose display-op flag helpers into one `dl_op_has_flags()` table query used by replay and bounds. Display-list storage now shares local validation stack/fail helpers, negative-size/resource-size predicates, owned path/picture free-and-null helpers, and resource-generation retainability predicates instead of passing four depth counters, spelling equivalent payload checks, duplicating cleanup in every relevant switch arm, and open-coding the same generation-present rule for glyph, surface, video, and webview resources. Retained-list resource freshness now shares generation-check helpers for surface-owned resources and global-generation resources while preserving their different invalidation policies, and retained-list clone payload copying now shares the path, dash-array, and gradient-stop clone checks while keeping picture duplication and element marker remapping custom. P5 safe slices moved owned PaintIR payload cleanup into the `PaintList` lifecycle, added shared owned-path, owned-gradient-stop, and owned-gradient-payload reset helpers inside that lifecycle cleanup, removed PDF's duplicate fallback cleanup switch, collapsed repeated PaintIR validation failure boilerplate through one per-command helper, moved validation stack-depth reporting onto one local state struct, collapsed validation end-of-list stack-balance checks through one local helper, collapsed the simple validation pop/decrement cases through one local helper, centralized repeated validation negative-size predicates including the remaining rounded-rect/filter/outer-shadow pairs, collapsed the saved-region depth/negative-region validation path for opacity, blend, and shadow-clip restore, now shares one non-negative size validator for the remaining rect-like PaintIR payload checks, shares PDF PaintIR local/stack transform composition through one helper while preserving backend-specific skip policy, shares SVG unsupported-op bookkeeping through one local helper, shares resource-plus-size validation for image, video, webview, and surface-blit payloads, shares SVG skipped-depth/open-group stack bookkeeping for transform, clip, and effect groups, shares SVG path-data construction/failure handling across path, gradient, and clip lowering, shares PDF raster-fallback success/failure accounting for gradient and SVG subscene fallbacks and effect-raster fallback, shares one PDF command-local transform resolver across path, gradient, image, and clip lowering cases, extends `PAINT_OP_LIST` into a generated name/flag descriptor table now consumed by PDF transform/effect preflight scanning, shares the list-level PaintIR flag scan used by PDF streaming preflight, tags raster no-op PaintIR transform-stack commands in the descriptor table so raster lowering no longer carries local empty cases for them, and now uses that descriptor table as the validation gate for unknown paint ops before the payload switch. The full P5 lowering descriptor table remains open: `paint_ir_validate`, raster/SVG lowering, and PDF lowering still use backend-specific command switches.

Verification passed after each descriptor slice with `make build` and `make layout suite=baseline` (`4325/4325`, 7 skipped). LOC gate for the P4 display-list files passed at `-10`; the P4b dirty-skip descriptor slice passes at `-18` over `display_list.h`, `display_list_storage.cpp`, and `display_list_replay.cpp`; the P4b replay-noop descriptor slice passes at `-25` over `display_list.h`, `display_list_storage.cpp`, and `display_list_replay.cpp`; the P4b display-op flag-query slice passes at `-37` over `display_list.h`, `display_list_storage.cpp`, `display_list_replay.cpp`, and `display_list_bounds.cpp`; the latest P4b replay-flush descriptor slice brings the display-list file set to `-87` over `display_list.h`, `display_list_storage.cpp`, `display_list_replay.cpp`, and `display_list_bounds.cpp`; the display-list storage validation/lifecycle helper slice passes at `-51` over `display_list_storage.cpp`; the latest display-list retainability helper slice passes at `-41` over `display_list_storage.cpp`; the retained resource-freshness helper slice passes at `-7` over `retained_display_list.cpp`; the retained clone-copy helper slice passes at `-13` over `retained_display_list.cpp`; the P5 paint/PDF file-set gate passes at `-60` over `paint_ir.cpp` and `render_pdf.cpp`; the late paint/PDF/view cleanup gate passed at `-193` over `view_pool.cpp`, `paint_ir.cpp`, and `render_pdf.cpp`; the latest PaintIR validation helper slices pass at `-18` over `paint_ir.cpp`, the latest non-negative-size validation helper slice passes at `-9` over `paint_ir.cpp`, the PDF transform helper slice passed at `-12` over `render_pdf.cpp`, the SVG unsupported-note helper slice brought `paint_ir.cpp` to `-32`, the PaintIR owned-payload cleanup slice brought `paint_ir.cpp` to `-37`, the PaintIR resource-size validation helper kept `paint_ir.cpp` at `-37`, the SVG stack-bookkeeping helper brought `paint_ir.cpp` to `-61`, the latest SVG path-data helper brings `paint_ir.cpp` to `-54`, the PDF fallback-accounting helper brought `render_pdf.cpp` to `-18`, the PDF command-transform helper brought `render_pdf.cpp` to `-23`, the latest PDF shared-fallback-accounting helper keeps `render_pdf.cpp` at `-23`, the PaintIR op descriptor flag slice passes at `-56` over `paint_ir.h`, `paint_ir.cpp`, and `render_pdf.cpp`, the PaintIR raster-noop descriptor slice passes at `-36` over `paint_ir.h` and `paint_ir.cpp`, the PaintIR validation descriptor gate keeps the PaintIR/PDF file set at `-67`, and the PaintIR list-flag helper slice brings the PaintIR/PDF file set to `-69`. The latest PaintIR gradient-payload lifecycle helper keeps the PaintIR/PDF file set at `-62` and passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped). Follow-up P5 raster-lowering cleanup now shares optional transform/clip pointer adapters across PaintIR raster command lowering while preserving effect-group temporary clip handling and SVG-local formatting policy; this passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up P4b replay dirty-skip cleanup: display-list replay now shares one dirty-clip skip helper for clean element ranges and dirty-skippable ops, preserving the element matching-index fast-forward and descriptor-flag policy. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up P4b replay pre-switch cleanup: display-list replay now shares one vector/descriptor pre-switch helper for vector-handled ops, replay no-ops, and vector-batch flush flags while leaving the raster/backdrop/shadow switch bodies policy-specific. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up P4b replay glyph dirty-clip cleanup: glyph replay now shares one dirty-clip tightening helper while preserving the replay switch's raster/backdrop/shadow policy ownership. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up P5 SVG-lowering cleanup: SVG payload and gradient matrix attribute emission now reuses the same optional transform adapter as raster lowering, while preserving SVG capability checks and effect-group policy. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up P5 SVG gradient-fill cleanup: linear and radial gradient lowering now share the generated gradient fill-path emission helper while preserving their distinct gradient definition attributes, path-data validation, fill-rule policy, transform emission, and stats accounting. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up P5 PDF-lowering cleanup: transformed fill-rect and rounded-rect PDF lowering now share the rounded-rect path/fill helper while preserving the existing untransformed rectangle fast path and per-command emission accounting. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up P5 raster-effect cleanup: PaintIR raster effect filter lowering now uses the same optional clip adapter as ordinary raster command lowering, eliminating the remaining raw effect-clip ternaries while preserving temporary clip lifetime. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up P5 PDF optional-transform cleanup: PDF PaintIR lowering and glyph-run rendering now share one local optional-transform adapter, removing the last raw PDF local-transform ternary while preserving transform-capability checks. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up P5 validation/lifecycle cleanup: PaintIR validation now shares gradient path/stops/count, glyph-bitmap descriptor, stroke-path, effect-group, and glyph-run payload predicates while keeping the per-op payload switch and messages intact. PaintList lifecycle cleanup also now uses a shared owned-glyph-run-text helper matching the owned path/gradient helpers. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up P5 builder optional-payload cleanup: PaintIR command builders now share optional transform and clip assignment helpers for path, gradient, image, glyph, picture, clip, filter, and surface commands while preserving the existing zero-initialized payload semantics. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up P5 PDF effect-group cleanup: PDF PaintIR lowering now shares one opacity-only effect-group predicate with the backend capability check localized, while preserving the existing passthrough/fallback accounting and opacity stack behavior. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up P5 SVG rect cleanup: SVG lowering now shares one filled-rect emitter for rect and rounded-rect paint ops while preserving capability checks, attribute order, color serialization, and emitted-count accounting. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up P5 owned-payload descriptor cleanup: `PAINT_OP_LIST` now tags the only PaintIR ops that can own heap payloads, and `paint_list_clear()` consults the descriptor flag before entering the owned-payload cleanup switch. This preserves the existing per-payload cleanup policy and passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up P5 validation stack-descriptor cleanup: PaintIR descriptors now carry stack push/pop direction for transform and effect-group ops, and validation consumes those descriptor flags before the payload switch while guarding against `PAINT_OP_LIST` drift. This removes the stack-only validation switch arms and passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up P5 effect-stack descriptor usage: PDF effect-balance scanning and SVG skipped-effect filtering now use the PaintIR effect-stack descriptor flags instead of spelling begin/end ops directly, preserving each backend's stack policy. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up P5 clip-stack descriptor validation: `PAINT_OP_LIST` now tags clip push/pop commands with stack direction, and PaintIR validation consumes those descriptor flags while preserving the existing clip-path payload check and pop-underflow message. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up P5 PDF fallback ownership cleanup: PDF effect-fallback path, stroke, and gradient wrappers now share one latest-command guard before marking PaintIR payload ownership, while keeping each payload's ownership bits explicit. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up P5 PDF transform-stack descriptor cleanup: PDF PaintIR lowering now handles transform push/pop through the PaintIR stack descriptor flags before the backend effect-group switch, preserving the existing transform capability, overflow, skip-depth, and underflow policy. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up P5 PDF effect-stack descriptor cleanup: PDF PaintIR lowering now handles effect-group begin/end through PaintIR effect-stack descriptor flags while preserving opacity-only acceptance, passthrough fallback, opacity stack overflow/underflow behavior, and emission accounting. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up P5 PDF clip-stack descriptor cleanup: PDF PaintIR lowering now handles clip push/pop through PaintIR clip-stack descriptor flags before the payload switch, preserving clip capability checks, command-transform resolution, skipped-depth behavior, and graphics-state restore policy. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

### Phase 7 — Structural (open-ended; in progress)
Ordered slices, each independently gated:
1. L6a grid `repeat()` expansion: one implementation shared by grid_placement.hpp and both intrinsic_sizing call sites (self-contained, duplicated 3×).
2. E5 event_sim onto shared hit-test/radio/text helpers (~75).
3. L5 flex height walker onto the intrinsic API (~120–250).
4. L6b/c flex-line packing and table column contributions shared between intrinsic pass and real layout (largest remaining mass, ~800–1500 total for L6; HIGH risk — every slice runs the full layout suite).
5. P4b replay-core merge (the deferred §5.1 half), P6 SVG border/text parity — only after descriptor tables are proven.
6. E6 snapshot accessor delegation, L7 table iterator extraction — opportunistic.

**2026-07-13 status:** started. L6a now has one intrinsic CSS `repeat()` track counter shared by intrinsic width and height fallback paths, fixing the previous width/height fallback split without changing the layout baseline. E5 has safe sub-slices implemented: live and simulated radio-group unchecking now share the same DFS helper while preserving live pseudo-state sync and simulator writer-only semantics, and simulator-local text target lookup plus rich-edit first-text fallback now share one text-descendant DFS while preserving `find_text_view()`'s null-target behavior. The live hit-test/text-extraction sharing pieces remain open because their policies still differ. E6 is implemented for caret and selection projection snapshots: visual/render/debug caret accessors and anchor/focus/visual/debug selection accessors now read through shared local snapshot structs while preserving their public failure defaults. M3 was also implemented opportunistically: `view_pool.cpp` now drives repetitive property clear/free teardown through generated member accessors and a table, keeping only real nested ownership hooks custom. M6 source-hygiene is implemented for the excluded WebDriver server: session and element route handlers now share request lookup/error helpers while preserving handler-specific WebDriver calls. L5 has safe preparatory cleanups: `layout_flex_measurement.cpp` now has one existing-height probe for explicit block height, laid-out height, and cached flex intrinsic height; all remaining matching length-only CSS height declaration checks now reuse `get_explicit_css_height()` in recursive, SVG/replaced, nested-child, and default-element measurement paths; width and height specified-style length readers now share one length-only resolver while preserving the positive-pixel-only contract; one shared inline-tag predicate now feeds both the flex measurement height classifier and margin/summing path while preserving the old `BR` split; one shared flex-container predicate now drives list/nested/container detection in measurement and intrinsic sizing; the recursive nested-flex zero-height fallback now shares the existing-height-or-intrinsic-height probe instead of carrying it inline; child contribution measurement now resolves explicit View-level or DOM CSS width/height through one local struct before width/height branches consume it; the nested-flex and regular-block zero-height fallback paths now share the parent cross-axis available-width calculation while preserving their old percentage-width policy split; flex child intrinsic-width probing now shares one child-font measurement helper so child text is not measured in the parent font; row-vs-column child-height accumulation now shares the same max-vs-sum helper across recursive, direct child-content, and intrinsic child traversal paths; inherited text-transform lookup now runs through one helper across button text, simple text-node, and child text traversal intrinsic measurement; intrinsic text-node whitespace normalization now shares one measurement-run helper across the simple text-node and child text traversal paths; direct nested-flex content classification now shares one text/element/explicit-height summary helper before choosing recursive height measurement or fallback; flex-measurement height fallbacks now route through one local `measure_intrinsic_sizes(..., AvailableSpace::make_width_definite(...))` wrapper when a block view exists, preserving the old fallback width at every call site; flex hypothetical cross-size measurement now also calls `measure_intrinsic_sizes()` with the actual item content width before falling back to the recursive measurement path; the fallback recursive flex-content height path now shares the existing-height probe, flex-direction check, and max-vs-sum accumulator from `layout_flex.cpp`; and the remaining obvious dimension locals in the flex measurement walker have been made `float` while leaving text metric API counts as `int`. The remaining manual recursive height classifier still has policy-specific recursion and is open. L7 is partially implemented: `layout_table.cpp` now has a table-wide row iterator feeding table-wide cell traversal and metadata/rowspan scans, a table-wide cell iterator for straight row/cell walks, one predicate-based table-cell finder for grid-position lookup, one shared row-cell iterator used by baseline alignment, rowspan collection, collapsed-row placement, row measurement, row-height synchronization, positioned rows, collapsed vertical border contribution, fixed-layout first-row width propagation, auto-layout cell width measurement, and table-wide cell traversal, one indexed row lookup shared by collapsed-border row-border candidate collection, one shared row-height/cell-alignment updater that preserves the existing rowspan-one-only and grow-only policies at each call site, one row-metadata-index helper for the repeated first-cell lookup pattern, one direct row-group iterator that now feeds the TBODY-row walker and no-row table height expansion, one row-group row iterator shared by section summaries, grouped-row layout, post-rowspan sync, metadata row synchronization/reflow paths, and collapsed-border row-group row counting, one TBODY row-group iterator for explicit-height distribution, shared row-y-position recalculation plus row/group view synchronization helpers for explicit table-height redistribution, shared explicit-height grid-spacing calculation plus the explicit-height row-y recalculation entry point, shared explicit-height row-extra application for body and no-body distribution while preserving their different eligibility policies, one shared table-row explicit-height resolver used by grouped and direct row layout while preserving percentage-height skip marking, one shared collapsed-row placement helper used by grouped and direct row layout, one shared row cell-measurement helper used by grouped and direct row layout, one shared direct-float clearance helper used by grouped and direct row layout, one direct-float scan helper shared by left/right intrusion, next-clear, and presence checks, one section-summary helper for explicit-height natural row-group accounting, column-span iteration/summing helpers for colspan width contribution and cell width calculation, a shared fixed-layout span assignment helper for `<col>`/first-row width propagation, shared fixed-layout helpers for positive `span` parsing plus grouped/standalone `<col>` CSS width resolution, shared fixed-layout helpers for `<col>/<colgroup>` explicit-width application, first-row cell width application, fixed explicit table-width resolution, fixed column-width distribution, and percent-or-length fixed-width value resolution, shared child horizontal-margin extraction for table-cell intrinsic width measurement while preserving the block-only shorthand fallback and inline form-control defaults, shared column constraint raise/clamp helpers plus one raw length-constraint resolver for `<col>` width/min-width/max-width application, shared column-total summing for percent-width totals and max-width minimum floors, whole-column assign/copy/scale helpers for fixed/auto distribution data moves, shared column summing for final table/tbody width totals, a shared preferred-width similarity predicate, a shared percent-column grow helper for the no-auto-column branch, a shared percent-distribution helper that preserves the old target/shrink/grow policy, a shared non-percent auto column distribution helper for the CSS 2.1 Case 1/2/3 block, shared occupancy/metadata grid placement helpers for the rowspan-normalization recount and final metadata assignment passes, one remaining positive-span parser for all table column/colgroup span attributes, one shared relative/sticky positioned-layout dispatcher for table row groups, rows, cells, and direct table children, one row-plus-cells positioned-layout helper reused by grouped and direct row paths, and shared positive vertical-margin helpers for table-caption height and placement accounting. These remove the largest remaining inline column-distribution blocks from `table_auto_layout`; smaller policy loops remain.

Verification for these Phase 7/M3 slices passed with `make build` and `make layout suite=baseline` (`4325/4325`, 7 skipped). Combined late-slice LOC gate passed at `-187` over `state_store.cpp`, `intrinsic_sizing.cpp`, `view_pool.cpp`, `paint_ir.cpp`, and `render_pdf.cpp`; the earlier L7 table slice separately passed at `-185` over `layout_table.cpp`, the L7 grid/span helper slice passes at `-30` over `layout_table.cpp`, the L7 fixed-layout/constraint helper slices pass at `-27` over `layout_table.cpp`, the L7 row/group metadata plus section-summary slices pass at `-16` over `layout_table.cpp`, the L7 empty-table float-row helper slice passes at `-18` over `layout_table.cpp`, the L7 explicit-height helper slice passes at `-19` over `layout_table.cpp`, the L7 row explicit-height resolver slice passes at `-40` over `layout_table.cpp`, the L7 collapsed-row placement slice passed at `-50` over `layout_table.cpp`, the L7 row cell-measurement helper slice passed at `-44` over `layout_table.cpp`, the L7 direct-float clearance helper slice passes at `-47` over `layout_table.cpp`, the L7 fixed-column distribution helper slice passes at `-42` over `layout_table.cpp`, the L7 explicit row-extra helper slice passes at `-30` over `layout_table.cpp`, the L7 positioned-layout dispatcher slice passes at `-38` over `layout_table.cpp`, the L7 direct row-group iterator slice passes at `-35` over `layout_table.cpp`, the L7 row-group row iterator slice passes at `-48` over `layout_table.cpp`, the L7 positioned-row helper slice passes at `-50` over `layout_table.cpp`, the L7 row-cell iterator slice passed at `-41` over `layout_table.cpp`, the L7 table-row iterator slice passed at `-33` over `layout_table.cpp`, the L7 indexed row lookup slice passes at `-36` over `layout_table.cpp`, the L7 collapsed vertical border contribution slice passes at `-23` over `layout_table.cpp`, the L7 predicate table-cell finder slice passes at `-16` over `layout_table.cpp`, the L7 first-row fixed-width iterator slice passes at `-18` over `layout_table.cpp`, the L7 auto-layout cell traversal slice passes at `-19` over `layout_table.cpp`, the L7 row-group row-count helper slice passes at `-11` over `layout_table.cpp`, the L7 direct-float scan helper slice passes at `-18` over `layout_table.cpp`, the L7 percent-or-length fixed-width resolver slice passes at `-11` over `layout_table.cpp`, the L7 intrinsic child-margin helper slice passes at `-7` over `layout_table.cpp`, the latest L7 caption vertical-margin helper slice passes over `layout_table.cpp`, the L5 flex-measurement height helper slice passes at `-20` over `layout_flex_measurement.cpp`, the L5 inline-tag helper slice passes at `-9` over `layout_flex_measurement.cpp`, the L5 flex-container predicate/float-compliance slice passes at `-60` over `layout_flex_measurement.cpp`, the L5 explicit DOM CSS-height helper slice passes at `-67` over `layout_flex_measurement.cpp`, the L5 nested-flex zero-height fallback slice passes at `-63` over `layout_flex_measurement.cpp`, the L5 child explicit-size probe slice passes at `-58` over `layout_flex_measurement.cpp`, the L5 fallback available-width helper slice passes at `-52` over `layout_flex_measurement.cpp`, the L5 child-font intrinsic-width helper slice passed at `-57` over `layout_flex_measurement.cpp`, the L5 child-height accumulation helper slice passes at `-52` over `layout_flex_measurement.cpp`, the L5 inherited text-transform helper slice passes at `-104` over `layout_flex_measurement.cpp`, the L5 whitespace text-run helper brought `layout_flex_measurement.cpp` to `-105`, the L5 nested-content summary helper kept `layout_flex_measurement.cpp` at `-101`, the L5 intrinsic-height API wrapper keeps `layout_flex_measurement.cpp` at `-91`, the L5 flex hypothetical-cross API slice passes at `-107` over `layout_flex.cpp` plus `layout_flex_measurement.cpp`, the L5 fallback recursive height helper slice passes at `-99` over `layout_flex.cpp` plus `layout_flex_measurement.cpp`, the latest L5 explicit CSS length helper slice brings the flex file set to `-128` over `layout_flex.cpp` plus `layout_flex_measurement.cpp`, the M6 WebDriver session/element lookup helper slice passes at `-211` over `webdriver_server.cpp`, and the E5 event-sim radio slice removes 24 lines from `event_sim.cpp`. The latest L7 caption vertical-margin helper slice passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L7 caption cleanup: table-caption positive horizontal margin accounting now shares helpers for wrapper width, caption width contribution, and top/bottom/caption-only placement while preserving the existing auto-margin and positive-only policy. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L7 caption predicate cleanup: table caption discovery and section-height accounting now share one block-view caption predicate while leaving the lower-level display/tag checks in anonymous wrapping and caption construction untouched. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L7 caption positioning cleanup: caption-only, top-caption, and bottom-caption placement now share one positive-margin positioning helper while preserving width adjustment, relayout, and height accumulation policy at each call site. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L7 caption relayout cleanup: top and bottom caption positioning now share the width-adjust/relayout/height helper while preserving their existing width-change probes and log messages. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L7 caption iteration cleanup: caption-only, top-caption, and bottom-caption placement now share one caption-array iterator while preserving each branch's sizing, relayout, and accumulation policy. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L7 direct table-child iteration cleanup: direct table block traversal now shares one iterator across row-group discovery, explicit-height section summaries, metadata row synchronization, row-group repositioning, rowspan-height refresh, and final positioned-layout dispatch while preserving each branch's row-group/direct-row/caption policy. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L7 table navigation cleanup: same-type table sibling scans now share one view-type navigation helper for first row group, row-group rows, row cells, and direct table cells while preserving the mixed direct-row/row-group traversal order in `ViewTable::next_row()`. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L7 post-rowspan height cleanup: grouped and direct row height refresh after rowspan distribution now share one metadata-to-view helper while preserving the existing `update_row_cells_after_height_change(..., true, true)` policy and leaving the later row-position reflow unchanged. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L7 colgroup column iteration cleanup: `<colgroup>` child scans now share one traversal helper across column layout, column/colgroup border lookup, metadata column counting, collapsed-column marking, and fixed-layout explicit column width propagation while preserving each caller's existing span/count policy. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L7 table column-source iteration cleanup: direct table-child scans for `<colgroup>` and standalone `<col>` now share one top-level column-source iterator across column layout, column/colgroup lookup, metadata column counting, collapsed-column marking, and fixed-layout explicit width propagation while preserving the existing per-caller span and early-result policies. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L7 metadata row-slot iteration cleanup: table metadata construction now shares one row-slot iterator for real table cells and out-of-flow cell slots across initial column counting, rowspan-normalization recount, and final metadata grid assignment while preserving each pass's span placement and out-of-flow one-slot policy. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

The remaining visible row-child scans in `layout_table.cpp` were rechecked after the metadata row-slot slice and are intentionally left open: they belong to DOM anonymous-table construction and float containment, not to the table metadata cell-slot traversal policy.

Follow-up L7 table-cell vertical-align child iteration cleanup: repeated table-cell child scans that skip uninitialized and absolute/fixed children now share one vertical-align child iterator across cell content-height measurement, baseline content shifting, recursive inline-child shifting, current-content-top calculation, per-cell vertical adjustment, and the legacy CSS vertical-align helper while leaving the rowspan-specific child bounds path separate. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L5 recursive-height guard cleanup: the manual recursive flex-height walker now shares one early existing-height/explicit-height guard while preserving its flex-container recursion, fallback measurement, and log text. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L5 nested-flex classifier cleanup: flex measurement now classifies nested flex container height through one `{height, reliability}` helper for empty content, explicit-height children, recursive content, text fallback, and element-only zero-height fallback while preserving the existing log messages and margin/reliability policy. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L5 recursive child-height cleanup: `measure_content_height_recursive()` now delegates each child recursion plus existing-height/intrinsic fallback decision to one helper while preserving recursion depth, max-vs-sum accumulation, and fallback log text. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L5 text child vertical-box cleanup: the flex measurement walker now resolves text-child vertical padding/border through shared helpers, preserving the old resolved-boundary-first and specified-style fallback order for longhands, shorthands, and border width lists. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L5 direct-child summary cleanup: list child counting, non-flex container inline/block/text classification, and generic text-child fallback now share one direct-child summary helper while preserving each branch's height heuristic and row/column accumulation policy. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L5 select intrinsic-width cleanup: flex measurement now shares option text-width scanning for direct `<option>` children and `<optgroup>` children while preserving the existing font setup, optgroup indent/min-width policy, and select arrow overhead math. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L5 button intrinsic-width cleanup: button text measurement now reuses the direct text-width helper with inherited `text-transform` applied, preserving the button-local font setup and intrinsic-height fallback policy. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L5 normal-line-height cleanup: temporary-font line-height measurement now uses one helper for flex child text lines, text-like form controls, and button content height while preserving each call site's fallback height and context-mutating font swaps. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L5 zero-height fallback cleanup: flex item contribution measurement now shares one zero-child-height fallback helper for nested flex and regular block children while preserving recursive measurement, available-width policy, and fallback log messages. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L5 row-share height cleanup: row flex children inside column flex parents now measure their estimated-share content height through one helper while preserving border-box adjustment, gap/child-count share calculation, CSS padding/border fallback, and diagnostics. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L5 direct element-height cleanup: `measure_flex_child_content()` now resolves direct child element heights through one classifier helper for headings, SVG/replaced elements, lists, nested/non-flex containers, text-like children, form controls, explicit CSS height, and default fallback. The existing margin and row/column accumulation policy remains at the call site. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L5 direct element accumulation cleanup: direct child element height contribution now shares one helper for inline-vs-row maxing, explicit-height margin suppression, synthetic margin fallback, and row/column accumulation while preserving the direct element-height classifier output. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up E5 simulator text traversal cleanup: event simulation now shares one text-descendant DFS for target-text lookup and rich-edit first-text fallback while preserving selector/text resolution policy and null-target behavior. This passed `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L5 direct-child traversal cleanup: flex measurement now shares the direct child text/element height traversal used by `measure_flex_child_content()`, preserving the text-as-inline max-height policy, the direct element classifier, and row-flex accumulation behavior. This passed the LOC gate over `layout_flex_measurement.cpp` (`-4`), `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L7 caption width-contribution cleanup: table auto-layout now measures caption width contribution through one helper while preserving explicit-vs-auto caption sizing, min/max clamps, table border/padding coordinate conversion, fixed margin-box expansion, and the original min/preferred table width policy. This passed the combined LOC gate over `layout_table.cpp` plus `layout_flex_measurement.cpp` (`-20`, with `layout_table.cpp` at `-16`), `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up P5 raster effect-stack descriptor cleanup: PaintIR raster lowering now handles effect-group push/pop through one descriptor-flag helper before the payload switch, preserving stack overflow/underflow diagnostics and effect begin/finish behavior while removing the switch-local effect stack arms. This passed the LOC gate over `paint_ir.cpp` (`-1`), `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L7 column constraint cleanup: table auto-layout now applies `<col>/<colgroup>` width, min-width, and max-width constraints through one helper while preserving implicit colgroup span division, min/max column arrays, and max-width clamping behavior. This passed the LOC gate over `layout_table.cpp` (`-14`), `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L7 fixed-height distribution cleanup: fixed table-layout row-height distribution now shares explicit height resolution and content-height adjustment through helpers while preserving CSS-vs-HTML height diagnostics, border-collapse/box-sizing border subtraction, separate-border padding and spacing subtraction, and the existing `fixed_row_height` policy. This passed the LOC gate over `layout_table.cpp` (`-30`), `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L7 fixed content-width cleanup: fixed table-layout column content width now resolves through one helper before `<col>` and first-row percentages are read, preserving border-collapse border-box subtraction, `box-sizing:border-box`, separate-border spacing subtraction, and the original diagnostics. This passed the LOC gate over `layout_table.cpp` (`-26`), `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L7 auto-width constraint cleanup: auto table layout now computes the available grid-width constraint through one helper while preserving the BFC float-avoidance `content_width` shortcut, parent/line/available-space fallbacks, margin subtraction, separate/collapsed table overhead subtraction, preferred-width clamping, and direct-float expansion input. This passed the LOC gate over `layout_table.cpp` (`-30`), `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L7 explicit content-area cleanup: auto table layout now converts explicit table width to the column content area through one helper while preserving collapsed-border half-border subtraction, separate-border content-box behavior, `box-sizing:border-box` border/padding subtraction, and existing diagnostics. This passed the LOC gate over `layout_table.cpp` (`-34`), `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L7 fixed padding-box cleanup: fixed table layout now converts the CSS fixed width to a padding-box through one helper before comparing against measured column width, preserving border-collapse/`box-sizing:border-box` border subtraction, separate-border padding addition, and the existing final-width diagnostics. This passed the LOC gate over `layout_table.cpp` (`-35`), `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L7 table min/max cleanup: table width min/max clamping and post-clamp column redistribution now live behind one helper while preserving border-box comparison, content-box padding/border expansion, max-width's minimum-content floor, original min/max comparison order, proportional redistribution, and empty-column equal distribution. This passed the LOC gate over `layout_table.cpp` (`-39`), `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L7 final width-prep cleanup: collapsed-column zeroing plus final column-sum, separate-border spacing, and table padding-box width assembly now share one helper while preserving saved original widths for collapsed columns, final column diagnostics, border-collapse no-extra-width policy, separate-border edge/between spacing, and padding accumulation for later min/max redistribution. This passed the LOC gate over `layout_table.cpp` (`-48`), `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L7 dead border-precision cleanup: table column positioning no longer runs the unused "Enhanced border precision" sampler; `cell_border_width` was only assigned and logged and had no layout consumers, while collapsed-border positioning already uses the measured column widths that include border halves. This passed the no-reference check for `cell_border_width`/`Enhanced border precision`, the LOC gate over `layout_table.cpp` (`-131`), `git diff --check`, `make check-int-cast`, `make build`, and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L7 row-group positioning cleanup: row-group content-width calculation, direct-float clearance/shift, and border-collapse/separate-border x/y/width assignment now share one helper while preserving the no-column floated/blockified fallback, border-spacing width contribution, collapsed-border half-border x offset, and existing diagnostics. This passed the then-current LOC gate over `layout_table.cpp`, `git diff --check`, `make check-int-cast`, `make build` (0 warnings), and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L7 row-group min-height cleanup: row-group height declaration resolution and explicit-height minimum distribution now share helpers while preserving percentage-height row marking, positive-row-only extra distribution, row metadata updates, border-spacing y accumulation, and the existing diagnostics. This passed the then-current LOC gate over `layout_table.cpp`, `git diff --check`, `make check-int-cast`, `make build` (0 warnings), and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L7 row visual-order cleanup: the first-header/source-order-body/first-footer ordering pass for row groups and direct rows now lives in one helper while preserving additional THEAD/TFOOT body-group treatment, direct-row tbody semantics, caller-owned ArrayList cleanup, and the existing ordering diagnostics. This passed the current LOC gate over `layout_table.cpp` (`-112`), `git diff --check`, `make check-int-cast`, `make build` (0 warnings), and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up C6 break logging cleanup: break-before and break-after keyword diagnostics now reuse the shared enum-name fallback helper instead of spelling the same `css_enum_info()` fallback inline. This passed the aggregate LOC gate over the touched code set (`-117`, with `resolve_css_style.cpp` unchanged at `+0`), `git diff --check`, `make check-int-cast`, `make build` (0 warnings), and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L7 row metadata tracking cleanup: grouped and direct row layout now record row y/height into metadata through one helper while preserving row labels in diagnostics and the existing collapsed-row/direct-row policies. This passed the current LOC gate over `layout_table.cpp` (`-108`), `git diff --check`, `make check-int-cast`, `make build` (0 warnings), and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L6 intrinsic display-detection cleanup: intrinsic width and height measurement now share one resolved/unresolved display helper for table, grid, and flex container detection across the explicit-width skip and table intrinsic-width branches; generated-content pseudo measurement now shares inline-vs-block display plus text/explicit-width measurement helpers; table-cell-like detection reuses the existing DOM display helper; and all remaining intrinsic `display` keyword declaration reads now go through shared style/element helpers. This preserves the `<table>` tag fallback, inline display keywords, pseudo default-inline policy, pseudo min-content text accounting, table-cell view-type override, late height-path `block`→`flow` mapping, and existing flex specified-style diagnostic. This passed the aggregate LOC gate over the touched code set (`-192`, with `intrinsic_sizing.cpp` at `-79`), `git diff --check`, `make check-int-cast`, `make build` (0 warnings), and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L7 caption parser/margin cleanup: table setup and caption-element marking now share the same `caption-side` keyword parser while preserving their distinct application policies: table styles set top or bottom, but caption element styles only override the table to bottom. Caption vertical-margin accounting also now composes the existing positive top/bottom margin helpers. This passed the current aggregate LOC gate over the touched code set (`-189`, with `layout_table.cpp` at `-105`), `git diff --check`, `make check-int-cast`, `make build` (0 warnings), and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L5 flex/grid display-probe cleanup: flex measurement now shares one specified-style `display` keyword probe across flex direction/container detection and grid-cache height exclusion while preserving resolved-display checks and the existing row-direction fallback. This passed the current aggregate LOC gate over the touched code set (`-201`, with `layout_flex_measurement.cpp` at `-16`), `git diff --check`, `make check-int-cast`, `make build` (0 warnings), and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L5 row-share box-extra cleanup: row flex children measured at an estimated width share now subtract unresolved horizontal padding/border through shared helpers instead of an inline CSS parse block, preserving the legacy shorthand-vs-longhand fallback order used before child bounds exist. This passed the current aggregate LOC gate over the touched code set (`-200`, with `layout_flex_measurement.cpp` at `-15`), `git diff --check`, `make check-int-cast`, `make build` (0 warnings), and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L5 intrinsic child eligibility cleanup: `calculate_item_intrinsic_sizes()` now checks display:none and absolute/fixed children through one local flex-item skip predicate while preserving resolved-position checks before the specified-style fallback. This passed the current aggregate LOC gate over the touched code set (`-195`, with `layout_flex_measurement.cpp` at `-10`), `git diff --check`, `make check-int-cast`, `make build` (0 warnings), and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L7 caption-stack positioning cleanup: top and bottom caption positioning now share one caption-stack helper while preserving top-vs-bottom width-reference policy, y-origin, per-caption relayout, and diagnostics. This passed the current aggregate LOC gate over the touched code set (`-181`, with `layout_table.cpp` at `-91`), `git diff --check`, `make check-int-cast`, `make build` (0 warnings), and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L7 cell vertical-align parser cleanup: table cell attribute parsing now maps resolved `in_line` and CSS declaration `vertical-align` keywords through one helper while preserving the default baseline, CSS override order, and existing log text split. This passed the current aggregate LOC gate over the touched code set (`-173`, with `layout_table.cpp` at `-83`), `git diff --check`, `make check-int-cast`, `make build` (0 warnings), and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L6 pseudo box-model cleanup: generated-content intrinsic width now resolves pseudo horizontal border, padding, and margin contributions through shared helpers while preserving the legacy `border-width` pair probe, length-only border longhands, length-item side shorthand scan, positive-only spacing policy, and existing pseudo width diagnostics. This passed the current aggregate LOC gate over the touched code set (`-153`, with `intrinsic_sizing.cpp` at `-59`), `git diff --check`, `make check-int-cast`, `make build` (0 warnings), and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L6 grid column-count cleanup: intrinsic width and height measurement now share the unresolved `grid-template-columns`/`repeat()` column-count probe while preserving width's zero-column fallback, height's one-column stacking fallback, and the existing height-path diagnostics. This passed the current aggregate LOC gate over the touched code set (`-147`, with `intrinsic_sizing.cpp` at `-53`), `git diff --check`, `make check-int-cast`, `make build` (0 warnings), and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L6 intrinsic gap declaration cleanup: intrinsic grid/flex measurement now shares length-declaration probes for `gap`, `row-gap`, and `column-gap`, including the old primary-declaration-suppresses-shorthand fallback rule. This passed the current aggregate LOC gate over the touched code set (`-138`, with `intrinsic_sizing.cpp` at `-44`), `git diff --check`, `make check-int-cast`, `make build` (0 warnings), and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L6 intrinsic keyword declaration cleanup: intrinsic display, flex-direction, flex-wrap, and writing-mode fallback probes now share one keyword-declaration helper while preserving width-vs-height flex policy splits. This passed the current aggregate LOC gate over the touched code set (`-144`, with `intrinsic_sizing.cpp` at `-50`), `git diff --check`, `make check-int-cast`, `make build` (0 warnings), and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L6 flex keyword interpretation cleanup: intrinsic flex width and height paths now share the specified-style `flex-direction` row classifier and `flex-wrap` wrapping classifier while preserving each caller's default, embed-style, and writing-mode policy. This passed the current aggregate LOC gate over the touched code set (`-144`, with `intrinsic_sizing.cpp` at `-50`), `git diff --check`, `make check-int-cast`, `make build` (0 warnings), and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L5 flex side-pair box-extra cleanup: flex measurement now shares CSS length longhand-pair and shorthand side-pair helpers for vertical text-child padding/border and horizontal row-share box-extra fallback while preserving the old vertical longhand-first order, horizontal single-length shorthand behavior, and border shorthand fallback. This passed the current aggregate LOC gate over the touched code set (`-158`, with `layout_flex_measurement.cpp` at `-24`), `git diff --check`, `make check-int-cast`, `make build` (0 warnings), and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

Follow-up L5 flex embedded-policy cleanup: flex measurement now shares one embedded `FlexProp` accessor plus flex-wrap and column-gap probes across intrinsic-size row wrapping, row-share estimated-width height measurement, and row-flex gap accounting while preserving the old embedded-style-before-specified-style policy. This passed the current aggregate LOC gate over the touched code set (`-147`, with `layout_flex_measurement.cpp` at `-13`), `git diff --check`, `make check-int-cast`, `make build` (0 warnings), and a clean `make layout suite=baseline` (`4325/4325`, 7 skipped).

### Running totals

| Milestone | Est. net LOC removed (cumulative) |
|---|---|
| Phase 0 | ~230 |
| Phases 0–2 | ~940 |
| Phases 0–4 | ~1,700–1,870 |
| Phases 0–6 | ~2,300–2,600 |
| + Phase 7 completed | ~3,300–4,600 |
| + D1/D2/D3 decided "delete" | up to ~7,500 (≈3.8% of radiant) |

The honest headline: **~2,300–2,600 LOC is high-confidence** (mechanical, golden-test-gated, phases 0–6); the rest depends on structural work (intrinsic sizing above all) and three owner decisions. Equally important, the descriptor tables (Phase 6) change the *slope*: today a new paint op touches 5–6 switches and a new DL op touches 11 — after Phase 6, one table row each.

---

## 5. Rules of engagement

1. **Never move-and-call-it-deleted.** The LOC gate lists new helper files alongside shrunk ones; the total must go down.
2. **Byte-identical first.** Phases 0, 1, 3, 4 must produce identical goldens/snapshots. Phase 2 (drift fix) and Phase 5 (baseline unification) may change outputs — each diff is reviewed as an intentional fix.
3. **One phase, one PR, all three gates.** No phase starts until the previous one is merged and its `count_loc.sh` total recorded.
4. **Per CLAUDE.md:** root-cause comments at any behavior-affecting fix point (the C3 drift fix in particular); `log_debug` not printf; floats in layout code; no `std::` containers in new helpers — use `lib/` types.
5. **When a finding dies on contact** (the code turns out to be load-bearing policy, not duplication), record it in §1.1 above rather than forcing the refactor — the negative list is what keeps future audits cheap.
