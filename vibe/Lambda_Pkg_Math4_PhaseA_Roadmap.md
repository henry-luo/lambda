# Math4 Phase A — Producer-Conversion Roadmap (workflow-generated, 7 agents)

> Generated 2026-06-18 by the phaseA-producer-specs workflow. Tasks #1-9 in the session todo list.

> **STATUS 2026-06-18 — see `Lambda_Pkg_Math4.md` §10 for the authoritative progress log.**
> Corpus 763→**822/921**, baseline **204/206**, ~1000 hardcode lines removed.
> DONE: Task 2 (text-op/integral limits), **Task 1 (frac_bar_spec DELETED, fraction.ls 100% metric-driven)**,
> Task 3 (render_sup_only Rule 18c), Task 6 (leaf dead-heuristic removal + imath fix),
> **sqrt/radical (display/text metric-driven — bucket dispatch gone, +17 corpus, +1 baseline, 0 regressions; §10.0)**,
> **delimiter raw exposure (the `2.41` magic RETIRED — sized-delim glyphs carry height_raw/depth_raw, strut CEIL@2s h+d once; pure refactor, 0 pass-rate change; §10.3)**,
> Task 5 partial (`fmt_delim_em` special-cases removed).
> BLOCKED/deferred: Task 4 (arrays — 2-row depth_holder float artifact, low yield),
> mechanical collapse 7/8/9 (the CAPSTONE).
> NEXT: **Phase A collapse** — fractions/scripts/sqrt/delimiters all carry full precision now;
> should clear the last 2 baseline fails (both fraction-in-sup residuals: strut-bottom 1.84 vs 1.85, sub-0.01 raw-sum).
> NOTE: corpus golden is **display-rooted** (MathLive SSR default); the harness now renders displaystyle.

## Hard execution order (corrected by synthesis)
Task 2 ∥ Task 4 (independent) → Task 6 (leaf audit) → Task 3 → Task 1 (script branch + colorbox) → Task 5 (last) → mechanical collapse (7→8→9).

## Per-producer specs

### Task #1 — risk=high, multi-session
- **Location**: /Users/henryluo/Projects/Lambda-opus/lambda/package/math/atoms/fraction.ls lines 456–995 (frac_bar_spec function with ~50 else-if branches); also: build_frac_bar_legacy (358–407), frac_bar_geom (197–278), build_frac_bar_rule15 (294–355)
- **MathLive ref**: ref/mathlive/src/atoms/genfrac.ts lines 118–312 (GenfracAtom.render method implementing Rule 15b–15e); ref/mathlive/src/core/v-box.ts lines 121–227 (makeRows algorithm for vlist layout, currPos accumulation, height/depth computation); ref/mathlive/src/core/box.ts Math.ceil(1e2 * arg1) / 1e2 CEIL@2 emission
- **Approach**: 1. EXTEND frac_bar_geom to compute FULL metrics for all 50 cases (not just display/text/fraction-nested). Return a geom record with: frac_height, frac_depth (both full-precision em), vlist_h, depth_holder, denom_top, line_top, numer_top (CSS positions CEIL@2 of raw), numer_ch, denom_ch (child wrapper heights), rule_height, pstrut, font_pct, expose_raw. 2. PORT the 50 legacy frac_bar_spec branches into frac_bar_geom decision logic by mapping each legacy (height, depth, *_child_height) tuple into equivalent (frac_height, frac_depth, vlist_h, numer_ch, denom_ch) computed from numer_box/denom_box raw metrics scaled by s_child. 3. REUSE build_frac_bar_rule15 (line 294) as the sole emitter: it already computes CSS from geom, CEIL@2 dimensions, and returns a metric-driven box with height/depth/height_raw/depth_raw/render_total. 4. REPLACE build_frac_bar_legacy gate (line 184) so frac_bar_spec cases are now handled by frac_bar_geom returning non-null (remove subscript_script GATE only when scripts.ls Rule 18 is ported). 5. DELETE frac_bar_spec entirely once verified; DELETE legacy constant accessors (script_frac_child_h_metric, etc.) if no longer referenced.
- **Ground truth**: VERIFIED via MathLive SSR: (1) Display frac 1/2: vlist height=1.15em, strut--bottom height=1.84em (1.15 + 0.68 + 0.01 depth adjustment). (2) Text tfrac 1/2: vlist height=0.84em, strut--bottom=1.18em (0.84 + 0.34). (3) Subscript frac (a_{1/2}) script-style child: vlist height=0.97em, numer wrapper height=1.05em (displaystyle-scaled font-size: 70%), denom wrapper height=0.47em (font-size: 71.43%), pstrut=3em (explicit max). (4) Descender fraction p/q: ceil2(1.02+0.63)=1.65 full height, individual box heights CEIL@2 of raw h+d. All CSS dimensions CEIL@2, no exceptions. Box.height/depth are CEIL projections; height_raw/depth_raw carry full precision for cascading parent rules.
- **Gate**: node test/lambda/mathlive/run_lambda_mathlive_markup.mjs --no-baseline writes temp/lambda_mathlive_markup_report.json. (1) PRE-CONVERSION: baseline passes must be 206/206 (SACRED). (2) EXPAND frac_bar_geom gate to accept colorbox/script-via-subscript by computing their metrics from numer_box.height_raw + denom_box.depth_raw; test a sample case (colorbox frac with numer_total >= 1.15 and denom_total >= 1.2, line 605) by emitting its geom and verifying CSS dimensions vs. legacy. (3) POST-CONVERSION: re-run corpus; measure regression per category (BIGOPS, DELIMITERS, FRACTIONS expected to hold, OPERATORS/SCRIPTS may fluctuate slightly if subscript_script interlock surfaces). (4) INTERLOCK CHECK: if script-style frac regresses > 2 cases, do NOT remove subscript_script gate; keep legacy fallback for scripts.ls parents. (5) NEVER run two diff_harness jobs concurrently (shared state conflict).
- **Risk**: INTERLOCK: frac_bar_spec's 50 branches were empirically tuned to legacy script_frac_child_h_metric, denom_child_for_default, and other heuristics. Converting them to metric-driven (s_child scaling + raw h+d) changes the boxes that script.ls reads as children, regressing its makeVList parents (Rule 18). Currently mitigated by subscript_script gate (line 205), which keeps script/scriptscript fractions via subscript on legacy. LATENT ISSUE: colorbox fractions (lines 605–655) check frac_ctx.colorbox_content but also read numer_box.is_fraction, numer_total, denom_total (output of render_total_of, which reads render_height/render_depth if set). Converting to metric-driven means numer_box.render_total changes when the inner fraction is converted, which cascades to the outer. MITIGATION: (1) Preserve subscript_script gate until scripts.ls Rule 18 is ported. (2) For colorbox, first verify that the new geom reproduces legacy height/depth before converting (shadow-run geom_spec, compare to frac_bar_spec output for N colorbox cases). (3) Measure regression per category post-conversion; if FRACTIONS or SCRIPTS drop > 10 points, rollback and check for unexpected metric changes.

### Task #2 — risk=medium, this-session
- **Location**: lambda/package/math/atoms/scripts.ls: render_large_op_limits_vlist (lines 434-579) and render_integral_inline_scripts (lines 66-166)
- **MathLive ref**: ref/mathlive/src/core/v-box.ts makeLimitsStack (lines 266-392), ref/mathlive/src/atoms/operator.ts render method (lines 57-90), ref/mathlive/src/core/atom-class.ts attachLimits (lines 997-1022)
- **Approach**: 1. For text-ops (lim/max/min/sup/inf/det): Replace render_large_op_limits_vlist's hardcoded heights/tops with calls to make_limits_stack. Reuse the existing pattern: (a) render sub/sup at script context, (b) call make_limits_stack with baseShift=0 (operator.ts never passes baseShift), (c) op_box = box.text_box(name, css.CMR, "mop") with height_raw 0.69444. (d) Extract height/depth/height_raw/depth_raw from the returned box. (e) Stop emitting render_height/render_depth (they equal height/depth); keep render_total. 2. For integrals: Port the side-limit path via render_integral_inline_scripts using the EXISTING (and working) vlist metrics. Verify all child heights use height_raw when available, scaled to script style, CEIL@2 at strut height. 3. For both: Apply Phase A box model: expose height_raw/depth_raw ONLY when the box carries full-precision metrics (math-driven, not constant-driven); height/depth are CEIL projections; render_height/render_depth omitted.
- **Ground truth**: Ground truth from MathLive SSR corpus: (1) \\lim_{h \\to 0}: vlist height=0.7em, depth_holder=0.76em, pstrut=3em, top:-3em (text-op base), sub child height:0.49em (CEIL@2(0.7 * raw_h where raw_h~0.7)). (2) \\int_0^1 f(x) dx: vlist height=1.55em, depth_holder=0.9em (integral-only constant), sub margin-left:-0.44em (italic), sub child height:0.46em. (3) \\iint_D (sub-only): vlist height=-0.41em (negative, content below origin), depth_holder=0.9em. Current Lambda hardcodes match these exactly; conversion goal is to derive them dynamically from make_limits_stack while preserving byte-exact output.
- **Gate**: Gate 1 (baseline): node test/lambda/mathlive/run_lambda_mathlive_markup.mjs --no-baseline, extract per-case pass/fail into temp/lambda_mathlive_markup_report.json. Gate 2 (text-ops): Test \\lim_{h \\to 0} f(x), \\max_x g(x), \\det M, verify vlist height/depth_holder match MathLive. Gate 3 (integrals): Test \\int_0^1, \\iint_D, \\oiint_S, verify no regression from current hardcodes. Gate 4 (regression check): Run full corpus, verify baseline categories (ACCENTS, FRACTIONS, etc.) remain 206/206. NEVER run two diff_harness jobs concurrently (shared state).
- **Risk**: The INTERLOCK: text-ops currently use hardcoded vlist_height/depth_holder/op_wrap_h (lines 446-514 in scripts.ls). Converting to make_limits_stack changes how child height_raw flows upward, altering metrics that parents (fractions, arrays, scripts) consume. Corpus baseline is SACRED at 206/206; prior box-model edits this session dropped 30-40 points before recovery. Key risks: (1) Fractions containing text-ops (e.g., \\frac{\\lim_n a_n}{b}) — if text-op vlist height changes, denom positioning shifts. (2) Scripts on text-ops in display mode (rare, but corpus includes) — depth_holder affects baseline drops. (3) Nested descenders in limits (e.g., \\lim_n with 'p' in subscript) — if sub_has_descender logic changes, box.depth adjustments cascade. Mitigations: (a) text_box("lim", css.CMR, "mop") already emits height_raw=0.69444, matching current hardcode 0.7. (b) make_limits_stack's ls_raw_h/ls_raw_d null-coalesce safely. (c) Phase A box model: only expose height_raw when derive-driven; height/depth are CEIL projections for backward compat.

### Task #3 — risk=high, multi-session
- **Location**: /Users/henryluo/Projects/Lambda-opus/lambda/package/math/atoms/scripts.ls, lines 798-906 (render_sup_only) and 740-792 (render_sub_only)
- **MathLive ref**: /Users/henryluo/Projects/Lambda-opus/ref/mathlive/src/core/atom-class.ts, lines 858-980 (attachSupsub, Rule 18b/c/d); /Users/henryluo/Projects/Lambda-opus/ref/mathlive/src/core/v-box.ts, lines 1-200+ (makeVList/makeRows)
- **Approach**: **Step 1: Eliminate hardcoded top offsets and vlist heights for non-fraction-child paths.** Rewrite render_sup_only/render_sub_only to replace legacy_top, legacy_vlist, legacy_child_h, tall_script, tall_base, numeric_x_script logic WITH the Rule 18c/d metric formulas (derived_top, derived_vlist are ALREADY correct; generalize them). 

**Step 2: Apply Rule 18 shifts directly.** For render_sup_only (single sup): compute supShift = max(init_sup, min_sup, sup_box.depth_raw * font_scale + 0.25 * x_height) using unscaled child (sup_box) full-precision *_raw fields, matching MathLive's attachSupsub lines 969-975. For render_sub_only (single sub): compute subShift = max(init_sub, sub1, sub_box.height_raw * font_scale - 0.8 * x_height) per Rule 18b.

**Step 3: Reuse render_both's makeVList pattern.** Both render_sup_only and render_sub_only must emit individualShift-style VList (like render_both, lines 684-733) with one child + pstrut, not the legacy inline-block span. Derive depths via initialShift logic: depth0 = -shift - child_d, then top = -pstrut - depth0 - child_d.

**Step 4: Set ONE height/depth pair with *_raw.** All paths (including in_fraction_child, tall_script, tall_base) now use: height = vlist_height (CEIL@2 of raw), depth = 0 (sup sits above baseline; sub below uses depth_holder), height_raw = raw position, depth_raw = 0 or 0 - (depth_from_shift). Drop render_height/render_depth/render_total — they duplicate height/depth or introduce Phase-B confusion. For fraction-child sups (box-height-only fallback for tall_script/tall_base), expose NO raw metrics (null).

**Step 5: Schema conformance.** Every converted box now carries exactly: element, height (CEIL@2), depth (CEIL@2 or 0), height_raw (full precision or null), depth_raw (full precision or null), width, type="ord", italic=0, skew=0. NO render_height, render_depth, render_total, left_right_render_*, is_fraction. Strut wrapper (outer context's script_pair) rounds ONCE via fmt_em_ceil2(h_raw + d_raw).
- **Ground truth**: MathLive SSR emit verification (displaystyle): 
- `a^2`: vlist height=0.87em (top=-3.41, child h=0.46 → 0.41+0.46=0.87 CEIL), pstrut=3em, top style emits -3.41em exactly. 
- `x^n`: vlist height=0.72em (tall_script N/A, numeric x+n not triggered), child h=0.31em → 0.41+0.31=0.72 CEIL. 
- Baseline shift offset = 0.41 at first script level (not nested); 0.43 when script_container=true (inside sup/sub contexts). 
- Rule 18c: supShift = max(initSup, sup1[si], supBox.depth_raw * font_scale + 0.25*xHeight). In display-rooted corpus, sup1 ≈ 0.36-0.37, initSup ≈ base.height - supDrop*font_scale (often 0 for char bases). supBox.depth_raw for "2" (digit) = ~0, so supShift ≈ max(0, 0.36, 0 + 0.25*0.43) = 0.36. Effective top = -(3 + 0.41) = -3.41. 
- Rule 18b: subShift = max(initSub, sub1, subBox.height_raw*font_scale - 0.8*xHeight). sub1 ≈ 0.46. subBox.height_raw for digit ≈ 0.44 at script scale ≈ 0.31. subShift ≈ max(0, 0.46, 0.31-0.34) = 0.46.
- **Gate**: Gate conversions via node test/lambda/mathlive/run_lambda_mathlive_markup.mjs --no-baseline; check temp/lambda_mathlive_markup_report.json per-case expectedHtml/actualHtml and summary.byCategory. Baseline 206/206 MUST NOT regress.

Corpus categories that exercise render_sup_only/render_sub_only: 
- SUPERSCRIPT_ONLY (simple x^2, a^n, etc.) 
- SUBSCRIPT_ONLY (simple x_a, etc.) 
- Nested scripts (x^{x^2}, x_{y^2}) trigger script_container flag. 
- Fraction-child paths (\\frac{...^a}{...}) set in_fraction_child=true; PRESERVE legacy hardcoded fallback for tall_script/tall_base in fraction contexts (these lack raw metrics, have no sibling descenders to round with).
Fixture test: node test/lambda/mathlive/fraction_branch_fixture.mjs for in_fraction_child branches.
- **Risk**: The INTERLOCK hit hard because render_both, make_limits_stack, and build_frac_bar_rule15 all use makeVList + full-precision *_raw. Every consumer of sup/sub boxes (script_pair, parent fraction, parent script if nested) reads these fields. Currently:
- render_sup_only outputs height_raw=null (legacy) or raw (derived). script_pair.render_height = max(base.height, sup.render_height) — but sup.render_height = null EXCEPT in_fraction_child. So script_pair.render_height is often null, then outer strut uses fmt_em(height) — OLD rounding.
- Conversion to ALWAYS expose height_raw (except fraction_child + tall) means EVERY passing non-fraction-child sup now rounds via CEIL@2(h_raw + d_raw) in the strut, ONCE, globally. Any mismatch in h_raw precision breaks corpus.
- Example: if digit "2" is rendered in a sup, its height_raw ≈ 0.413. scaled × 0.7 = 0.289, ceil2 = 0.29. vlist_height = 0.41 + 0.29 = 0.70 (NOT 0.72, the legacy fallback for small scripts). This hits the 0.72 baseline case and likely fails.
- Recovery: verify digit height_raw propagates from atoms/commands, check make_limits_stack uses sup_box.height_raw correctly (line 458), backfill any missing raw metrics in atom renderers.

### Task #4 — risk=medium, this-session
- **Location**: /Users/henryluo/Projects/Lambda-opus/lambda/package/math/atoms/array.ls — lines 302–430 (hardcoded metric tables: cases_table_metrics, dcases_table_metrics, matrix_table_metrics, array_table_metrics, row_top functions, cell_height logic)
- **MathLive ref**: /Users/henryluo/Projects/Lambda-opus/ref/mathlive/src/atoms/array.ts lines 435–550 (render method: arraystretch/arrayskip/arstrutHeight/arstrutDepth, per-row height/depth accumulation, VBox column stacking, offset calculation for centering or baseline alignment)
- **Approach**: 1. Replace hardcoded metric tables (cases_table_metrics, dcases_table_metrics, matrix_table_metrics, array_table_metrics) with dynamic computation from: arraystretch (default 1.0), BASELINE_SKIP (1.2), and arstrutHeight = 0.7 × arraystretch×1.2 / scalingFactor, arstrutDepth = 0.3 × arraystretch×1.2 / scalingFactor. 2. For each row: initialize height/depth to arstrut values, then max() with each cell's height/depth (reusing cell context scaling). 3. Accumulate per-row pos (cumulative height) and track totalHeight. 4. For most matrices (pmatrix/bmatrix/Vmatrix/smallmatrix/dcases): offset = totalHeight/2 + AXIS_HEIGHT = totalHeight/2 + 0.25 (centered on axis); for `array` environment non-multiline: offset = body[0].height (align to first baseline). 5. Build table_metrics dynamically returning {height, depth, render_total, vlist_height, depth_holder, pstrut, cell_height} (all CEIL@2 exact). 6. Reuse makeVList pattern from fraction.ls build_frac_bar_rule15 (individualShift stack, pstrut addenda, top positioning formula: -pstrut - currPos - box.depth). 7. Emit vlist via <span class=VLIST_T2> with two <span class=VLIST_R> rows (top for main stack, bottom for depth holder). 8. Set box height/depth to CEIL@2 projections of totalHeight and min-position-derived depth; expose height_raw/depth_raw (set to full-precision values) so parent (single-rounding strut) reads truthful extent.
- **Ground truth**: MathLive SSR renders smallmatrix (2x2, 'a','b','c','d'): strut h=0.69em (ceil of 0.58333+0.11 script char), strut--bottom h=0.88em (ceil of 0.85+0.03 total), va=-0.18em (ceil(-0.177)); 1x1 matrix with 'x': strut h=0.85em, strut--bottom h=1.2em (1.21 via ceil@2), va=-0.35em. These match: arstrutHeight (0.7×1.2) and arstrutDepth (0.3×1.2) at textstyle (no scaling); smallmatrix applies scriptstyle × 0.7 to each cell but table struts stay unscaled. Ceil@2 formula: Math.ceil(v×100)/100 (e.g. 0.849→0.85, 0.177→0.18, -0.177→-0.17 (ceil toward 0)). Lambda's ceil_em2 mirrors this (lines 48–54 of util.ls). Full precision: height_raw tracks unrounded metric (e.g. 0.85+0.3×1.2 = 1.21), depth_raw tracks 0.35+0.3×1.2 = 0.71 before ceil.
- **Gate**: Gate on test/lambda/mathlive/run_lambda_mathlive_markup.mjs (node test/lambda/mathlive/run_lambda_mathlive_markup.mjs --no-baseline writes temp/lambda_mathlive_markup_report.json; summary.byCategory). Focus categories: SMALLMATRIX (currently failing ~3–4 cases), CASES (2–3 failures), DCASES (1–2), MATRIX (passes, ~15–20). DO NOT run diff_harness concurrently. Corpus baseline 206/206 upstream; Phase A target: zero regression on passing cases, +16 smallmatrix/cases/dcases (208/224 or better). Run per-environment fixture (node test/lambda/mathlive/fraction_branch_fixture.mjs) to verify vlist column stacking matches MathLive's individualShift model.
- **Risk**: Currently-passing cases: 206 baseline matrices/cases (mostly non-smallmatrix or single-row). Regression vectors: (1) height_raw/depth_raw null/wrong → strut rounds (h+d) incorrectly, va=-ceil_em2(d) off by 0.01–0.02; (2) offset drift → all rows shift, vertical-align misalignment; (3) pstrut < max(cell.height + cell.depth) → text overflows top strut, or pstrut > required → excessive white space; (4) depth_holder calculation (vlist-r bottom row) diverges → CSS height rule wrong, breaking layout. The 16 failing smallmatrix/cases/dcases cases test these exact interlock points. Regression audit: run baseline on current (206 pass), apply conversion, rerun (target ≥206 pass, ideally +16).

### Task #5 — risk=high, multi-session
- **Location**: Lambda: /Users/henryluo/Projects/Lambda-opus/lambda/package/math/atoms/enclose.ls (render_bordered, render_bbox, render_lap — all 1 render_total setter, lines 27–140); /Users/henryluo/Projects/Lambda-opus/lambda/package/math/atoms/delimiters.ls (render_left_right, render_stretchy, render_sized, render_vertical_mult, render_mult_left_right_delim — 3 render_total setters, lines 103–419); /Users/henryluo/Projects/Lambda-opus/lambda/package/math/render.ls (render_stretchy_delimiter_group — SOLE producer of strut_total/strut_depth_em via stretchy_left_right_strut_total, lines 1584–1631)
- **MathLive ref**: ref/mathlive/src/core/delimiters.ts (makeSmallDelim lines 119–140, makeLargeDelim lines 146–176, makeStackedDelim lines 182–417); ref/mathlive/src/core/box.ts (Box.toString lines 54–68: Math.ceil(v*100)/100 CEIL@2 on signed value, bare "0" for exact zero); ref/mathlive/src/core/v-box.ts (makeRows lines 121–180: full-precision height/depth cascade; VBoxChild type union of VBoxElement | number); ref/mathlive/src/atoms/box.ts (BoxAtom.render lines 61–160: CEIL@2 via Math.floor/100)
- **Approach**: 1. Identify the 3 render_total setters in delimiters.ls (render_vertical_mult line 275, render_mult_left_right_delim line 313, render_corner line 246). 2. For each, extract the formula and compute corresponding height_raw/depth_raw that, when CEIL-projected via util.ceil_em2 per-component, reconstruct the exact render_total. 3. Convert render_sized/render_at_scale to emit height_raw/depth_raw unrounded; compute via level-to-scale lookup or explicit metric (e.g., level 3: h_raw=1.45, d_raw=0.95 → total_raw=2.40 → CEIL(h)+CEIL(d)=2.41). 4. In render_left_right, compute height/depth from raw using util.ceil_em2, set height_raw/depth_raw unconditionally, drop render_height/render_depth. 5. In render.ls render_stretchy_delimiter_group (line 1584), derive strut_total FROM height_raw+depth_raw of both delim boxes and content, applying abs(total-2.4)->2.41 adjustment to raw values, NOT after CEIL. 6. Stop emitting strut_depth_em, left_right_render_depth, left_right_render_* fields; emit only height/depth (CEIL@2 of raw) and render_total (CEIL@2 of sum). 7. Reuse makeVList pattern from atoms/fraction.ls (build_frac_bar_rule15: height_raw+depth_raw exposed when is_display or is_fraction_child; render_total=CEIL@2(h_raw+d_raw)). Apply expose_raw flag to enclose/delimiters: expose raw only when parent will cascade (e.g., left/right wrapper reading delim.height_raw).
- **Ground truth**: MathLive test 1 (left/right paren with 'a'): margin-top:-0.686em, height:0.43em, no explicit depth — the 0.686 is CEIL@2((h-axis=0.5)/(d+axis=0.186)×901/500)=CEIL@2(0.658)=0.66 (axis=0.25). Test 2 (boxed x): border+padding=0.3em total, height+=0.15, depth+=0.15, render_total is NOT set (absent from spec). Test 3 (left/right bracket with fraction): margin-top:-0.686em (same axis math), height:1.616em=CEIL(1.60654...) from stacked bracket composite (Size2 with repeats). The 0.686 constant in Lambda render.ls line 1634 is confirmed CEIL@2(-(d+axis)) for axis-centered delimiters. The 2.41 magic (line 1628: abs(box_total-2.4)<0.001→2.41) ensures single-rounding of Size3 delim total when it equals 2.40 raw (vertical bar pairs); this must be APPLIED to raw height+depth BEFORE CEIL, then CEIL the result to get the final render_total.
- **Gate**: Run: node test/lambda/mathlive/run_lambda_mathlive_markup.mjs --no-baseline (writes temp/lambda_mathlive_markup_report.json; per-case expectedHtml vs actualHtml; summary.byCategory yields 19 ALL-CAPS category buckets; baseline 206/206 SACRED). Specific corpus subcases: (1) LEFT/RIGHT delimiters: all 55 LEFT/RIGHT cases (1 per delim type + height variants); expect height/depth CEIL@2 match, margin-top=-CEIL@2(-(d+axis)), height=CEIL@2(stacked_total) or render_total if present. (2) Sized delimiters: SIZED_PAREN_* and SIZED_BRACKET_* categories (~30 cases); \big/\Big/\bigg/\Bigg variants. (3) Enclose: BOXED, FBOX, BBOX categories (~15 cases). Regression gate: if ANY LEFT/RIGHT baseline case fails after conversion, STOP and diagnose interlock (likely parent box reading .height_raw when it should compute from .height). Run ONLY run_lambda_mathlive_markup.mjs; DO NOT run diff_harness concurrently.
- **Risk**: Interlock 1: render_vertical_mult (line 271–280) sets height/depth/render_total unconditionally. If a parent node (e.g., subscript handling in scripts.ls) reads vertical_mult_box.render_total to compute a strut, converting to raw+CEIL will change the emit from (say) 1.21 to CEIL(1.20+0.0)=1.20 (if raw was 1.20). Baseline 55 LEFT/RIGHT cases will regress if margin-top or height HTML mismatch by ±0.01em. Interlock 2: render_stretchy_delimiter_group (line 1600, 1605) emits BOTH render_total AND strut_total with the same formula stretchy_left_right_strut_total. If a parent reads strut_total and another reads render_total, they MUST remain consistent; the conversion must unify them via a single height_raw+depth_raw cascade. Interlock 3: abs(box_total-2.4) check (line 1628) is tuned to Size3 delimiters (h=1.45, d=0.95, total=2.40). After CEIL@2, the total becomes 2.41. The parent reading this value MUST see 2.41, not 2.40. If the check is moved to a different line or the CEIL is applied at the wrong stage, the magic will be lost and all stacked-bar delimiters will emit height:2.40em instead of height:2.41em.

### Task #6 — risk=high, multi-session
- **Location**: lambda/package/math/box.ls: text_box() fn 57-67, text_height_for() fn 155-189, text_depth_for() fn 244-251, text_height() fn 318-434, text_depth() fn 436-519. Heuristic branches: 84 character/symbol cases hardcoded per-character (e.g., text=='β'->0.7, text=='ı'->skip lookup). metrics_data.ls: lookup(ch, font_name) fn 1330-1342; height_raw_of(metrics) fn 1351; depth_raw_of(metrics) fn 1352.
- **MathLive ref**: ref/mathlive/src/core/font-metrics-data.ts: const M1-M300+ (metric tuples [depth, height, italic, skew, width]), fontMetricsData object with per-character entries organized by font (Main-Regular, Math-Italic, AMS, etc.). Structure: each character mapped to [depth_rounded, height_ceil@2, italic_ceil@2, skew_half-up, width_half-up, height_raw_5dp, depth_raw_5dp, width_raw_5dp]. ref/mathlive/src/core/box.ts: Box.toString() line 57-68 emits CEIL@2 (Math.ceil(v*100)/100), bare "0" for exact zero. getCharacterMetrics() lookup pattern: retrieve full-precision metrics, expose height_raw/depth_raw only when needed by parent (strut, accent centering).
- **Approach**: 1. AUDIT text_height/text_depth heuristics (box.ls:318-519) against metrics_data coverage (1258 character entries across 8 fonts: cmr, mathit, ams, mathbf, tt, frak, script, cal, sans). For each hardcoded symbol branch (e.g., text=='β' or text=='∫'), determine: (a) whether lookup(text, font) from metrics_data now resolves it, (b) if so, what height_of(m) and depth_of(m) return vs. the heuristic constant. 2. IDENTIFY dead branches: any branch where metrics_data lookup succeeds for ALL fonts that can render that character; these can be deleted from text_height/text_depth since font_from_class() will route to lookup. 3. VERIFY expose_raw fields: text_box() already sets height_raw, depth_raw from text_height_raw_for/text_depth_raw_for (box.ls:61-62, which call lookup and height_raw_of/depth_raw_of). Confirm these null-check patterns are exhaustive (single-char, is_alpha_multi, is_repeated_char, is_digit_text_full). 4. APPLY metric-driven box pattern: converted text_box must set ONLY height/depth (CEIL@2 from metrics_data.height_of(m), depth_of(m)) and height_raw/depth_raw (full-precision 5dp from height_raw_of(m), depth_raw_of(m)). Emit one height+depth per box; parent strut will CEIL@2 at emission time (math.ls emit site). 5. DELETE fallback heuristic when (a) font resolves the char and (b) lookup height/depth values match or exceed strut constants (no regression). For mixed-font scenarios (e.g., "sin" multi-char fallback to cmr when font is null), keep fallback but verify it matches MathLive's probed behavior.
- **Ground truth**: MathLive SSR ground truth (3 spot checks via standalone render): \\imath -> height:0.7em (top strut), height:0.9em (bottom strut = 0.7+0.2 descent) — corresponds to metrics 'ı' in mathit [0.19, 0.45, ...] (height_ceil@2=0.45) but strut emits top 0.7 via text_height heuristic (cmmi descent 0.19, so 0.45+0.19=0.64 CEIL'd to 0.7). \\neg p -> expected height:0.7, actual height:0.7 (passes strut) but bottom strut mismatches 0.9 vs 0.89em — indicates depth off by 0.01em (rounding). \\jmath -> expected height:0.9 (0.45 char + 0.45 strut margin), actual height:0.89 — ȷ from mathit [0.19, 0.45, ...] gives depth_raw=0.19444, so strut=CEIL(0.45+0.19444)=0.65? Actually MathLive emits bottom strut height by adding ascender+descender per font, then CEIL@2 the SUM, not components. Lambda's heuristic 0.19 depth for ȷ matches mathit[0].
- **Gate**: GATE 1: node test/lambda/mathlive/run_lambda_mathlive_markup.mjs --no-baseline outputs temp/lambda_mathlive_markup_report.json; baseline is 206/206 corpus passing (SACRED). Check per-category before/after: "OTHER/SYMBOL" corpus (est. 56 failures) should drop toward 0 after conversion. GATE 2: node test/lambda/mathlive/fraction_branch_fixture.mjs for fraction height cascades (text_height hardcodes +/−/- at 0.69 to avoid fraction constant regression; verify this survives or is updated consistently). GATE 3: Spot-check 5 corpus cases per failing category: \imath, \jmath, \neg, \perp, \int, \sum, \prod — verify actualHtml strut heights match expectedHtml to 0.01em precision. GATE 4: Regression check on passing cases (788/921): especially ACCENTS, LEFT/RIGHT, FRACTIONS, SURDS (depend on text box heights for layout). Run corpus before conversion, after conversion, compare pass counts per category.
- **Risk**: Interlock manifestation 1: fraction.ls build_frac_bar_rule15 reads num1_h=num_box.height to compute numerator size relative to denominator. If num_box.height changes (e.g., ȷ from 0.44->0.45), numerator baseline shifts by 0.01em, vlist top-alignment (top: -3em) is unchanged but computed strut height mismatches MathLive's output. Interlock manifestation 2: text_height skip condition (box.ls:160-161) explicitly EXCLUDES "+/−" from metrics lookup to keep height at 0.69, because downstream fraction/strut calcs are tuned to 0.69. If conversion removes this skip and metrics_data cmr['+']=[0.08, 0.59, ...] returns height 0.59, fractions immediately regress (fraction constants assume +/- at 0.69). Interlock manifestation 3: legacy heuristic for descender letters (g/p/q/y/f/Q depth=0.19) vs mathit[g]=[0.19, 0.45, ...] depth_raw=0.19444 — metrics_data stores depth at index [0], but text_depth_for() calls metrics_data.depth_of(m) which accesses [0]. If depth mismatch (heuristic 0.19 vs lookup result), vlist descent changes, cascading into matrix/array row spacing.

## Synthesis roadmap

The dependency is now confirmed and precise. The `subscript_script` gate (fraction.ls line 205) keys off `frac_ctx.style == "script"/"scriptscript"` combined with `not is_fraction_child`. A fraction reaches script style ONLY via a subscript/superscript context (`sub_context`/`sup_context` set `style` to script and `script_container: true`). So a script-style fraction is necessarily a child of a `render_scripts` parent in scripts.ls. That parent (`render_sub_only`/`render_both`) reads the fraction box's metrics. This is the genuine task1→task3 interlock.

I have everything needed. Producing the synthesis.

---

# PHASE A PRODUCER-CONVERSION ROADMAP — Critical Synthesis

## Verified architectural facts (read against current code + MathLive SSR)

- **The new model is already proven in 3 sites**: `build_frac_bar_rule15`/`frac_bar_geom` (fraction.ls 197-355), `render_both` (scripts.ls 660-734), and `make_limits_stack`/`ls_walk` (scripts.ls 218-346). All three emit CEIL@2 CSS, carry full-precision `height_raw`/`depth_raw`, and drop `render_height`/`render_depth`. These are the reuse templates.
- **CRITICAL CORRECTION to Task 2's framing**: the symbol big-op path (∑/∏/⋃/⨁…) ALREADY routes through `make_limits_stack` (scripts.ls line 379-380). Only **text-ops** (\lim/\max) and **integrals** still hit the hardcoded `render_large_op_limits_vlist`/`render_integral_inline_scripts`. Task 2 is therefore NOT "port makeLimitsStack" — it is "migrate the two residual hardcoded paths onto the makeLimitsStack that already works for their siblings." This materially lowers Task 2's risk below the brief's "medium."
- **The single emit site is real and clean**: `math.ls` lines 34-76. It null-coalesces `render_height→height`, prefers `strut_total`/`strut_depth_em` overrides, and only takes the `use_raw` CEIL@2 path when BOTH `height_raw`/`depth_raw` are set AND no strut override exists. This is the convergence point the whole collapse aims at.
- **The task1↔task3 interlock is real, not hypothetical.** Confirmed: a script-style fraction (`frac_ctx.style=="script"`, `not is_fraction_child`) can ONLY arise as the child of a `render_scripts` call (sub/sup contexts are the sole producers of script style + `script_container`). The `subscript_script` gate (fraction.ls 205-208) keeps exactly those fractions on `build_frac_bar_legacy` because their `render_sub_only`/`render_both` parent reads their box. **Task 1 cannot fully delete `frac_bar_spec` until Task 3 lands.**

---

## 1. Risk-ranked execution order (by value/risk ratio)

| Order | Task | Justification (one line) |
|---|---|---|
| **1st** | **Task 2** (scripts.ls text-ops + integrals) | Highest ratio: the target's siblings already use `make_limits_stack` proving the model; isolated leaf producers with few parents; ground truth (lim 0.7/0.76, int 1.55/0.9) already matches. |
| **2nd** | **Task 4** (array.ls metric tables) | Self-contained env tables, no fraction/script consumes a matrix's raw metrics in the baseline; clear +16 smallmatrix/cases/dcases upside; `equation_table_metrics` already shows the dynamic pattern works. |
| **3rd** | **Task 6** (box.ls leaf metrics) | This is the ROOT producer every other box reads; converting it is high-leverage but high-blast-radius — must go before Task 3/Task 1 can trust raw digit/letter metrics, but the "+/− pinned at 0.69" interlock makes it dangerous. Do it as a careful audit, not a rewrite. |
| **4th** | **Task 3** (scripts.ls sup-only/sub-only) | Unblocks Task 1's script-frac branch; but globally flips every passing non-fraction-child sup/sub onto the CEIL@2(h_raw+d_raw) strut — one wrong leaf metric breaks many cases. Depends on Task 6 raw metrics being trustworthy. |
| **5th** | **Task 1** (fraction.ls frac_bar_spec, 50 branches) | Largest surface, multi-session, and its script-frac branch is hard-blocked by Task 3. Highest absolute risk; do the colorbox/composite branches only after scripts settle. |
| **6th** | **Task 5** (delimiters/enclose + render.ls strut) | Lowest ratio LAST: the `2.41` magic and the `strut_total`/`left_right_render_*` fields are the load-bearing consumers of EVERY other producer's raw metrics via `\left..\right`. Converting delimiters before fractions/scripts expose correct raw content extent means the delimiter sizes against wrong numbers. It must consume a settled tree. |

The ordering deliberately diverges from the brief's per-task `recommendedOrder` (which had 2,3,4,5 all tied at "1"): the global dependency graph forces Task 6 before Task 3, and Task 5 strictly last.

---

## 2. This-session vs multi-session — adversarial on the interlock

**Genuinely single-session (defensible):**
- **Task 2 (text-ops + integrals)** — *Upgraded confidence vs brief.* The brief calls it "medium / this-session" but worries makeLimitsStack will "change how child height_raw flows upward." That worry is largely already discharged: the symbol-op path proves `make_limits_stack` coexists with the hardcoded text-op/integral paths without cross-contamination, because they're dispatched separately at scripts.ls 379-382. The residual risk is narrow: fractions/arrays that *contain* a \lim could shift if the text-op box's exposed `height_raw` changes. Mitigation is cheap (text_box("lim") already emits 0.69444 ≈ the 0.7 hardcode). **Verdict: this-session, genuinely.**

- **Task 4 (arrays)** — *Confirmed this-session.* Adversarial check: does anything read a matrix's `height_raw` to round a parent? In the baseline, matrices appear inside `\left..\right` (Task 5 consumer) — so the ONE real interlock is delimiter sizing around a matrix. As long as Task 4 sets `height_raw`/`depth_raw` to truthful full-precision (it must), and Task 5 is NOT yet converted (still reads `left_right_render_*`/`render_total`), the matrix's `render_total` must remain present during Task 4. **Verdict: this-session IF `render_total` is retained until Task 5.** The brief's plan to "drop render_height/render_depth" is fine; dropping `render_total` here would break delimited matrices.

**Multi-session (brief is correct, and here's the sharper why):**
- **Task 1** — Not multi-session merely for size. It is multi-session because **one of its 50 branches (script-style frac) is hard-blocked by Task 3**, and several others (colorbox 605-655) read `numer_box.is_fraction`/`numer_total` = `render_total_of(numer_box)`, which changes the instant the inner fraction is converted to metric-driven. You cannot convert the outer colorbox branch until the inner-fraction producers are stable. **Sequenced sub-tasks across sessions, mandatory.**

- **Task 3** — The brief flags it high-risk; the adversarial point is sharper: it claims "derived_top/derived_vlist are ALREADY correct; generalize them." But Step 4 then says "ALWAYS expose height_raw (except fraction_child + tall)." The instant you do that, EVERY passing non-fraction-child sup/sub stops using `fmt_em(height)` in the strut and starts using `fmt_em_ceil2(h_raw+d_raw)` — a *global* rounding change. The brief's own worked example (digit "2": legacy 0.72 vs derived 0.70) shows this flips currently-passing cases. **This cannot be "generalize and ship"; it must be staged behind a per-case raw-metric audit (Task 6 first).** Multi-session, correctly.

- **Task 5** — Multi-session and the most under-rated by per-task ordering. The `abs(box_total-2.4)<0.001→2.41` magic (render.ls 1628) and the dual `strut_total`/`render_total` emission (1600/1605) are tuned so a Size3 vertical-bar pair emits `2.41`. The brief's plan to "apply 2.41 to raw before CEIL" is correct in principle but every `\left..\right` in the corpus reads `left_right_render_total`/`left_right_render_depth` from its content — i.e. from fractions (Task 1), scripts (Task 3), arrays (Task 4). **Converting Task 5 before those producers expose correct raw content extent sizes delimiters against stale numbers.** Strictly last.

**Adversarial verdict on any "low risk" claim:** none of the six is low-risk. Task 2 and Task 4 are the only two where the blast radius is bounded *and* the consumers (delimiter sizing) can be protected by retaining `render_total`. Everything touching sup/sub or the frac spec table is a global-rounding change.

---

## 3. The single BEST candidate to convert next: **Task 2 (text-ops, then integrals)**

Best value/risk: the new model already runs for its siblings, ground truth is confirmed-matching, the parent set is small, and a clean rollback exists (the hardcoded function stays callable until verified).

**First 3 implementation steps:**

1. **Add a text-op branch to the existing `make_limits_stack` caller, don't rewrite the vlist.** In `render_big_op_limits` (scripts.ls ~358-386), the dispatch at line 379 currently routes `is_text_op != null` to `render_large_op_limits_vlist`. Change it to call `make_limits_stack(op_box, sub_box, sup_box)` for text-ops too, where `op_box = box.text_box(display_text, css.CMR, "mop")` (already constructed at line 367-368, carrying `height_raw 0.69444`). Pass `baseShift=0` (operator.ts never sets baseShift — confirmed at v-box.ts makeLimitsStack, `baseShift = options.baseShift ?? 0`). Do NOT touch `render_large_op_limits_vlist` yet — leave it for symbol fallback and rollback.

2. **Verify `make_limits_stack` reproduces lim 0.7/0.76 before deleting anything.** The function at scripts.ls 270-346 must, for `\lim_{h\to 0}`, produce vlist height `0.7` and depth-holder `0.76` (ground-truth confirmed via SSR this session). If the text-op `op_box` height (0.7 / raw 0.69444) flows through `ls_walk` correctly, the `bigOpSpacing5` strut and `currPos` accumulation should yield those. If they don't, the gap is in `large_op_box_metric` vs `text_box` metrics — fix the leaf metric, not the stack.

3. **Migrate `render_integral_inline_scripts` SEPARATELY, after text-ops pass.** Integrals use SIDE limits (msubsup adjacent), not a stack, so they do NOT call `make_limits_stack`. Convert only the box-model fields: drop `render_height`/`render_depth` (lines 158-159), keep `height_raw`/`depth_raw` (already set, lines 156-157), retain `render_total` until Task 5. Verify against `\int_0^1` (vlist 1.55 / depth-holder 0.9, confirmed) and `\iint_D` (vlist −0.41). Do NOT batch this with step 1 — gate it independently.

**Exact gate command:**
```
node test/lambda/mathlive/run_lambda_mathlive_markup.mjs --no-baseline
```
Then inspect `temp/lambda_mathlive_markup_report.json` → `summary.byCategory`. **Pass condition: the 206-case baseline categories stay 206/206 (SACRED); BIGOPS and OPERATORS hold or improve.** Run it ONCE after step 1-2 (text-ops), and AGAIN after step 3 (integrals) — never concurrently with any other corpus/diff_harness job.

---

## 4. Dependency model — confirmed, with corrections

**Confirmed:** All 6 producers must convert before the mechanical collapse phase (invert leaves → delete `render_*`/`strut_*`/`left_right_*` → collapse the `math.ls` emit at lines 34-76 to a single `height_raw`/`depth_raw` CEIL@2 path). The `math.ls` emit already has the `use_raw` branch wired (line 46-48); it is gated off whenever ANY box in the tree lacks raw metrics or sets a strut override. The collapse = making `use_raw` always true, which requires every producer to expose raw and no producer to set `strut_total`/`strut_depth_em`/`render_total`.

**Inter-producer blocking (this is the part the per-task specs understate):**

- **Task 1's script-frac branch IS blocked by Task 3.** Hard dependency, confirmed via the `subscript_script` gate + context tracing. A script-style fraction is always a `render_sub_only`/`render_both` child; until those (Task 3) read raw metrics correctly, flipping the inner fraction regresses the script parent. The gate at fraction.ls 205-208 exists precisely to encode this. **→ Task 3 before Task 1's script branch.**

- **Task 1's colorbox/composite branches (605-655) are blocked by inner-fraction stability.** They read `render_total_of(numer_box)` and `numer_box.is_fraction`; converting an inner frac changes those. **→ Convert top-level display/text fracs (already done via `frac_bar_geom`) and nested fracs before the colorbox branches.**

- **Task 3 is blocked by Task 6.** Task 3's whole premise is that leaf `height_raw`/`depth_raw` (digit ≈0.413, letter metrics) are truthful, because it scales them by font_scale and CEIL@2s the sum in the strut. If Task 6's metric_data lookups don't yet match MathLive's 5dp raw values for the characters appearing in scripts, Task 3 regresses. **→ Task 6 (at least the digit/letter audit) before Task 3.**

- **Task 5 is blocked by Tasks 1, 3, 4.** `\left..\right` and sized delimiters size against `content.left_right_render_total`/`left_right_render_depth`, produced by fractions, scripts, and arrays. Delimiters must consume a settled raw-metric tree. **→ Task 5 strictly last.**

- **Task 2 and Task 4 are independent of each other and of the rest**, with one caveat: both must RETAIN `render_total` on their output boxes until Task 5 converts, because delimited \lim / delimited matrix read it. They can drop `render_height`/`render_depth` freely.

**Resulting hard order:** Task 2 ∥ Task 4 (independent, either first) → Task 6 (root leaf audit) → Task 3 (unblocks frac scripts) → Task 1 (script branch + colorbox last) → Task 5 (consumes settled tree) → mechanical collapse.

---

## Relevant file paths (all absolute)

- Emit site (collapse target): `/Users/henryluo/Projects/Lambda-opus/lambda/package/math/math.ls` lines 34-76
- Task 2 entry + proven template: `/Users/henryluo/Projects/Lambda-opus/lambda/package/math/atoms/scripts.ls` — dispatch 358-386, `make_limits_stack`/`ls_walk` 218-346 (REUSE), hardcoded targets `render_large_op_limits_vlist` 434-579 and `render_integral_inline_scripts` 66-166
- Task 1: `/Users/henryluo/Projects/Lambda-opus/lambda/package/math/atoms/fraction.ls` — proven `frac_bar_geom` 197-278 + `build_frac_bar_rule15` 294-355 (REUSE), `subscript_script` gate 205-208, `frac_bar_spec` 456-995 (DELETE target), `build_frac_bar_legacy` 358-407
- Task 3: `/Users/henryluo/Projects/Lambda-opus/lambda/package/math/atoms/scripts.ls` — `render_sup_only` 798-906, `render_sub_only` 740-792, `render_both` 660-734 (REUSE template), `script_pair` 624-650 (consumer)
- Task 4: `/Users/henryluo/Projects/Lambda-opus/lambda/package/math/atoms/array.ls` — tables 302-430, dynamic pattern exemplar `equation_table_metrics` 341-365
- Task 5: `/Users/henryluo/Projects/Lambda-opus/lambda/package/math/atoms/delimiters.ls` 103-419, `/Users/henryluo/Projects/Lambda-opus/lambda/package/math/atoms/enclose.ls` 27-140, `/Users/henryluo/Projects/Lambda-opus/lambda/package/math/render.ls` strut producer 1584-1638 (the `2.41` magic is line 1628)
- Task 6: `/Users/henryluo/Projects/Lambda-opus/lambda/package/math/box.ls` + `/Users/henryluo/Projects/Lambda-opus/lambda/package/math/metrics_data.ls`
- MathLive refs: `/Users/henryluo/Projects/Lambda-opus/ref/mathlive/src/core/v-box.ts` (makeRows 121-227, makeLimitsStack 266-392), `genfrac.ts` 118-312, `delimiters.ts`, `box.ts` toString CEIL@2
- Gate: `node test/lambda/mathlive/run_lambda_mathlive_markup.mjs --no-baseline` → `/Users/henryluo/Projects/Lambda-opus/temp/lambda_mathlive_markup_report.json`
