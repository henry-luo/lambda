// Function with typed parameters and return type
function add(a: number, b: number): number {
    return a + b;
}

function greet(name: string): string {
    return "Hello, " + name + "!";
}

console.log(add(3, 4));
console.log(add(100, 200));
console.log(greet("Lambda"));
