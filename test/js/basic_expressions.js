// Basic JavaScript expressions test

// Variable declarations
var x = 42;
let y = "hello";
const z = true;

// Arithmetic operations
var sum = x + 10;
var product = x * 2;
var division = x / 2;

// String operations
var greeting = y + " world";

// Boolean operations
var isTrue = z && true;
var isFalse = !z;

// Comparison operations
var equal = x == 42;
var strictEqual = x === 42;
var notEqual = x != 0;

// Object and array literals
var obj = {
    name: "test",
    value: 123
};

var arr = [1, 2, 3, "four"];

// Property access
var objName = obj.name;
var arrFirst = arr[0];

// Function call (simple)
// console.log("Hello from JavaScript!");

// Return the sum for testing
sum;
