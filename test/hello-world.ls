let g = 2;
fn main() {
    // comment
    let a = 8 + 5, b = true, c = null, d = "hello", e = [1, 2, 3], h=["123", "true", "str"],
        f = g + a + 1, r=e[0], k = for (j in [1, 2], m in [3,4]) j+m,
        p = {a:b}, q = p.a, 
    if (f > 15 and q and r==1) "great"
    else "not great"  
}
