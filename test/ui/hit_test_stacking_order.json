{
  "name": "Hit-test follows shared stacking paint order",
  "html": "test/ui/hit_test_stacking_order.html",
  "description": "Clicks the overlap of two positioned layers. The higher z-index element should receive the event even though both boxes contain the point.",
  "events": [
    {"type": "log", "message": "=== hit_test_stacking_order ==="},
    {"type": "click", "x": 110, "y": 100},
    {"type": "assert_text", "target": {"selector": "#status"}, "equals": "high", "assert_timeout": 500}
  ]
}

