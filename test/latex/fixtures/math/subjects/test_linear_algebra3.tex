% test_linear_algebra3.tex - Special matrices and operations
\documentclass{article}
\usepackage{amsmath}
\usepackage{amssymb}
\pagestyle{empty}
\begin{document}

% Orthogonal matrix
$$Q^T Q = Q Q^T = I$$

% Singular Value Decomposition
$$A = U \Sigma V^T$$

% Norm
$$\|x\|_2 = \sqrt{\sum_{i=1}^{n} |x_i|^2}$$

% Inner product
$$\langle x, y \rangle = \sum_{i=1}^{n} x_i \overline{y_i}$$

\end{document}
