% brackets can be used in optional arguments when protected by a group
\documentclass{article}
\usepackage{echo}
\begin{document}
% echo simply outputs its arguments delimited by + (mandatory args) or - (optional args)
\echoOGO[{t]}t]{ext} more
\end{document}