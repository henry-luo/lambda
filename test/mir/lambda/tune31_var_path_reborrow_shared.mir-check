{
  "schema_version": 1,
  "description": "Tune31 Phase III A1: after a body-side var-root capture, a nested typed field borrow restores root preparation before the direct checked child walk.",
  "checks": [
    {
      "in_func": "_tune31_share_then_increment_#",
      "expect_seq": [
        { "pattern": "call\tcow_prepare_write_p, cow_prepare_write" },
        { "pattern": "call\tcow_path_borrow_typed_map_field_p, cow_path_borrow_typed_map_field" }
      ]
    }
  ]
}
