{
  "schema_version": 1,
  "description": "v5: the `int` parameter uses the i64 lane, so it appears as `i64:`. Tail-home forwarding: the epilogue adopts the result into the incoming _scalar_home, then restores the number watermark, then returns (Stack API #15/#16/#21).",
  "checks": [
    {
      "in_func": "_accumulate_#",
      "expect": [
        ":\tfunc\ti64, p:runtime, i64:_n, i64:_acc, p:_scalar_home"
      ],
      "expect_seq": [
        { "pattern": ", _scalar_home" },
        { "pattern": ", number_frame_", "next_line": true },
        { "pattern": "ret\t", "next_line": true }
      ],
      "count": {
        ", _scalar_home": 1
      }
    }
  ]
}
