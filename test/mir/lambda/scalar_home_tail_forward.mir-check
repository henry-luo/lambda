{
  "schema_version": 1,
  "description": "C16/G0: the `int` parameter is in int's one native lane, the IEEE double, so it appears as `d:` rather than `i64:`; the int64 scalar-home donation this fixture checks is unchanged. Tail-home forwarding: the epilogue adopts the result into the incoming _scalar_home, then restores the number watermark, then returns (Stack API #15/#16/#21).",
  "checks": [
    {
      "in_func": "_accumulate_#",
      "expect": [
        ":\tfunc\ti64, p:runtime, d:_n, i64:_acc, p:_scalar_home"
      ],
      "expect_seq": [
        { "pattern": ", _scalar_home" },
        { "pattern": ", number_frame_", "next_line": true },
        { "pattern": "ret\t", "next_line": true }
      ],
      "count": {
        ", _scalar_home": 1
      }
    }
  ]
}
