{
  "schema_version": 1,
  "description": "v5: the `int` parameter uses the i64 lane, so it appears as `i64:`. Return convention 3 (RV1 shape 2): the body takes no caller home at all and hands the wide payload back on a second MIR result, so the obsolete adopt cluster is replaced by the pending-pair classify (ursh/sub/ubgt) and a two-lane `ret`.",
  "checks": [
    {
      "in_func": "_accumulate_#",
      "return_convention": 3,
      "expect": [
        ":\tfunc\ti64, i64, p:runtime, i64:%p1, i64:%p2"
      ],
      "forbid": [
        "_scalar_home",
        "lambda_item_adopt_scalar_home"
      ],
      "expect_seq": [
        { "pattern": "ursh\t" },
        { "pattern": "sub\t" },
        { "pattern": "ubgt\t" },
        { "pattern": "mov\ti64:" },
        { "pattern": "ret\t" }
      ]
    }
  ]
}
