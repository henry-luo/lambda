{
  "schema_version": 1,
  "description": "D5.3/D8.4.3v2: immutable native arguments admit total scalar calls before the loop test, including a zero-trip invocation. 2026-09-13: a guarded `while` emits a proven fast arm plus a canonical generic sibling (D2.2.2, D8.3.1), so the hoisted call is emitted once per ARM. The count tracks that; the property itself -- the call is admitted before the loop test and never from inside the body -- is pinned by the expect_seq and by the second group, which forbids the call inside the proven arm's body (loop test .. back edge). The loop test the hoisted call must precede is now the native int-lane compare (T19-C); it no longer widens to the double lane, so the ordering pin follows `lt`.",
  "checks": [
    {
      "in_func": "_stable_abs_#",
      "expect_seq": [
        "call\tfabs_p, fabs",
        "lt\t{{r}}, {{r}}, %p1"
      ],
      "count": {
        "call\tfabs_p, fabs": 2
      }
    },
    {
      "in_func": "_stable_abs_#",
      "in_range": {
        "from": "lt\t{{r}}, {{r}}, %p1",
        "to": "jmp\tL"
      },
      "forbid": [
        "call\tfabs_p, fabs"
      ]
    }
  ]
}
