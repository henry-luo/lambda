{
  "schema_version": 1,
  "description": "Self-tail recursion reuses an exclusive final string argument and freezes at publication.",
  "checks": [
    {"in_func": "_tail_builder_#", "expect": ["call\tfn_strcat_p, fn_strcat", "call\tfn_string_freeze_p, fn_string_freeze"], "forbid": ["call\tfn_join_p, fn_join"]},
    {"in_func": "_field_tail_#", "expect": ["call\tfn_strcat_p, fn_strcat"]}
  ]
}
