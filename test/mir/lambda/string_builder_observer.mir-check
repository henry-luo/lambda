{
  "schema_version": 1,
  "description": "A length-only consumer retains builder ownership through the loop.",
  "checks": [{"in_func": "_observe_builder_#", "expect": ["call\tfn_strcat_p, fn_strcat"], "forbid": ["call\tfn_string_freeze_p, fn_string_freeze", "call\tfn_join_p, fn_join"]}]
}
