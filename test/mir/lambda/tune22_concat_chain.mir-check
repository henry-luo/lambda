{
  "schema_version": 1,
  "description": "T22-6: a left-associated all-string append chain uses the exclusive builder, while a chain containing an int retains generic join conversion (S9.1.1, D5.2.2v3).",
  "checks": [
    {
      "in_func": "_concat_chain_tune22_#",
      "expect": ["call\tfn_strcat_p, fn_strcat"],
      "forbid": ["call\tfn_join_p, fn_join", "call\tfn_string_p, fn_string"]
    },
    {
      "in_func": "_concat_mixed_tune22_#",
      "expect": ["call\tfn_join_p, fn_join"]
    }
  ]
}
