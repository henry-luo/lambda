{
  "schema_version": 1,
  "description": "Tune26 T26-2: a var float[] destination keeps its checked COW store, while a distinct readonly float[] peer receives the loop's dense extent proof and raw float load. 2026-09-13: the var destination's checked COW store is now array_num_set_cow_idx (CW32v2); it COW-prepares at operation entry and still returns ItemError, so the checked path this fixture pins is the same boundary under its current name.",
  "checks": [
    {
      "in_func": "_tune26_var_readonly_peer_#",
      "expect_seq": [
        "ge\t{{r}}, %p3, -2147483648",
        "le\t{{r}}, %p3, 2147483647",
        "ge\t{{r}}, {{r}}, {{r}}",
        "bt\tL",
        "lsh\t{{r}}, {{r}}, 3",
        "dmov\t{{r}}, d:({{r}})"
      ],
      "expect": [
        "call\tarray_num_set_cow_idx"
      ]
    }
  ]
}
