{
  "schema_version": 1,
  "description": "Bound-entry wrapper epilogue: pass an activation-local scalar home to the direct body, adopt its result into the incoming caller home, and only then restore the number watermark and return (Stack API #24/#27).",
  "checks": [
    {
      "in_func": "_inner_b_#",
      "expect_seq": [
        { "pattern": "add\tscalar_home_" },
        { "pattern": ", scalar_home_" },
        { "pattern": "call\tlambda_item_adopt_scalar_home_p" },
        { "pattern": ", number_frame_" },
        { "pattern": "ret\t" }
      ],
      "expect": [
        ", _scalar_home"
      ],
      "count": {
        "call\tlambda_item_adopt_scalar_home_p": 1,
        "lambda_item_heap_rehome": 0
      }
    }
  ]
}
