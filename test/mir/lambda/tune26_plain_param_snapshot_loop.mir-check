{
  "schema_version": 1,
  "description": "Tune26 T26-4: a plain float[] parameter snapshots through one entry detach, then a non-null same-lane loop uses its certified unique owner and compact COW/OOB cold arm.",
  "checks": [
    {
      "in_func": "_tune26_plain_param_snapshot_loop_#",
      "expect": [
        "call\tcow_mark_shared_p, cow_mark_shared",
        "call\tcow_prepare_write_p, cow_prepare_write",
        "call\tarray_num_set_cow_idx_p, array_num_set_cow_idx"
      ],
      "count": {
        "call\tcow_mark_shared_p, cow_mark_shared": 1,
        "call\tcow_prepare_write_p, cow_prepare_write": 1
      }
    }
  ]
}
