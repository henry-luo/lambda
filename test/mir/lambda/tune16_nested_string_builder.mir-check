{
  "schema_version": 1,
  "description": "Tune16 C5 flattens a nested concatenation RHS without routing the inner join through fn_join.",
  "checks": [
    {
      "in_func": "_main_#",
      "expect": [
        "fn_strcat_many"
      ],
      "forbid": [
        "call\tfn_join_p, fn_join"
      ]
    }
  ]
}
