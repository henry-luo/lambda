// Phase III B1: an owned five-piece append takes the fixed-arity builder path.
pn main() {
    var result: string = ""
    let a: string = "a"
    let b: string = "b"
    let c: string = "c"
    let d: string = "d"
    result = result ++ a ++ b ++ c ++ d
    print(result)
    print("\n")
}
