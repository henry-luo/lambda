{
  "schema_version": 1,
  "description": "Tune26 T26-4: a plain bool[] snapshot marks at entry and uses the compact COW setter on the first reached store, without the cloning checked-contract setter.",
  "checks": [
    {
      "in_func": "_tune26_plain_bool_param_lazy_snapshot_#",
      "expect": [
        "call\tcow_mark_shared_p, cow_mark_shared",
        "call\tarray_num_set_cow_idx_p, array_num_set_cow_idx"
      ],
      "forbid": [
        "call\tcow_prepare_write_p, cow_prepare_write",
        "call\tlambda_array_set_checked_lane_p, lambda_array_set_checked_lane"
      ],
      "count": {
        "call\tcow_mark_shared_p, cow_mark_shared": 1
      }
    }
  ]
}
