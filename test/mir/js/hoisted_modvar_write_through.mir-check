{
  "schema_version": 1,
  "description": "A hoisted inner function declaration inside an IIFE stores through to its module-var slot right after the function object is created, so a nested closure resolving it through the module var sees the real callable (Stage 4C LambdaJS bug #4).",
  "checks": [
    {
      "in_func": "_js_anon1_#_body",
      "expect_seq": [
        { "pattern": "call\tjs_new_function_p" },
        { "pattern": "call\tjs_set_module_var_p" }
      ],
      "expect": [
        ", _js_helper_"
      ],
      "count": {
        "call\tjs_set_module_var_p": { "min": 1 }
      }
    }
  ]
}
